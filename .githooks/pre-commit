#!/usr/bin/env bash
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"

# 1. Manifest conformance (project-specific)
.claude/scripts/manifest-conformance.sh

# 2. Tests
pytest --quiet

# 3. Lint
ruff check && ruff format --check
