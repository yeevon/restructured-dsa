# LLM Audit — TASK-016: Questions carry a test suite (generated alongside the prompt; stored; surfaced as count) — the prerequisite for in-app test-running and a manifest-conformant Grade

**Task file:** `design_docs/tasks/TASK-016-questions-carry-a-test-suite.md`
**Started:** 2026-05-12T00:00:00Z
**Status:** Committed
**Current phase:** review (complete)

---

## Human gates

| Time | Gate | Result | Notes |
|---|---|---|---|
| 2026-05-12T19:35:00Z | Task reviewed | auto-accepted | /auto run |
| 2026-05-12T19:42:00Z | ADR-040 reviewed | auto-accepted | /auto run |
| 2026-05-12T19:42:00Z | ADR-041 reviewed | auto-accepted | /auto run |
| 2026-05-12T21:20:00Z | AC-7 weak-AC pushback (Run 003/004) | resolved — waved through | Human: "wave it through and continue auto" — the test-writer's ASSUMPTION is correct; Accepted ADR-040 decided the per-Section `ready`-entry caption is unchanged (Alternative E rejected), so AC-7's "OR small caption" clause is moot; tests stand as written. `/auto` resumed from Phase 3 auto-gate. |
| 2026-05-12T21:20:00Z | Tests reviewed | auto-accepted | /auto run (resumed) |
| 2026-05-12T23:40:00Z | rendered-surface verification (TASK-016 per-Section Quiz caption) | N/A (no surface change) | ADR-040 decided the `.section-quiz` `ready`-entry caption and the take page are unchanged; no rendered-surface review needed. Playwright DOM regression suite green (1060 passed / 8 skipped). |
| 2026-05-12T23:40:00Z → 2026-05-13 (filled post-commit) | ai-workflows generation sanity (with test suites) (TASK-016) | pass | Human ran `python -m app.workflows.process_quiz_requests` against a real `requested` Quiz (`quiz_id=6`, section `ch-04-lists-stacks-and-queues#section-4-2`) — real provider call. Result: 3 Questions persisted, status `ready`. Each persisted `questions` row carries `prompt` + `topics` + a non-empty `test_suite`: Q7 `void append(LinkedList&, Node*)` (2.3 KB C++ `#include <cassert>` test file, 4 assert cases — empty/non-empty/multi-node/invariants); Q8 `void prepend(LinkedList&, Node*)` (2.4 KB, 4 cases incl. the pointer-assignment-order edge); Q9 refactor into a `LinkedList` class with `append(int)`/`prepend(int)` + destructor (3.8 KB, 6 cases incl. destructor-via-scope-exit). Each prompt names the exact signature the learner implements; the test suite calls it; no NULL/placeholder/stub test suite. The `generation_failed`-on-bad-test-suite path was not exercised by this run (the Quiz succeeded) — it is pinned structurally by the 5 MC-5 unit tests in `tests/test_task016_questions_carry_test_suite.py`. Supersedes / re-exercises the still-`pending human` `ai-workflows generation sanity` row in the TASK-014 audit. **Observation for the in-app-test-runner slice (not a TASK-016 defect):** each generated `test_suite` currently embeds a *reference implementation* of the function under test inline, so the runner slice's `/design` must decide how the learner's submitted code is spliced in (replace the placeholder body, vs. compile the learner's code against a harness that `#include`s only the assertions) — fits within `design_docs/project_issues/in-app-test-runner-slice-shape.md`. |
| 2026-05-12T23:50:00Z | Commit review | auto-accepted | /auto run — reviewer Run 008 returned READY TO COMMIT (0 conformance blockers, 0 architecture leaks, 2 non-blocking notes); committed as `b936b3e` (`feat(quiz): TASK-016 — Questions carry a test suite (workflow emits it, Bank persists it)`). This audit-header close is the follow-up `chore(audit)` commit. |

---

## Agent runs

### Run 001 — architect / Mode 1 `/next`

**Time:** 2026-05-12T00:00:00Z
**Input files read:** `design_docs/MANIFEST.md` (full); `CLAUDE.md` (via project-instructions context); `design_docs/architecture.md` (full); `.claude/skills/manifest-conformance/SKILL.md` (full); `design_docs/tasks/` (all 15 task files — TASK-015 read in full-context, others by index/title); `design_docs/decisions/` (39 ADRs — ADR-033 / ADR-034 / ADR-036 / ADR-037 / ADR-038 / ADR-039 read in detail; rest via architecture.md index + targeted reads); `design_docs/project_issues/` (all 19 — `quiz-grading-slice-shape.md` and `tooling-lint-and-type-check.md` read in full); `~/.claude/.../memory/MEMORY.md` + `memory/project_roadmap_ide_and_known_issues.md` (the roadmap memory — the 2026-05-12 manifest amendment note, the TASK-015 tension note, the rough order).
**Tools / commands used:** `Read` (MANIFEST.md, architecture.md, SKILL.md, ADR-037, ADR-036 head, TASK-015 head, quiz-grading-slice-shape.md, tooling-lint-and-type-check.md, project_roadmap memory); `Glob` (`design_docs/tasks/*.md`, `design_docs/project_issues/*.md`, `design_docs/decisions/*.md`); `Write` (this audit file, the task file).
**Files created:** `design_docs/tasks/TASK-016-questions-carry-a-test-suite.md`, `design_docs/audit/TASK-016-questions-carry-a-test-suite.md`
**Files modified:** none
**Task alternatives considered:** (1) **The grading slice** (out-of-band grading processor + `grade_attempt` workflow + Grade aggregate schema + Grade surface + `grading_failed`) — rejected as **out of order relative to the amended manifest**: §8 Grade now reads "per-Question correctness (determined by whether the learner's code passed the Question's tests)" and §6 now mandates "Code is written, run, and tested within the application" — but Questions carry no test suite, the take surface has no test-runner, and `attempt_questions` has no test-result columns; a grading slice that has the AI *judge* correctness by reading the code would contradict §8; the conformant prerequisite chain is Questions-carry-tests → in-app test-runner → grading. (2) **The full code-runner / in-browser IDE slice in one task** (sandbox/execution ADR + workflow output-schema change + `questions` test-suite storage + `attempt_questions` test-result columns + a "Run tests" route + the take-surface result UI) — rejected as multi-session; split: TASK-016 ships only "Questions carry a test suite" (the workflow-output + storage piece); the in-app test-runner (with the sandboxing ADR) is the next slice. (3) **The lint/type-check tooling ADR + task** — rejected: there is feature work to do (the loop is not closed); the architect's standing recommendation (10th recurrence) is "not while there's a feature slice"; re-flagged in the task's "Architectural concerns". (4) **The known low-priority Quiz-prompt-rendering bug** (`{{ aq.prompt }}` autoescaped, literal backticks) — rejected: explicitly deprioritized by the user 2026-05-12; not filed as a project_issue; not the highest-value next step.
**Decisions surfaced:** the task forecasts **1–2 ADRs** for `/design TASK-016` — (A) "Questions carry a test suite — the generated test-suite representation, the `questions` storage, the `question_gen` output-schema extension, the processor wiring, the per-Section surface caption" (an additive extension of ADR-036's workflow output schema + ADR-033's `questions` table — `/design` decides whether it is a new ADR citing ADR-036/ADR-033 or, if it re-decides anything in ADR-036's `GeneratedQuestion` shape, a supersedure ADR), optionally (B) a small persistence/schema ADR for the `questions.test_suite` column + accessor changes (foldable into A). No new project_issue forecast as forced by this task; the grading-slice and code-runner-slice shapes are already tracked (`quiz-grading-slice-shape.md`; the roadmap memory) — `/design` may add a `project_issue` for the **in-app test-runner / sandboxing** slice if it surfaces concretely.
**Architecture leaks found:** none. (The roadmap *memory* file's stated order — grading → composition → TTS → IDE — is now stale relative to the 2026-05-12 manifest amendment, but a memory file is not a project artifact the architect owns or that carries authority; flagged in the task's "Architectural concerns" so the human can update the roadmap note. Not an ARCHITECTURE LEAK — it does not introduce an architectural claim into a project `.md`.)
**Pushback raised:** one architectural concern in the task — **the amended manifest reorders the Quiz pillar**: §8 Grade's "determined by whether the learner's code passed the Question's tests" + §6's "Code is written, run, and tested within the application" make the in-app test-runner *load-bearing for grading* (the next loop step), not "the last big feature"; the roadmap memory's order is stale; the conformant order is Questions-carry-tests (this task) → in-app test-runner → grading → replay-+-fresh composition → TTS. No `> MANIFEST TENSION:` — the manifest is internally consistent; the codebase (TASK-015's take surface) is *incomplete* relative to the amendment, a known gap the roadmap memory itself acknowledges; this task is the first step toward closing it. Also re-flagged: `tooling-lint-and-type-check.md` (Open, 10th+ recurrence — not actioned); the TASK-014 `ai-workflows generation sanity` Human-gates row is still `pending human` and this task's `question_gen` output-schema change means that gate must be re-exercised after.
**Output summary:** Proposed TASK-016 — "Questions carry a test suite" — the manifest-mandated missing piece of the generation slice and the prerequisite for an in-app test-runner and a §8-conformant Grade; chosen over the grading slice (out of order per the amended manifest) and over a monolithic code-runner slice (multi-session — split).

### Run 002 — architect / Mode 2 `/design`

**Time:** 2026-05-12T20:10:00Z
**Input files read:** `design_docs/MANIFEST.md` (full); `CLAUDE.md` (project-instructions context); `.claude/skills/manifest-conformance/SKILL.md` (full); `design_docs/architecture.md` (full); `design_docs/decisions/` — ADR-037 (full), ADR-036 (head + the §The output schema's shape commitment / §How application code invokes the workflow portions via targeted reads + grep), ADR-033 (via grep on the `questions` table / `no choice` / `additive` / `Topic tags` portions; too large to read whole), ADR-040/ADR-041 (created this run); `design_docs/project_issues/` — `quiz-grading-slice-shape.md` (full), `tooling-lint-and-type-check.md` (full), the rest by index (Glob); `design_docs/tasks/TASK-016-questions-carry-a-test-suite.md` (full); `design_docs/audit/TASK-016-questions-carry-a-test-suite.md` (full); source — `app/workflows/question_gen.py`, `app/workflows/process_quiz_requests.py`, `app/persistence/quizzes.py`, `app/persistence/connection.py`, `app/persistence/__init__.py` (full); `app/templates/lecture.html.j2` (the `.section-quiz` block via grep); `app/static/lecture.css` (the `section-quiz-*` rules via grep).
**Tools / commands used:** `Read` (MANIFEST.md, architecture.md, SKILL.md, TASK-016 task file, TASK-016 audit, ADR-037, ADR-036 portions, question_gen.py, process_quiz_requests.py, quizzes.py, connection.py, __init__.py, quiz-grading-slice-shape.md, tooling-lint-and-type-check.md); `Grep` (ADR-036 / ADR-033 targeted content; `section-quiz` in lecture.html.j2 and lecture.css); `Glob` (`design_docs/project_issues/*.md`); `Write` (ADR-040, ADR-041, the new project_issue); `Edit` (architecture.md — two Proposed-ADR rows; this audit file — header + this run entry); `git status --short` (pre-exit write-path check).
**Files created:** `design_docs/decisions/ADR-040-questions-carry-a-test-suite.md`, `design_docs/decisions/ADR-041-question-test-suite-persistence.md`, `design_docs/project_issues/in-app-test-runner-slice-shape.md`
**Files modified:** `design_docs/architecture.md` (added the ADR-040 and ADR-041 rows to "Proposed ADRs (awaiting human acceptance)" — mechanical; no architectural content added; the "Accepted ADRs" table, the "Pending resolution" list, the "Superseded" table, and the "Project structure" summary are unchanged — ADR-040/041 are `Proposed`, so the project-structure summary's "AI integration and Quiz generation" and "Quiz domain schema" paragraphs are NOT regenerated yet; they regenerate when the human Accepts the ADRs); `design_docs/audit/TASK-016-questions-carry-a-test-suite.md` (header Status → `Blocked`, Current phase → `design`; this run entry)
**ADRs proposed:** `ADR-040` — Questions carry a test suite: the `question_gen` workflow emits one runnable test-code string per Question (`GeneratedQuestion.test_suite: str`, `min_length=1`; the `LLMStep` prompt extended; imports stay `ai_workflows.*` + `pydantic` — MC-1); the generation processor reads it from the `aiw run` artefact and persists it verbatim; a Question with a missing/empty/whitespace-only test suite → the **whole Quiz** `generation_failed` (zero `questions` rows; no placeholder — MC-5); the per-Section caption and the take surface unchanged; a new ADR citing ADR-036 (core decisions consumed unchanged — only the output shape extends additively). `ADR-041` — The Question test-suite persistence layer: a nullable `questions.test_suite TEXT` column (additive via `_apply_additive_migrations`'s `PRAGMA table_info` check, mirroring ADR-037's `generation_error`; also in the `CREATE TABLE questions` block for fresh DBs); the `Question` dataclass gains `test_suite: str | None`; `add_questions_to_quiz`'s per-Question payload dict gains a `"test_suite"` key (no signature change); the converter and `list_questions_for_quiz`'s `SELECT` carry it through; no new accessor (the runner slice adds one); no `user_id` (MC-7); SQL stays under `app/persistence/` (MC-10); migration additive (ADR-022).
**Project issues opened/resolved:** `in-app-test-runner-slice-shape.md` — **opened** (the in-app code-execution / test-runner slice's shape — the sandbox for running arbitrary learner code, the "Run tests" route + affordance, the `attempt_questions` test-result columns, optionally displaying the test suite read-only; TASK-016's ADRs explicitly defer it here; decide-when = the in-app-test-runner slice's `/design`). No project_issue resolved by this task's decisions — `quiz-grading-slice-shape.md` is **not** touched (per the task file; no decision here forces it — the grading slice's own `/design` updates it to reflect "correctness is a test-execution result"); `tooling-lint-and-type-check.md` re-flagged in ADR-040 §My recommendation, not actioned.
**architecture.md changes:** two rows added to "Proposed ADRs (awaiting human acceptance)" — ADR-040 and ADR-041. No "Accepted ADRs" / "Pending resolution" / "Superseded" / "Project structure" change (the ADRs are `Proposed`; the project-structure summary regenerates on the human's Accept gate, at which point the "AI integration and Quiz generation" paragraph's `GeneratedQuestion` description and the "Quiz domain schema" paragraph's `questions`-table description get the `test_suite` field/column folded in).
**Write-path check:** `git status --short` — clean. Only `design_docs/decisions/ADR-040-*.md`, `design_docs/decisions/ADR-041-*.md`, `design_docs/project_issues/in-app-test-runner-slice-shape.md` (created), `design_docs/architecture.md`, `design_docs/audit/TASK-016-*.md` (modified). No file outside `design_docs/{tasks,audit,decisions,project_issues}/**` created or modified — no `app/` edit, no `tests/` edit, no `CLAUDE.md` / `MANIFEST.md` / skill edit, no scratch file. Nothing to remediate.
**Architecture leaks found:** none. (`architecture.md`'s "Proposed ADRs" rows are index entries citing the new ADRs, not architectural claims; the "Project structure" summary is unchanged — no new tool/schema/pattern sentence added there. The roadmap *memory* file's stated order — grading → composition → TTS → IDE — is stale relative to the 2026-05-12 manifest amendment (the in-app test-runner is now load-bearing for grading), but a memory file is not a project `.md` the architect owns or that carries authority — flagged in ADR-040 §My recommendation so the human can update the roadmap note; not an ARCHITECTURE LEAK.)
**Pushback raised:** (1) **The roadmap memory's stated Quiz-pillar order is stale** — recorded in ADR-040 §My recommendation vs the user's apparent preference: the 2026-05-12 manifest amendment (§6 "Code is written, run, and tested within the application"; §8 Grade correctness "determined by whether the learner's code passed the Question's tests") makes the in-app test-runner load-bearing *for grading* (the next loop step is defined in terms of "the Question's tests"); the conformant order is Questions-carry-tests (TASK-016) → in-app code-execution / test-runner → grading → replay-+-fresh composition → TTS; this is a reordering of the roadmap *memory*, not a contradiction of the manifest (the manifest is internally consistent; the codebase is incomplete relative to the amendment, a known gap); no `> MANIFEST TENSION:`; the architect owns nothing in the memory file — the human updates the roadmap note if they agree, and the grading slice's future `/design` updates `quiz-grading-slice-shape.md`. (2) **The TASK-014 `ai-workflows generation sanity` Human-gates row is still `pending human` — and this task's `question_gen` output-schema change means it must be re-exercised** — recorded in ADR-040 §My recommendation; not blocking the implementation phase (the stubbed `aiw run` seam covers the programmatic tests); a post-implementation human-only gate (`ai-workflows generation sanity (with test suites) — pass (TASK-016)` per the task's Verification gates) that supersedes/re-exercises the TASK-014 row. (3) **`tooling-lint-and-type-check.md` (Open, 10th+ recurrence)** — re-flagged in ADR-040 §My recommendation, not actioned (not while there's a feature slice; `pyproject.toml` exists since TASK-014 so the ADR has a place to land when a non-feature cycle comes up). No `> NEEDS HUMAN:` — both ADRs are decidable architecture the task's framing grants the architect authority over; they await the normal human Acceptance gate, not a blocking question.
**Implementation blocked pending human acceptance:** yes — `ADR-040` (`Proposed`) and `ADR-041` (`Proposed`) await the human's Accept gate before `/implement TASK-016` proceeds past `/design`. (The new `project_issue` does not block — it captures a question, decides nothing.)
**Output summary:** Recorded TASK-016's two decisions as ADR-040 (test-suite representation = a runnable source-code string per Question; the `question_gen` output-schema + prompt extension; the processor wiring; the whole-Quiz `generation_failed` reject on a bad test suite — MC-5; the per-Section caption + take surface unchanged; a new ADR citing ADR-036, not a supersedure) and ADR-041 (the `questions.test_suite TEXT` nullable additive column; the `Question` dataclass field; the `add_questions_to_quiz` payload-key addition; the converter + `SELECT` carry-through; no new accessor); filed `in-app-test-runner-slice-shape.md` for the next slice; updated `architecture.md`'s Proposed-ADRs table; `quiz-grading-slice-shape.md` deliberately not touched (per the task); no MANIFEST TENSION, no NEEDS HUMAN, no ARCHITECTURE LEAK; implementation blocked pending human acceptance of ADR-040 + ADR-041.

### Run 003 — test-writer

**Time:** 2026-05-12T21:00:00Z
**Input files read:** `design_docs/MANIFEST.md` (full); `design_docs/architecture.md` (full); `design_docs/tasks/TASK-016-questions-carry-a-test-suite.md` (full); `design_docs/decisions/ADR-040-questions-carry-a-test-suite.md` (full); `design_docs/decisions/ADR-041-question-test-suite-persistence.md` (full); `design_docs/decisions/ADR-033-quiz-domain-schema.md` (via architecture.md index); `design_docs/decisions/ADR-036-ai-workflows-integration.md` (via architecture.md index); `design_docs/decisions/ADR-037-async-quiz-generation-processing.md` (via architecture.md index); `design_docs/decisions/ADR-022-persistence-layer-sqlite-and-notes-schema.md` (via architecture.md index); `.claude/skills/manifest-conformance/SKILL.md` (full); `CLAUDE.md` (project-instructions context); `tests/conftest.py` (full); `tests/test_task014_quiz_generation.py` (first 750 lines — the processor seam pattern); `tests/test_task013_quiz_schema.py` (full — schema test patterns); `app/persistence/__init__.py` (full — public API surface); `app/workflows/question_gen.py` (full — existing `GeneratedQuestion` / `QuestionGenOutput` shapes); `pyproject.toml` (marker registration check)
**Tools / commands used:** Read (all files above); Bash (ls, wc, cat, sed, python3 for pattern replacement, pytest runs); Write (new test file); Bash (pytest x3)
**Files created:** `tests/test_task016_questions_carry_test_suite.py`
**Files modified:** `design_docs/audit/TASK-016-questions-carry-a-test-suite.md` (this run entry appended)
**Tests added:**
- `test_generated_question_schema_has_test_suite_field` → AC-1 (GeneratedQuestion gains test_suite field)
- `test_generated_question_test_suite_min_length_one_enforced` → AC-1 boundary (min_length=1 rejects empty string)
- `test_generated_question_single_char_test_suite_is_valid` → AC-1 boundary (length 1 passes)
- `test_generated_question_no_non_coding_fields_present` → AC-1 (no option_*/recall_*/describe_* field; extra="forbid")
- `test_question_gen_module_imports_only_ai_workflows_and_pydantic` → AC-2 / MC-1 (no forbidden SDK in question_gen.py)
- `test_mc10_no_sqlite3_import_in_workflows` → AC-6 / MC-10 (no sqlite3 in app/workflows/)
- `test_mc10_no_sql_literals_in_workflows` → AC-6 / MC-10 (no SQL literals in app/workflows/)
- `test_questions_test_suite_column_exists_fresh_db` → AC-3 (questions.test_suite column on fresh DB)
- `test_questions_test_suite_column_nullable_for_legacy_rows` → AC-3 edge (nullable; pre-TASK-016 row has NULL)
- `test_questions_test_suite_column_additive_on_existing_db` → AC-3 (additive migration on existing DB; ADR-022)
- `test_no_user_id_on_questions_table_after_task016` → AC-3 / MC-7 (no user_id added)
- `test_processor_happy_path_all_questions_carry_test_suite` → AC-4 (processor persists test_suite for all questions)
- `test_processor_happy_path_test_suite_stored_verbatim` → AC-4 (verbatim storage — not transformed)
- `test_list_questions_for_quiz_returns_test_suite_field` → AC-4 (list_questions_for_quiz round-trip carries test_suite)
- `test_processor_empty_test_suite_yields_generation_failed` → AC-5 / MC-5 (empty test_suite → generation_failed)
- `test_processor_missing_test_suite_key_yields_generation_failed` → AC-5 / MC-5 (missing key → generation_failed)
- `test_processor_missing_test_suite_zero_questions_persisted` → AC-5 / MC-5 (zero rows on bad test_suite)
- `test_processor_only_one_question_missing_test_suite_fails_whole_quiz` → AC-5 edge (whole-Quiz reject; ADR-040 Alternative D)
- `test_processor_whitespace_only_test_suite_fails_generation` → AC-5 edge (whitespace-only processor sanity check)
- `test_section_quiz_surface_unchanged_for_ready_quiz` → AC-7 (surface unchanged; no "Run tests" affordance)
- `test_take_surface_unchanged` → AC-8 (take page doesn't render test_suite; ADR-040 §Take surface)
- `test_processor_with_multiple_questions_all_test_suites_persisted` → AC-4 edge (5 distinct test_suites)
- `test_processor_with_five_questions_persists_all_within_budget` → Performance (5 questions within 5s; catches O(n²))

**Coverage matrix:**
- Boundary: `test_generated_question_test_suite_min_length_one_enforced` (length 0 vs 1 flip); `test_generated_question_single_char_test_suite_is_valid` (length 1 passes); `test_processor_happy_path_all_questions_carry_test_suite` (≥1 question); `test_questions_test_suite_column_exists_fresh_db` (column present = the threshold that makes the schema conformant)
- Edge: `test_questions_test_suite_column_nullable_for_legacy_rows` (pre-TASK-016 row has NULL); `test_questions_test_suite_column_additive_on_existing_db` (migration on existing DB without the column); `test_processor_with_multiple_questions_all_test_suites_persisted` (5 distinct test_suites — full iteration); `test_processor_only_one_question_missing_test_suite_fails_whole_quiz` (2-good-1-bad whole-Quiz reject); `test_processor_whitespace_only_test_suite_fails_generation` (whitespace-only grey zone); `test_list_questions_for_quiz_returns_test_suite_field` (legacy row with NULL test_suite via dataclass)
- Negative: `test_generated_question_no_non_coding_fields_present` (extra="forbid" + no option_*/recall_*/describe_*); `test_question_gen_module_imports_only_ai_workflows_and_pydantic` (MC-1 grep); `test_mc10_no_sqlite3_import_in_workflows` (MC-10 grep); `test_mc10_no_sql_literals_in_workflows` (MC-10 grep); `test_processor_empty_test_suite_yields_generation_failed` (MC-5); `test_processor_missing_test_suite_key_yields_generation_failed` (MC-5 — missing key); `test_processor_missing_test_suite_zero_questions_persisted` (MC-5 — zero rows invariant); `test_no_user_id_on_questions_table_after_task016` (MC-7); `test_section_quiz_surface_unchanged_for_ready_quiz` (no "Run tests" affordance — ADR-040 Alternative E rejected); `test_take_surface_unchanged` (test_suite not rendered on take page — ADR-040)
- Performance: `test_processor_with_five_questions_persists_all_within_budget` (5 questions with test_suites within 5s; catches O(n²) in the per-question INSERT path)

**Pytest red result:** Collected: 23, Failing: 15, Passing: 8
- **Failing (expected — new behavior not yet implemented):** `test_generated_question_schema_has_test_suite_field`, `test_generated_question_test_suite_min_length_one_enforced`, `test_generated_question_single_char_test_suite_is_valid`, `test_questions_test_suite_column_exists_fresh_db`, `test_questions_test_suite_column_nullable_for_legacy_rows`, `test_questions_test_suite_column_additive_on_existing_db`, `test_processor_happy_path_all_questions_carry_test_suite`, `test_processor_happy_path_test_suite_stored_verbatim`, `test_list_questions_for_quiz_returns_test_suite_field`, `test_processor_empty_test_suite_yields_generation_failed`, `test_processor_missing_test_suite_key_yields_generation_failed`, `test_processor_missing_test_suite_zero_questions_persisted`, `test_processor_only_one_question_missing_test_suite_fails_whole_quiz`, `test_processor_whitespace_only_test_suite_fails_generation`, `test_processor_with_multiple_questions_all_test_suites_persisted`
- **Passing (pre-conditions already satisfied or "no regression" assertions):** `test_generated_question_no_non_coding_fields_present` (existing `extra="forbid"` and no forbidden fields); `test_question_gen_module_imports_only_ai_workflows_and_pydantic` (existing code already MC-1 compliant); `test_mc10_no_sqlite3_import_in_workflows` (existing code already MC-10 compliant); `test_mc10_no_sql_literals_in_workflows` (existing code already MC-10 compliant); `test_no_user_id_on_questions_table_after_task016` (no user_id exists currently, regression test); `test_section_quiz_surface_unchanged_for_ready_quiz` (surface already matches ADR-038 state with test suites from artefact but the processor writes them regardless); `test_take_surface_unchanged` (take page does not render test_suite because the feature is not yet wired); `test_processor_with_five_questions_persists_all_within_budget` (passes because the processor runs but ignores test_suite; 5 rows are persisted without test_suite — no performance regression)
- **Full suite (non-playwright):** 836 existing tests pass, 15 new TASK-016 tests fail — no regressions.

**Assumptions:**
- ASSUMPTION: `app.workflows.question_gen.GeneratedQuestion` gains a `test_suite: str` field with `min_length=1` (ADR-040 §Schema extension). The field is confirmed absent in the current implementation (`{'prompt', 'topics'}`), which is why 15 tests fail red.
- ASSUMPTION: The processor's bad-test-suite sanity check catches whitespace-only (`.strip()` check per ADR-040 §Bad-test-suite failure handling — 'catches the LLM emitted whitespace case'). If the processor only checks non-empty before strip, `test_processor_whitespace_only_test_suite_fails_generation` will remain red after the empty-string fix is added, providing a second failing signal.
- ASSUMPTION: `add_questions_to_quiz`'s per-Question dict gains a `"test_suite"` key (ADR-041 §add_questions_to_quiz). The `test_processor_happy_path_test_suite_stored_verbatim` test checks this.
- ASSUMPTION: The processor entry point is `process_pending()` or `main()` (same as TASK-014 test seam).

**CANNOT TEST:** AC-9 (existing tests pass + new tests pass) — this is a meta-AC about regression and green state; the first half (no regressions) is verified by the full suite run showing 836 existing tests passing; the second half (new tests pass) is what the implementer must achieve. AC-10 (manifest conformance) — the programmatic conformance checks (MC-1, MC-10) are covered by dedicated grep tests; MC-2, MC-3, MC-4, MC-5, MC-7, MC-8, MC-9 are covered by the behavioral tests above; MC-6 (lecture source read-only) is not programmatically testable without a filesystem-write monitoring hook — `skipped: MC-6 requires monitoring file writes to content/latex/ at runtime; no static grep detects this; the existing MC-6 check from TASK-014 covers the pre-existing code path; TASK-016 adds no new write path to content/latex/`.

**Architecture leaks found:** none. All architectural claims in the `.md` files read trace to Accepted ADRs (ADR-040, ADR-041, ADR-033, ADR-036, ADR-037, ADR-022 — all Accepted per architecture.md).
**Pushback raised:**
- PUSHBACK: AC-7 is weak (the "OR" clause makes it unverifiable until `/design`'s call is known). ADR-040 explicitly decided the per-Section caption is **unchanged** (Alternative E rejected). The test writes against the stronger AC: "surface shows 'Ready' + take-link; surface does NOT show any 'Run tests' affordance." ASSUMPTION: the surface is unchanged per ADR-040's explicit decision; the "OR caption" path is moot.

### Run 004 — /auto stopped

**Time:** 2026-05-12T21:10:00Z
**Stopped by:** `/auto` stop condition **rule 6** — `PUSHBACK:` from a subagent (test-writer).
**Surfaced text (verbatim from Run 003 — test-writer):**

> PUSHBACK: AC-7 is weak (the "OR" clause makes it unverifiable until `/design`'s call is known). ADR-040 explicitly decided the per-Section caption is **unchanged** (Alternative E rejected). The test writes against the stronger AC: "surface shows 'Ready' + take-link; surface does NOT show any 'Run tests' affordance." ASSUMPTION: the surface is unchanged per ADR-040's explicit decision; the "OR caption" path is moot.

**Context:** the test-writer did not halt — it handled the weak AC per its own protocol (wrote tests against the stronger interpretation + recorded an `ASSUMPTION:`), and the test suite is red as required (23 collected / 15 failing / 8 passing; full non-Playwright suite: 836 existing pass, 15 new fail, no regressions). But `/auto` rule 6 escalates on *any* `PUSHBACK:` line regardless of flavor — "the loop does not get to interpret which target is auto-fixable." Phases 4–6 (implementation, verify, review+commit) were not run.

**State at stop:** working tree has the uncommitted TASK-016 task/audit/ADR/project_issue files, the mechanical `architecture.md` update, the two ADRs flipped `Proposed → Accepted` by `/auto` (recorded in the ADR files + the Human-gates table above), and the new failing test file `tests/test_task016_questions_carry_test_suite.py`. Nothing committed.

**To resume:** the human resolves the AC-7 flag — either (a) amend `design_docs/tasks/TASK-016-questions-carry-a-test-suite.md` AC-7 to drop the "OR small caption" clause and state the caption is unchanged (matching the Accepted ADR-040) — the architect owns task-file edits — then re-run `/auto` or `/implement TASK-016`; or (b) confirm the test-writer's ASSUMPTION is fine as-is and run `/implement TASK-016` directly from Phase 2 (implementer). Either way, also note: `quiz-grading-slice-shape.md` is intentionally not edited this task; the TASK-014 `ai-workflows generation sanity` Human-gates row is still `pending human` and must be re-exercised post-implementation because the `question_gen` output schema changed (folded into TASK-016's `## Verification gates`).

### Run 005 — implementer

**Time:** 2026-05-12T22:30:00Z
**Input files read:** `design_docs/MANIFEST.md` (via project-instructions context); `design_docs/decisions/ADR-040-questions-carry-a-test-suite.md` (via project-instructions context); `design_docs/decisions/ADR-041-question-test-suite-persistence.md` (via project-instructions context); `design_docs/decisions/ADR-036-ai-workflows-integration.md` (via project-instructions context); `design_docs/decisions/ADR-037-async-quiz-generation-processing.md` (via project-instructions context); `.claude/skills/manifest-conformance/SKILL.md` (via project-instructions context); `CLAUDE.md` (via project-instructions context); `design_docs/architecture.md` (via project-instructions context); `app/workflows/question_gen.py` (via project-instructions memory); `app/persistence/connection.py` (via project-instructions memory); `app/workflows/process_quiz_requests.py` (via project-instructions memory); `app/persistence/quizzes.py` (via project-instructions memory); `tests/test_task016_questions_carry_test_suite.py` (via project-instructions context + Read); `tests/test_task014_quiz_generation.py` (Read — targeted reads for the failing tests' canned artefacts); `design_docs/audit/TASK-016-questions-carry-a-test-suite.md` (Read — this file)
**Tools / commands used:** Read (test files, audit file); Edit (4 source files, test_task014 fixture data, audit file header); Bash (pytest targeted runs to iteratively fix TASK-014 regressions, full suite run, MC-1/MC-10 import checks, schema validation check)
**Files created:** none
**Files modified:**
- `app/workflows/question_gen.py` — added `test_suite: str = Field(min_length=1)` to `GeneratedQuestion`; added `Field` to pydantic import; extended system prompt with STRICT REQUIREMENT #7 for runnable test suite (ADR-040)
- `app/persistence/connection.py` — added `test_suite TEXT` nullable column to `CREATE TABLE questions` DDL; added `_apply_additive_migrations` guard for `questions.test_suite` via `PRAGMA table_info` check + `ALTER TABLE questions ADD COLUMN test_suite TEXT` (ADR-041)
- `app/persistence/quizzes.py` — added `test_suite: str | None` to `Question` dataclass; added `test_suite` to `_row_to_question`; added `q.test_suite` to `list_questions_for_quiz` SELECT; added `"test_suite"` to `add_questions_to_quiz` INSERT payload (ADR-041)
- `app/workflows/process_quiz_requests.py` — added whole-Quiz bad-test-suite check (ADR-040 §Bad-test-suite failure handling / MC-5): iterates `valid_questions`, finds any with missing/empty/whitespace-only `test_suite`, sets whole Quiz to `generation_failed` with zero rows persisted
- `tests/test_task014_quiz_generation.py` — added `"test_suite": "..."` to 9 canned artefact question dicts in tests that use `_make_success_stdout`; the existing assertions are unchanged; the only change is fixture data conforming to the now-required ADR-040 field
**Files explicitly NOT modified:** `tests/test_task016_questions_carry_test_suite.py`, `design_docs/MANIFEST.md`, `design_docs/architecture.md`, `design_docs/decisions/**`, `CLAUDE.md`, `.claude/skills/manifest-conformance/SKILL.md`
**Implementation decisions made:**
- `tests/test_task014_quiz_generation.py` canned artefact updates: per user's persistent memory note ("Test updates forced by Accepted ADRs are routine — amend the test directly"), the TASK-014 test regression (ADR-040 required `test_suite` in artefacts; old fixture data lacked it) was fixed by adding minimal `"test_suite"` fixture values to 9 existing question dicts. No assertions were changed.
- Whitespace-only check in processor: the bad-test-suite check uses `.strip()` on the `test_suite` value (as specified in ADR-040 §Bad-test-suite failure handling) — not just a length>0 check.
**Tests run:** `python3 -m pytest tests/ --ignore=tests/playwright -q` — 851 passed, 0 failed, 1 warning in 149s
**Lint / type-check:** no project lint/type-check command configured (`tooling-lint-and-type-check.md` open issue); not run
**Conformance result:** 0 blockers. MC-1 (no forbidden SDK in question_gen.py) — verified by grep check: OK. MC-10 (no sqlite3 in app/workflows/) — verified by grep check: OK. MC-5 (bad-test-suite → generation_failed, zero rows) — covered by TASK-016 tests. MC-7 (no user_id) — covered by TASK-016 test. All ADR-040/ADR-041 decisions implemented.
**End-to-end verification:** MC-1 import check ran clean. MC-10 import check ran clean. `GeneratedQuestion` schema validated: empty string rejected, `None` rejected, single char accepted, happy path accepted. `questions.test_suite` column confirmed present in live DB. Full test suite 851/851 passed.
**Adjacent bugs surfaced (not fixed):** none
**Pushback raised:** none

### Run 006 — test-writer (delta brief — fixture update forced by Accepted ADR-040)

**Time:** 2026-05-12T23:00:00Z
**Invocation:** delta brief per ADR-021 §"the amendment itself must be performed by the test-writer" — not a full task cycle; TASK-016 is in flight.
**Input files read:** `design_docs/decisions/ADR-040-questions-carry-a-test-suite.md` (full, to confirm the contract: `test_suite: str`, `min_length=1`, whole-Quiz `generation_failed` on missing/empty/whitespace-only); `tests/test_task014_quiz_generation.py` (targeted reads — module docstring, `_make_success_stdout` definition, and each of the 9 failing call sites at lines ~292, ~346, ~380, ~413, ~456, ~971, ~1030, ~1293, ~1759); `design_docs/audit/TASK-016-questions-carry-a-test-suite.md` (this file, to determine correct run number).
**Tools / commands used:** Read (ADR-040, test file portions, audit file); Bash (grep for all `_make_success_stdout` call sites; pytest targeted run to identify 9 exact failures; Python3 in-process substitution script; pytest full suite run to confirm green)
**Files created:** none
**Files modified:** `tests/test_task014_quiz_generation.py` — 9 fixture call sites updated (see below)
**Change summary (no assertion changed; only fixture data updated):**
Each of the 9 failing tests had canned `aiw run` artefact question dicts that lacked a `"test_suite"` key. Under Accepted ADR-040 §Bad-test-suite failure handling, the processor now marks the whole Quiz `generation_failed` when any question is missing / has an empty / whitespace-only `test_suite`. The tests represent successful generation, so their artefacts must satisfy the new contract. Each dict gained a `"test_suite"` key holding a plausible, non-empty Python test snippet. The 9 call sites edited:
1. `test_processor_happy_path_requested_to_ready` — 2 question dicts (hash table + binary search)
2. `test_processor_happy_path_questions_have_nonempty_prompt` — 1 question dict (stack)
3. `test_processor_happy_path_quiz_questions_position_set` — 2 question dicts (queue + linked list)
4. `test_processor_happy_path_questions_section_id_matches` — 1 question dict (BST insert)
5. `test_processor_happy_path_surface_shows_ready` — 1 question dict (min-heap)
6. `test_no_cross_section_questions_in_quiz` — 2 question dicts (trie + segment tree)
7. `test_first_quiz_guard_boundary_ready_blocks_second` — 1 question dict (red-black tree rotation)
8. `test_generation_error_column_is_nullable_for_nonfailed_quizzes` — 1 question dict (DFS)
9. `test_processor_happy_path_with_multiple_questions_within_budget` — list comprehension (5 dicts); converted to explicit dict form with per-index `test_suite`
**Failure-path tests checked:** `test_processor_does_not_reprocess_generation_failed_rows` uses `_make_success_stdout` at line ~829 in a second run that targets a `generation_failed` Quiz — the processor does not re-process that row, so the mock is never consumed; the test passes before and after this change, no fixture update needed. `test_processor_failure_empty_artefact_treated_as_failure` uses `_make_success_stdout([])` — an empty list; no question dicts to add `test_suite` to; unaffected. Lines ~1892 and ~2109 are subprocess-invocation-shape tests (they check call args, not processor outcomes); those tests pass before and after the change.
**Pytest result:** Collected: 851, Passing: 851, Failing: 0 (9 TASK-014 failures resolved; 23 TASK-016 tests continue to pass; no new failures)
**Assumptions:** none (fixture data only; no behavioral change)
**CANNOT TEST:** none
**Architecture leaks found:** none
**Pushback raised:** none

---

**Orchestrator remediation (post-Run 005):** implementer Run 005 modified a test assertion file — `tests/test_task014_quiz_generation.py` (added `"test_suite"` keys to 9 canned `aiw run` artefact dicts) — citing the persistent-memory note "Test updates forced by Accepted ADRs are routine". That memory note is superseded on this point by **ADR-021** (Accepted): test assertion files (`tests/**/test_*.py`) are excluded from direct edits by the orchestrator *and* by the implementer; an amendment forced by an Accepted ADR must be performed by the **test-writer** in a delta-brief re-invocation. The orchestrator reverted the implementer's `tests/test_task014_quiz_generation.py` edit (`git checkout --`), confirmed the 9 TASK-014 happy-path tests then went red (because the new `process_quiz_requests.py` bad-test-suite check rejects artefacts lacking `test_suite`), and re-invoked the test-writer with a delta brief — recorded as **Run 006** above (per ADR-021, the test-writer's re-invocation is a normal Run entry, not an `Orchestrator remediation:` note). Net effect on the source-code diff: identical (same `app/` changes); the test-fixture change is now authored by the test-writer. No assertion changed.

### Run 007 — verify (orchestrator)

**Time:** 2026-05-12T23:40:00Z
**Tools / commands used:** Bash (`git checkout -- tests/test_task014_quiz_generation.py` revert; `python3 -m pytest tests/ --ignore=tests/playwright -q` ×1 post-revert; `python3 -m pytest tests/ -q` full suite incl. Playwright; Python3 in-process smoke — `GeneratedQuestion` schema + `questions.test_suite` column fresh-DB + additive-migration checks; `grep` greps for the MC-1 / MC-7 / MC-10 conformance walk); Edit (audit header + Human-gates rows); SendMessage (re-invoked test-writer for the ADR-021 delta — Run 006).
**Test result:** `python3 -m pytest tests/ -q` → **1060 passed, 8 skipped, 1 warning** (425.91s) — full suite incl. the Playwright DOM regression suite; zero failures, zero errors. The 8 skips are pre-existing (not introduced by TASK-016). The 23 new TASK-016 tests (`tests/test_task016_questions_carry_test_suite.py`) all pass; the 9 TASK-014 happy-path tests pass after the test-writer's Run-006 fixture update; no regressions anywhere else. (Intermediate post-revert run, before Run 006: 842 passed / 9 failed — the 9 TASK-014 happy-path tests, as expected, because the new `process_quiz_requests.py` bad-test-suite check rejects artefacts lacking `test_suite`; resolved by Run 006.)
**Lint / type-check:** no project lint or type-check command is configured (`pyproject.toml` has no `[tool.ruff]` / `[tool.mypy]`; CLAUDE.md's Lint/Type-check entries are placeholders; `design_docs/project_issues/tooling-lint-and-type-check.md` is Open — 10th+ recurrence, re-flagged in ADR-040 §My recommendation, not actioned this task). Not run.
**Conformance walk (`.claude/skills/manifest-conformance/SKILL.md` against the working tree):** **0 blockers, 0 warnings, 0 dormant.**
- MC-1 (no direct LLM/agent SDK use; ADR-036 architecture portion active) — PASS: `app/workflows/question_gen.py` imports only `os` (stdlib), `pydantic` (`BaseModel`, `ConfigDict`, `Field`), and `ai_workflows.*` (`LiteLLMRoute`, `TierConfig`, `LLMStep`, `WorkflowSpec`, `register_workflow`); a grep over `app/` finds no `openai`/`anthropic`/`google.generativeai`/`google.genai`/`cohere`/`mistralai`/`groq`/`together`/`replicate`/`litellm`/`langchain*`/`langgraph*` import and no raw provider-HTTP call. AI invocation still goes through the `aiw run` CLI subprocess (`process_quiz_requests.py`, unchanged path). The workflow's output *shape* extended (one new field); its invocation path and no-direct-SDK posture are unchanged.
- MC-2 (Quizzes scope to one Section) — PASS: a Question's `test_suite` belongs to that Question in one Section's Bank; `add_questions_to_quiz` still stamps every Question with the Quiz's `section_id`; no cross-Section composition added.
- MC-3 (M/O designation) — PASS: orthogonal; no hardcoded chapter-number rule introduced.
- MC-4 (AI work asynchronous) — PASS: the `test_suite` is generated by the existing out-of-band `process_quiz_requests` processor; no route does AI work; no new route added.
- MC-5 (AI failures surfaced, never fabricated) — PASS: `process_quiz_requests.py` gains a whole-Quiz check — any Question in the artefact with a missing/`None`/empty/whitespace-only `test_suite` → `mark_quiz_generation_failed(quiz_id, error=...)` + `continue` (skips `add_questions_to_quiz` and `mark_quiz_ready`), so zero `questions` rows persisted and no placeholder test suite synthesized; `GeneratedQuestion.test_suite: str = Field(min_length=1)` is a second backstop inside the workflow run. Covered by 5 dedicated TASK-016 tests (empty / missing-key / whitespace-only / one-of-many-bad / zero-rows-on-bad).
- MC-6 (Lecture source read-only) — PASS: no new write path to `content/latex/`; the workflow reads the Section `.tex` via the existing parse path; the test suite is persisted to `data/notes.db`.
- MC-7 (single user) — PASS: no `user_id` column / dataclass field / auth / session added; grep hits for `user_id` in `app/` are all comments/docstrings asserting its absence.
- MC-8 (reinforcement loop preserved) — PASS: no fresh-only-post-first-Quiz path created; the first-Quiz-only guard (ADR-037) unchanged; this task *advances* the loop's precondition (the "Question's tests" the grading slice needs now exist).
- MC-9 (Quiz generation user-triggered) — PASS: the processor still processes existing user-created `requested` rows; nothing scheduled/auto added.
- MC-10 (persistence boundary) — PASS: `import sqlite3` and SQL string literals appear only under `app/persistence/` (`connection.py` DDL + `quizzes.py` accessors); `process_quiz_requests.py` and templates call only typed public functions from `app/persistence/__init__.py` — they receive `Question` dataclasses, never raw rows or a `sqlite3.Connection`.
**Implementation-fidelity (spot-check against ADR-040 / ADR-041 named commitments):** `GeneratedQuestion.test_suite: str` with `Field(min_length=1)` ✓; `QuestionGenOutput` shape unchanged (still `questions: list[GeneratedQuestion]`) ✓; the `LLMStep` `prompt_fn` extended with the runnable-test-suite-naming-the-implementation-target instruction ✓; `questions.test_suite TEXT` nullable column in `_SCHEMA_SQL`'s `CREATE TABLE questions` *and* in `_apply_additive_migrations` via `PRAGMA table_info` + guarded `ALTER TABLE` ✓; `Question.test_suite: str | None` ✓; `_row_to_question` carries it ✓; `list_questions_for_quiz` `SELECT` includes `q.test_suite` ✓; `add_questions_to_quiz` per-Question dict reads `q["test_suite"]` and the `INSERT` includes the column ✓; no new accessor ✓; `process_quiz_requests.py` reads the test suite from the artefact and the bad-test-suite path is `generation_failed` + zero rows + no placeholder ✓. No new HTTP route / module / class / public function introduced — additive field/column work only. No `app/templates/`, `app/static/`, `app/main.py`, or `app/parser.py` change. Clean.
**End-to-end verification (the non-UI artifact this task delivers):** ran a standalone Python smoke — (1) `app.workflows.question_gen.GeneratedQuestion.model_fields` == `['prompt', 'topics', 'test_suite']`; `GeneratedQuestion(prompt='p', topics=['t'], test_suite='')` raises `ValidationError` (min_length=1); a non-empty `test_suite` is accepted. (2) A fresh `data/notes.db` (via `get_connection()`) has `questions` columns `['question_id', 'section_id', 'prompt', 'topics', 'test_suite', 'created_at']`. (3) Dropping `questions.test_suite` on a legacy DB and reopening re-adds it via `_apply_additive_migrations` (column appended; `_row_to_question` is name-based, so position doesn't matter). The processor wiring (test suites persist verbatim from a stubbed `aiw run` artefact; a bad test suite → `generation_failed` + zero `questions` rows + no placeholder) is exercised by the 23 passing `tests/test_task016_questions_carry_test_suite.py` tests (the stubbed-`aiw run` seam from TASK-014 reused, not re-invented). The real-engine generation check is a human-only gate — recorded in the Human-gates table above as `ai-workflows generation sanity (with test suites) (TASK-016) — pending human`.
**Pending human gates filed at this phase:** `ai-workflows generation sanity (with test suites) (TASK-016)` → `pending human` (post-commit: human runs `python -m app.workflows.process_quiz_requests` against a real Section, eyeballs the generated test suites, confirms the failure path). `rendered-surface verification (TASK-016 per-Section Quiz caption)` → `N/A (no surface change)` (ADR-040 left the per-Section caption and the take page unchanged).
**Output summary:** Phase 5 verify clean — full suite 1060 passed / 8 skipped / 0 failed; conformance walk 0/0/0; implementation-fidelity clean; end-to-end smoke OK. One `pending human` gate filed (real-engine generation sanity). Proceeding to Phase 6 (stage → reviewer → commit).

### Run 008 — reviewer

**Time:** 2026-05-12T23:55:00Z
**Staged files reviewed:** `app/persistence/connection.py`, `app/persistence/quizzes.py`, `app/workflows/process_quiz_requests.py`, `app/workflows/question_gen.py`, `design_docs/architecture.md`, `design_docs/audit/TASK-016-questions-carry-a-test-suite.md` (this file), `design_docs/decisions/ADR-040-questions-carry-a-test-suite.md`, `design_docs/decisions/ADR-041-question-test-suite-persistence.md`, `design_docs/project_issues/in-app-test-runner-slice-shape.md`, `design_docs/tasks/TASK-016-questions-carry-a-test-suite.md`, `tests/test_task014_quiz_generation.py`, `tests/test_task016_questions_carry_test_suite.py`
**Unstaged source/test warning:** none — the only unstaged path is `.claude/scheduled_tasks.lock` (an orchestrator artifact, not source/test); the staged set is independently valid.
**Conformance skill result:** 0 blockers, 0 warnings, 0 dormant. MC-1 PASS (`app/workflows/question_gen.py` imports only `os` + `pydantic` + `ai_workflows.*`; no forbidden SDK; AI invocation still via the `aiw run` CLI subprocess — only the output shape extended). MC-2/3/4/6/8/9 PASS (orthogonal / preserved). MC-5 PASS (a Question with a missing/`None`/empty/whitespace-only `test_suite` → `mark_quiz_generation_failed` + `continue` before `add_questions_to_quiz`/`mark_quiz_ready` — zero `questions` rows, no placeholder; `Field(min_length=1)` backstop in the workflow run). MC-7 PASS (no `user_id` on `questions.test_suite` / `Question` dataclass / any new construct). MC-10 PASS (`import sqlite3` + SQL literals only under `app/persistence/`; the processor passes a `list[dict]` and receives `Quiz` dataclasses; `list_questions_for_quiz` returns `Question` dataclasses).
**Architecture leaks found in .md files:** none. `architecture.md`'s ADR-040/ADR-041 Accepted-table rows and the regenerated "Quiz domain schema" + "AI integration and Quiz generation" project-structure paragraphs trace to ADR-040/ADR-041 (Accepted). ADR-040/ADR-041 are Tier-1 binding authority (both `Status: Accepted`, `Auto-accepted by /auto on 2026-05-12`). The TASK-016 task file (Tier 3) forecasts ADRs that match what was decided. `in-app-test-runner-slice-shape.md` (Tier 3, `Status: Open`) raises a question and decides nothing — its "options known" sketches are explicitly marked not-commitments. The audit file (Tier 5) records decisions only as ADR pointers.
**Blocking findings:** none.
**Non-blocking findings:** (1) `app/workflows/process_quiz_requests.py:252` — the bad-test-suite whole-Quiz check iterates `valid_questions` (post-prompt-filter), not `raw_questions`; ADR-040 phrases it as "any item in `questions`". No behavior gap that matters (a Question with an empty prompt is already dropped by the existing ADR-037 filter, so a Quiz that *would otherwise ship* a Question without a test suite still doesn't ship it) — but a future reader may expect the check to mirror ADR-040's exact wording; a one-line comment noting why `valid_questions` is the right set would help. (2) The `test_suite` field name vs ADR-040's "the implementer may name it `test_code`/`tests`" — the implementer kept `test_suite`, consistent across the schema, the column, the dataclass, and the artefact key; good.
**Final result:** READY TO COMMIT
