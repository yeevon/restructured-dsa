# LLM Audit — TASK-013: Quiz domain model + per-Section Quiz surface (read side) — the first vertical slice of the reinforcement loop

**Task file:** `design_docs/tasks/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md`
**Started:** 2026-05-11T12:00:00Z
**Status:** Reviewed (the Run 005 /auto-stop PUSHBACK is resolved — see Run 006 + Run 008 + Run 009; reviewer Run 010 = READY TO COMMIT; awaiting commit)
**Current phase:** review

---

## Human gates

| Time | Gate | Result | Notes |
|---|---|---|---|
| 2026-05-11T12:10:00Z | Task reviewed | auto-accepted | /auto run |
| 2026-05-11T13:45:00Z | ADR-033 reviewed | auto-accepted | /auto run |
| 2026-05-11T13:45:00Z | ADR-034 reviewed | auto-accepted | /auto run |
| 2026-05-11T14:30:00Z | Tests reviewed | auto-accepted | /auto run — 96 tests collected (3 new files), 42 failing pre-implementation |
| 2026-05-11T16:30:00Z | ADR-035 reviewed | accepted | Human-directed cleanup ADR (no-JS over-reach). Accepted by the human; architecture.md row moved to Accepted; project-structure summary regenerated. |
| 2026-05-11T17:45:00Z | rendered-surface verification (TASK-013 per-Section Quiz surface) | pending human | Human reviews the per-Section Quiz surface on a Mandatory + an Optional Chapter (last-run Playwright screenshots under `tests/playwright/artifacts/` or the dev server directly): empty-state legible; "Generate a Quiz" button visually clear; `requested`-status display reads honestly; **no layout regression** from the `.section-end` `flex-direction: column` change. Edit this row to `pass` (or file a finding) post-commit. |

---

## Agent runs

### Run 001 — architect / Mode 1 `/next`

**Time:** 2026-05-11T12:00:00Z
**Input files read:** `design_docs/MANIFEST.md` (full); `CLAUDE.md` (provided in context + read full); `design_docs/architecture.md` (full); `.claude/skills/manifest-conformance/SKILL.md` (full); `design_docs/tasks/TASK-012-rhs-notes-rail-and-no-anchor-completion-redirect.md` (full); task file list (TASK-001 → TASK-012 via `Glob`); ADR file list (ADR-001 → ADR-032 via `Glob`); `design_docs/decisions/ADR-022-persistence-layer-sqlite-and-notes-schema.md` (full); `design_docs/decisions/ADR-027-section-completion-affordance-bottom-of-section.md` (header + §Decision); `design_docs/decisions/ADR-032-notes-save-must-not-relocate-the-reader.md` (full); project_issue file list (via `Glob`); `design_docs/project_issues/notes-save-relocates-reader.md` (full); `design_docs/project_issues/tooling-lint-and-type-check.md` (full); `design_docs/project_issues/parser-fidelity-body-linebreak-and-display-math.md` (full); `design_docs/project_issues/ch06-textbackslash-double-renders-as-double-backslash.md` (full); `design_docs/audit/TASK-012-rhs-notes-rail-and-no-anchor-completion-redirect.md` (full — Runs 001–014); `app/**/*.py` file list (via `Glob`); git log (recent commits, from context).
**Tools / commands used:** `Read` (MANIFEST.md, architecture.md, manifest-conformance/SKILL.md, CLAUDE.md, TASK-012 task file, ADR-022, ADR-027 header, ADR-032, notes-save-relocates-reader, tooling-lint-and-type-check, parser-fidelity-body-linebreak-and-display-math, ch06-textbackslash-double-renders-as-double-backslash, TASK-012 audit); `Glob` (`design_docs/tasks/*.md`, `design_docs/decisions/*.md`, `design_docs/project_issues/*.md`, `app/**/*.py`); `Write` (`design_docs/tasks/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md`, `design_docs/audit/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md`).
**Files created:** `design_docs/tasks/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md`, `design_docs/audit/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md`
**Files modified:** none
**Task alternatives considered:** (1) [chosen] Quiz domain schema + per-Section Quiz read surface (generation deferred) — the first vertical slice of Quiz-bootstrap; (2) Quiz-bootstrap all-at-once (schema + `ai-workflows` + async delivery + Notification + replay-+-fresh) — rejected, ~5 ADRs, multi-session by construction; (3) `ai-workflows` integration spike first — rejected, architecture-on-spec + §4 secondary-objective-can't-drive-scope; (4) Topic vocabulary bootstrap as a standalone task — rejected, no behavioral consumer until generation (but folded into this task's `/design` as a conditional schema decision); (5) continue polishing the Lecture page (parser-fidelity / parked ADR-032) — rejected, Lecture page settled, doesn't advance the primary objective; (6) a bigger Quiz slice incl. a Quiz-taking surface — rejected, too large, presupposes generation; (7) a smaller slice (schema only, no UI) — rejected, horizontal layer not vertical slice; (8) lint/type-check tooling task — rejected, doesn't advance the primary objective.
**Decisions surfaced:** forecasts in the task file — ADR-NNN Quiz domain schema (`quizzes` / `questions` / `quiz_attempts` + join/history tables under `app/persistence/quizzes.py`, in `data/notes.db` per ADR-022's cohabitation rule, ADR-022's migration story); ADR-NNN per-Section Quiz surface placement (where the Quiz block goes in the reading flow — forecast in/near `.section-end` per ADR-027 §Future-surfaces; whether a placeholder Quiz-trigger affordance ships); possibly ADR-NNN Topic vocabulary (conditional — only if `/design` concludes the cheap TEXT-column form for Question Topic tags is genuinely worse than a `topics` table + join). No project_issue created (the Topic question is folded into `/design`, not parked). The eighth-recurrence `tooling-lint-and-type-check` project_issue is surfaced (not acted on).
**Architecture leaks found:** none. (Noted but not leaks: TASK-012 reviewer's nits #2 (stale "Proposed" text in two project_issue resolution notes), #3 (minor `section_number` over-specification in architecture.md's project-structure summary), #5 (the `scroll-margin-top: 50vh` vs ADR-031's "near the bottom" prose) — all stale-text or minor-over-specification, not architectural claims without ADR backing. ADR-032 is `Proposed` — Tier-1 but carries no authority until the human gates it; `notes-save-relocates-reader.md` is a Tier-3 tracking breadcrumb correctly pointing at ADR-032.)
**Pushback raised:** none. No `MANIFEST TENSION` (the manifest is internally consistent with starting the Quiz pillar — §3/§7 motivate it; §5/§6 constrain the schema by construction; §7's "a Quiz feature without the loop is not the project" is not in tension because this task is the *foundation* the loop is built on with the loop's tasks named, not "a Quiz feature without the loop"; §4's `ai-workflows` commitment is deliberately not exercised by this task). No `> PRIMARY OBJECTIVE COMPLETE` (the primary objective — the reinforcement loop — has *not* been substantially achieved; this task is slice 1 of 3 toward it). One architectural concern flagged for the architect's `/design` attention: this task is the closest the project has come to "build the schema before the feature exists" — the task file argues why it is defensibly *not* architecture-on-spec (concrete next consumer; vertical slice; established bootstrap rhythm; makes the heavy AI task focused; ADR-022 pre-positioned exactly this) and instructs the architect to surface a `PUSHBACK:` if `/design` concludes otherwise rather than ship a horizontal layer.
**Output summary:** TASK-013 task file and this audit file created; the next task is the first vertical slice of Quiz-bootstrap — the Quiz domain schema + the per-Section Quiz read surface (empty-state), with `ai-workflows` integration / Quiz generation / async delivery / Notification / the replay-+-fresh composition all explicitly deferred to the next task(s). 2–3 ADRs forecast (Quiz schema; per-Section Quiz surface placement; possibly Topic vocabulary); none requires `ai-workflows`.

### Run 002 — architect / Mode 2 `/design`

**Time:** 2026-05-11T13:30:00Z
**Input files read:** `design_docs/MANIFEST.md` (full); `CLAUDE.md` (full, from context); `.claude/skills/manifest-conformance/SKILL.md` (full); `design_docs/architecture.md` (full); `design_docs/tasks/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md` (full, in chunks); `design_docs/decisions/ADR-022-persistence-layer-sqlite-and-notes-schema.md` (full); `design_docs/decisions/ADR-024-section-completion-schema-and-persistence-module.md` (full); `design_docs/decisions/ADR-027-section-completion-affordance-bottom-of-section.md` (full); `design_docs/decisions/ADR-031-section-completion-redirect-section-end-anchor-scroll-margin.md` (full); `app/persistence/connection.py` (full); `app/persistence/__init__.py` (full); `app/persistence/notes.py` (full); `app/persistence/section_completions.py` (full); `app/main.py` (full); `app/templates/lecture.html.j2` (full); `app/parser.py` (`extract_sections` signature/docstring via Grep); `design_docs/audit/TASK-013-...md` (full); ADR file list + project_issue file list (via Glob); project_issues Quiz-mention scan (via Grep — no dedicated Quiz-schema / Quiz-surface project_issue exists).
**Tools / commands used:** `Read` (MANIFEST.md, architecture.md, manifest-conformance/SKILL.md, TASK-013 task file ×4 chunks, ADR-022, ADR-024, ADR-027, ADR-031, connection.py, __init__.py, notes.py, section_completions.py, main.py, lecture.html.j2, TASK-013 audit); `Glob` (`design_docs/decisions/*.md`, `design_docs/project_issues/*.md`); `Grep` (`def extract_sections` in parser.py; `[Qq]uiz` in project_issues); `Write` (ADR-033, ADR-034); `Edit` (architecture.md — two Proposed-ADR rows; this audit file — header + Run 002).
**Files created:** `design_docs/decisions/ADR-033-quiz-domain-schema.md`, `design_docs/decisions/ADR-034-per-section-quiz-surface-placement.md`
**Files modified:** `design_docs/architecture.md` (two rows added to "Proposed ADRs (awaiting human acceptance)" — ADR-033, ADR-034; no other change — index-only, no architectural content added beyond the rows; project-structure summary not regenerated since the new ADRs are still `Proposed`, per the maintenance protocol's no-op-until-Accepted rule); `design_docs/audit/TASK-013-...md` (header Status → Blocked, Current phase → design; this Run 002 entry appended).
**ADRs proposed:** `ADR-033` — Quiz domain schema (`quizzes` / `questions` / `quiz_questions` / `quiz_attempts` / `attempt_questions` under `app/persistence/quizzes.py` in `data/notes.db`; ADR-022 migration story; two lifecycle enums (`quizzes.status` generation lifecycle, `quiz_attempts.status` attempt/grading lifecycle), each naming a failure state, stored as TEXT, additive-extensible; Topic tags as a `'|'`-delimited `questions.topics TEXT` column now — the `topics` table + `question_topics` join + the Topic-vocabulary-source decision deferred to the generation task's `/design`, no separate ADR, no project_issue; Grade aggregate (score/weak-topics/recommended-sections) and `notifications` table deferred to their consuming tasks, both additive; the placeholder Quiz-trigger writes one `status='requested'` Quiz row, no AI call, nothing fabricated; `quiz_questions` many-to-many join (not a `quiz_id` FK on `questions`) so a Question may appear in multiple Quizzes for its Section over time; `attempt_questions` carries `response` + `is_correct` + `explanation`, the last two NULL until grading, so per-Question wrong-answer-replay history is recordable; no `user_id` on any of the five tables; no choice/recall/describe columns on `questions`; `REFERENCES` clauses declared, `PRAGMA foreign_keys = ON` recommended; n-tables threshold for `_SCHEMA_SQL` flagged for the next persistence-touching task). `ADR-034` — Per-Section Quiz surface placement on the Lecture page (a `.section-quiz` block inside the existing `<div class="section-end">` wrapper at the bottom of each `<section>`, after the completion form — the cognitive-sequence action zone ADR-027 forecast, reached as a decision applying ADR-027/028/030's placement-quality principles; empty-state caption "No quizzes yet for this Section." as the must-ship; a plain-language Quiz-status list for the populated case, scaffolded minimally now because the placeholder trigger creates `requested` rows; no takeable affordance for any status this task; the surface never presents a `requested`/`generating` Quiz as finished — MC-5 applied to the read surface; a real, user-triggered `POST /lecture/{chapter_id}/sections/{section_number}/quiz` route that calls `app.persistence.request_quiz(section_id)` (validates the Section ID at the route handler per ADR-024's split; no AI call; no background job; nothing fabricated) and PRG-redirects 303 to `/lecture/{chapter_id}#section-{section_number}-end`, reusing ADR-031's `#section-{n-m}-end` anchor + `scroll-margin-top` no-relocate recipe unchanged; `render_chapter` gains one bulk `list_quizzes_for_chapter(chapter_id)` query → `section_quizzes_by_id`, mirroring `complete_section_ids` / `rail_notes_context`; `section-quiz-*` CSS classes in `lecture.css` per ADR-008, no new file, no `base.css` change; no JavaScript). [×2 ADRs]
**Project issues opened/resolved:** none opened (the Topic-vocabulary question is *not* a homeless open question — ADR-033 §Topic tags decides it: ship the cheap delimited-TEXT column now, defer the `topics` table + `question_topics` join + the Topic-vocabulary-source decision to the generation task's `/design`, which is a named home — so no project_issue is filed; per the task's instruction "ship it now only if the cheap delimited-TEXT-column form is genuinely worse … otherwise defer it … and say so explicitly in the schema ADR" — the architect concluded the cheap form is *not* genuinely worse because the migration to the relational form is additive/clean, so deferred). none resolved (no project_issue was filed against the Quiz schema or the per-Section Quiz surface placement; ADR-027's §Future-surfaces *forecast* the Quiz affordance in/near `.section-end` but that is a forecast in ADR-027, not a project_issue — ADR-034 reaches that conclusion afresh as a decision).
**architecture.md changes:** two rows added to "Proposed ADRs (awaiting human acceptance)" — ADR-033, ADR-034. No content added beyond the index rows. Project-structure summary not regenerated (the new ADRs are `Proposed`, not `Accepted`; the maintenance protocol's no-op-until-Accepted rule applies — the summary regenerates after the human gate moves the rows to "Accepted ADRs"). No other section of architecture.md touched.
**Write-path check:** clean. Files created/modified are confined to `design_docs/{decisions,audit}/**` and `design_docs/architecture.md` — all within Mode 2's allowed write paths (`design_docs/{tasks,audit,decisions,project_issues}/**` plus the architecture.md index it maintains mechanically). No file outside that set was created or modified — no `app/`, no `tests/`, no `CLAUDE.md`, no `MANIFEST.md`, no skill file, no scratch file.
**Architecture leaks found:** none. Every `.md` read in this `/design` cycle classified correctly per the tier table: MANIFEST.md (Tier 1, product behavior only — §5/§6/§7's Quiz constraints read as product behavior the schema honors by construction, not architecture-in-disguise; §4's `ai-workflows` is the one explicit manifest-level architectural commitment, acknowledged and not over-reached); ADR-022/024/027/031 (Tier 1, Accepted — consumed unchanged, no leak); architecture.md (Tier 1, index only — the two new rows added are index rows, no architectural claim introduced; the project-structure summary is unchanged and remains derived from the Accepted-ADR set); CLAUDE.md (Tier 2, process/conventions — carries its ADR-016 quick-reference correctly with the citation; no leak); the conformance skill (Tier 2 — correctly reflects MC-1's architecture portion as still dormant (no AI-engine ADR), MC-7/MC-10's architecture portions as active per ADR-022; no leak); TASK-013 task file (Tier 3 — requests work, no architecture introduced; its forecasts ("forecasts a `quiz_questions` join table", "in/near `.section-end` per ADR-027 §Future-surfaces") are explicitly framed as forecasts for `/design` to decide afresh, which this cycle did). No `.md` file in the set introduces an architectural claim without an Accepted-ADR backing.
**Pushback raised:** none. No `MANIFEST TENSION` — the manifest is internally consistent with this task's decisions (§5/§6/§7's Quiz constraints are satisfied by the schema's construction; §4's `ai-workflows` commitment is deliberately not exercised — the AI-engine ADR is the next task's job). No `ARCHITECTURE LEAK`. No `> NEEDS HUMAN` — the genuine fork (the placeholder Quiz-trigger affordance: real `requested`-row route vs disabled-or-caption vs nothing) was decided by the architect (Option 1 — a real user-triggered route that records a `requested` row; rationale in ADR-034's "My recommendation vs the user's apparent preference" and ADR-033 §The `requested` status) per the task's explicit grant of authority ("the task's framing gives the architect the authority to pick"); the risk is bounded (a `requested` row with no processor *if the next task slips* is harmless and recoverable in a single-user dev database; the next task is scheduled), so escalating a decision the task asked the architect to make would be the wrong move. No `PUSHBACK` on the architecture-on-spec question — the architect's read after designing the schema + surface is that this slice is defensibly *not* the architecture-on-spec anti-pattern (concrete next consumer scheduled; vertical slice — schema + write route + read route + template + tests; established bootstrap rhythm; ADR-022 pre-positioned exactly this; the `attempt_questions` / `quiz_questions` / lifecycle-enum shapes are the expensive-to-get-wrong decisions and getting them now makes the heavy AI task focused rather than larger), as the task file argued; the architect deliberately did *not* try to also ship Quiz generation "to make it less thin" and did *not* refuse to ship the surface "because it's just an empty-state" — the empty-state surface plus the real `requested`-row write path *is* the vertical slice. One disagreement-with-the-task-forecast surfaced inside the ADRs (not a pushback, a normal `/design` refinement): the task forecast "a `status` enum"; the architect split it into two lifecycle enums (`quizzes.status` generation; `quiz_attempts.status` attempt/grading) because the two lifecycles belong to different entities — argued in ADR-033 §Lifecycle enums and §Alternative C and flagged in ADR-033's "My recommendation vs the user's apparent preference" for the human to weigh at the gate.
**Implementation blocked pending human acceptance:** yes — ADR-033 (`Proposed`) and ADR-034 (`Proposed`) await the human's `Accepted` gate before `/implement TASK-013` proceeds. Neither requires `> NEEDS HUMAN` (no decision was escalated to the human); they are routine `Proposed` ADRs awaiting the normal acceptance gate. Once Accepted, the architecture.md rows move from "Proposed ADRs" to "Accepted ADRs" and the project-structure summary regenerates from the new Accepted-ADR set (adding a "Quiz domain" paragraph and a "per-Section Quiz surface" note) per the maintenance protocol.
**Output summary:** Two Proposed ADRs recorded for TASK-013 — ADR-033 (Quiz domain schema: five tables under `app/persistence/quizzes.py` in `data/notes.db`, ADR-022 migration story, two failure-state-naming lifecycle enums, delimited-TEXT Topic tags with the `topics` table deferred to the generation task, Grade aggregate + `notifications` table deferred (additive), the placeholder trigger writes a `requested` row, no `user_id`, no choice/recall/describe columns, no `ai-workflows` decision) and ADR-034 (per-Section Quiz surface inside `.section-end` at bottom-of-Section, empty-state + plain-language status list, a real user-triggered `requested`-row route reusing ADR-031's no-relocate recipe, `section-quiz-*` CSS in `lecture.css`, no JS, no `ai-workflows` call). No ADR superseded; no project_issue opened or resolved. architecture.md updated with two Proposed-ADR rows. Implementation blocked pending human acceptance of ADR-033 and ADR-034.

### Run 003 — test-writer

**Time:** 2026-05-11T14:30:00Z
**Input files read:** `design_docs/tasks/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md` (chunks via offset/limit); `design_docs/decisions/ADR-033-quiz-domain-schema.md` (full, two chunks); `design_docs/decisions/ADR-034-per-section-quiz-surface-placement.md` (full); `CLAUDE.md` (from context); `tests/conftest.py` (full); `tests/playwright/conftest.py` (full); `tests/test_task009_notes_bootstrap.py` (full — style reference); `tests/test_task012_rhs_notes_rail_and_redirect.py` (header, 80 lines — style reference); `tests/playwright/test_task012_rhs_notes_rail_dom.py` (header + 100 lines — style reference); `tests/playwright/test_task010_section_completion_dom.py` (header + 80 lines — style reference); `app/persistence/__init__.py` (full — public API surface); `app/persistence/connection.py` (full — schema bootstrap mechanic); `design_docs/audit/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md` (full — existing runs context).
**Tools / commands used:** Read (×13 files); Bash (`ls tests/`, `ls tests/playwright/`, `grep -n task pyproject.toml`, `python3 -m pytest` ×3 runs); Write (×3 new test files); Bash (cat >> audit file).
**Files created:** `tests/test_task013_quiz_schema.py`, `tests/test_task013_quiz_surface_http.py`, `tests/playwright/test_task013_quiz_surface_dom.py`
**Files modified:** `design_docs/audit/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md` (this run entry appended; header Current phase updated to `test`)
**Tests added:**
- `test_quiz_schema_tables_exist_after_bootstrap` → AC-1 (five tables exist)
- `test_quiz_schema_bootstrap_idempotent` → AC-1 (idempotent re-bootstrap)
- `test_quizzes_table_has_section_id_column` → AC-2 (section_id column)
- `test_quizzes_section_id_not_null_constraint` → AC-2 (NOT NULL enforced)
- `test_no_user_id_on_any_quiz_domain_table` → AC-3 (MC-7, no user_id)
- `test_questions_table_has_prompt_column` → AC-4 (coding-task prompt column)
- `test_questions_has_topics_column` → AC-4 (topics column)
- `test_questions_table_has_no_choice_recall_describe_columns` → AC-4 (no non-coding cols)
- `test_question_in_two_quizzes_same_section` → AC-5 (many-to-many join)
- `test_attempt_questions_columns_include_response_is_correct_explanation` → AC-5 (attempt_questions structure)
- `test_attempt_questions_is_correct_and_explanation_nullable` → AC-5 (nullable until grading)
- `test_quizzes_status_enum_boundary_requested` → AC-5 (quizzes.status 'requested')
- `test_quizzes_status_enum_boundary_generation_failed` → AC-5 (failure state, MC-5)
- `test_quiz_attempts_status_enum_boundary_grading_failed` → AC-5 (attempt failure state, MC-5)
- `test_mc10_no_sqlite3_import_outside_persistence_package` → AC-6 (MC-10 boundary)
- `test_mc10_no_sql_literals_outside_persistence_package` → AC-6 (MC-10 boundary)
- `test_request_quiz_creates_requested_status_row` → AC-9 (request_quiz round-trip)
- `test_list_quizzes_for_chapter_returns_dict_with_section_id_key` → AC-9 (list_quizzes_for_chapter)
- `test_list_quizzes_for_chapter_empty_for_unknown_chapter` → AC-9 (empty result)
- `test_request_quiz_multiple_calls_create_multiple_rows` → AC-9 (multiple rows)
- `test_list_quizzes_for_chapter_many_rows_within_budget` → AC-9 (performance, 50 rows)
- `test_quiz_surface_on_all_12_chapters` ×12 parametrized → AC-7 (all chapters, batch)
- `test_quiz_surface_on_mandatory_chapter` → AC-8 (Mandatory chapter)
- `test_quiz_surface_on_optional_chapter` → AC-8 (Optional chapter)
- `test_quiz_surface_empty_state_text` → AC-7 (exact empty-state copy)
- `test_section_quiz_css_classes_present` → AC-7 (section-quiz-* CSS namespace)
- `test_quiz_button_form_present_in_section_end` → AC-9 (form/button present)
- `test_post_quiz_route_returns_303` → AC-9 (303 PRG redirect)
- `test_post_quiz_route_location_ends_with_section_end_anchor` → AC-9 + ADR-031 (Location anchor)
- `test_post_quiz_creates_exactly_one_requested_row` → AC-9 (one row, status='requested')
- `test_post_quiz_on_every_mandatory_chapter` → AC-9 boundary (Mandatory Chapters)
- `test_post_quiz_unknown_chapter_returns_4xx` → AC-9 (section_id validation, unknown chapter)
- `test_post_quiz_out_of_range_section_number_returns_4xx` → AC-9 (section_id validation, bad section)
- `test_post_quiz_wrong_method_get_returns_405_or_404` → AC-9 (POST-only route)
- `test_quiz_surface_shows_requested_status_after_post` → AC-9 (populated-case render, MC-5)
- `test_requested_quiz_never_presented_as_ready` → AC-9 + MC-5 (never fabricated)
- `test_no_javascript_in_quiz_surface` → ADR-034 (no-JS posture)
- `test_no_regression_lecture_page_still_returns_200` ×12 → AC-10 (no regressions)
- `test_no_regression_existing_notes_form_still_present` ×12 → AC-10 (Notes surface unchanged)
- `test_no_regression_section_completion_form_still_present` ×12 → AC-10 (completion form unchanged)
- `test_lecture_page_with_many_quizzes_within_budget` → AC-7 performance (30 rows, <5s)
- `test_each_section_end_contains_section_quiz_block` → AC-11(a) (DOM placement)
- `test_section_quiz_empty_state_text_in_dom` → AC-11(a) (empty-state text in DOM)
- `test_quiz_surface_renders_on_mandatory_chapter_dom` → AC-11(b) (Mandatory M/O)
- `test_quiz_surface_renders_on_optional_chapter_dom` → AC-11(b) (Optional M/O)
- `test_quiz_surface_present_on_both_mandatory_and_optional` → AC-11(b) (combined M/O)
- `test_generate_quiz_form_is_present_in_dom` → AC-11(c) (real form/button)
- `test_generate_quiz_button_label_readable` → AC-11(c) (button label)
- `test_section_end_wrapper_still_contains_completion_form` → AC-10 / ADR-034 §What is NOT changed
- `test_quiz_block_comes_after_completion_form_in_dom` → ADR-034 §Placement (order)
- `test_generate_quiz_click_url_contains_section_end_anchor` → ADR-034 + ADR-031 (Playwright PRG)
- `test_section_quiz_block_count_matches_section_count` → ADR-034 §Placement (one per section)
**Coverage matrix:**
- Boundary: `test_quiz_schema_tables_exist_after_bootstrap` (all 5 tables), `test_quizzes_status_enum_boundary_requested` / `_generation_failed` / `quiz_attempts_status_enum_boundary_grading_failed` (lifecycle enum values), `test_quiz_surface_on_all_12_chapters` (all 12 chapters), `test_post_quiz_on_every_mandatory_chapter` (Mandatory chapters), `test_no_regression_*` ×36 parametrized (all 12 chapters × 3 existing surfaces).
- Edge: `test_questions_table_has_no_choice_recall_describe_columns` (no non-coding columns), `test_no_user_id_on_any_quiz_domain_table` (all 5 tables checked), `test_attempt_questions_is_correct_and_explanation_nullable` (nullable grading columns), `test_list_quizzes_for_chapter_empty_for_unknown_chapter` (zero-result edge), `test_quiz_surface_present_on_both_mandatory_and_optional` (M/O inheritance), `test_no_javascript_in_quiz_surface` (no <script> tags), `test_quiz_block_comes_after_completion_form_in_dom` (DOM order).
- Negative: `test_post_quiz_unknown_chapter_returns_4xx` (unknown chapter, no row created), `test_post_quiz_out_of_range_section_number_returns_4xx` (bad section, no row created), `test_post_quiz_wrong_method_get_returns_405_or_404` (wrong HTTP method), `test_requested_quiz_never_presented_as_ready` (MC-5: never fabricated), `test_mc10_no_sqlite3_import_outside_persistence_package` (MC-10 grep), `test_mc10_no_sql_literals_outside_persistence_package` (MC-10 grep), `test_quizzes_section_id_not_null_constraint` (DDL NOT NULL enforced).
- Performance: `test_list_quizzes_for_chapter_many_rows_within_budget` (50 Quiz rows, <5s), `test_lecture_page_with_many_quizzes_within_budget` (30 Quiz rows, <5s Lecture render), catching O(n²) regressions in bulk query and template loop.
**Pytest red result:** Collected: 96, Failing: 52, Passing: 44
**Assumptions:**
- ASSUMPTION: `request_quiz` and `list_quizzes_for_chapter` are exposed from `app.persistence` (added to `__init__.py`) per ADR-033 §Module path §Public API.
- ASSUMPTION: The "Generate a Quiz for this Section" button form has action ending in '/quiz' (e.g. `action="/lecture/{chapter_id}/sections/{n}/quiz"`) per ADR-034 template.
- ASSUMPTION: ch-01-cpp-refresher's first section number is "1-1" and ch-07-heaps-and-treaps' first is "7-1" per ADR-002 section numbering.
- ASSUMPTION: ch-01-cpp-refresher is a Mandatory Chapter; ch-07-heaps-and-treaps is an Optional Chapter per ADR-004's canonical mapping.
- ASSUMPTION: The .section-quiz CSS class is used on the block wrapper inside .section-end per ADR-034 §CSS namespace.
- ASSUMPTION: The 44 passing tests include: MC-10 greps (no sqlite3 outside persistence — trivially pass since no Quiz code exists yet), `test_questions_table_has_no_choice_recall_describe_columns` and `test_no_user_id_on_any_quiz_domain_table` (pass vacuously — tables don't exist, so no forbidden columns are present), `test_post_quiz_wrong_method_get_returns_405_or_404` (passes because the route doesn't exist → 404/405), `test_requested_quiz_never_presented_as_ready` (passes because no quiz HTML exists → `section-quiz-item--ready` is absent), and all 36 `test_no_regression_*` tests (existing lecture surfaces are intact).
**CANNOT TEST:** none. All ACs under `## Acceptance criteria` are programmatic; AC-12 (ADR records, architecture.md updates) was not listed as a programmatic AC in the task.
**Architecture leaks found:** none. All `.md` files read classified correctly per the tier table. ADR-033 and ADR-034 are Accepted ADRs (Tier 1). `architecture.md` is index-only. No `.md` file introduces an architectural claim without an Accepted-ADR backing.
**Pushback raised:** none. All ACs are testable as written. No AC is a human-only verification gate misclassified under `## Acceptance criteria` (the rendered-surface visual review is correctly under `## Verification gates (human-only; not programmatic ACs)`). No weak ACs requiring strengthening were found — the ACs are specific enough to translate to meaningful failing tests.

### Run 004 — implementer

**Time:** 2026-05-11T15:00:00Z
**Input files read:** `design_docs/tasks/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md` (chunks); `design_docs/decisions/ADR-033-quiz-domain-schema.md` (full); `design_docs/decisions/ADR-034-per-section-quiz-surface-placement.md` (full); `design_docs/decisions/ADR-022-persistence-layer-sqlite-and-notes-schema.md` (skipped — consumed via patterns in existing code); `design_docs/decisions/ADR-024-section-completion-schema-and-persistence-module.md` (skipped — consumed via patterns in existing code); `CLAUDE.md` (from system context); `.claude/skills/manifest-conformance/SKILL.md` (from system context); `tests/test_task013_quiz_schema.py` (full); `tests/test_task013_quiz_surface_http.py` (full); `tests/playwright/test_task013_quiz_surface_dom.py` (full); `app/persistence/connection.py` (full); `app/persistence/__init__.py` (full); `app/persistence/section_completions.py` (full); `app/main.py` (full); `app/templates/lecture.html.j2` (full); `app/static/lecture.css` (full); `app/templates/base.html.j2` (full — checked for existing `<script>` tags); `tests/playwright/conftest.py` (full); `design_docs/audit/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md` (full — existing runs context).
**Tools / commands used:** Read (×15 files); Write (`app/persistence/quizzes.py`); Edit (`app/persistence/connection.py` ×2, `app/persistence/__init__.py`, `app/main.py` ×3, `app/templates/lecture.html.j2`, `app/static/lecture.css` ×2); Bash (`python3 -m pytest` ×4 runs, `grep` ×4, `curl` ×3, dev server start/stop, `git stash` / `git stash pop` — to verify pre-implementation test state); Edit (this audit file header + Run 004 entry).
**Files created:** `app/persistence/quizzes.py`
**Files modified:** `app/persistence/connection.py` (appended Quiz-domain DDL to `_SCHEMA_SQL`; added `PRAGMA foreign_keys = ON` to `get_connection()`); `app/persistence/__init__.py` (added Quiz public API re-exports: `Quiz`, `Question`, `request_quiz`, `list_quizzes_for_section`, `list_quizzes_for_chapter`); `app/main.py` (added `request_quiz` + `list_quizzes_for_chapter` imports; added `section_quizzes_by_id = list_quizzes_for_chapter(chapter_id)` to `render_chapter`; passed `section_quizzes_by_id` to template; added `POST /lecture/{chapter_id}/sections/{section_number}/quiz` route); `app/templates/lecture.html.j2` (added `.section-quiz` block inside `.section-end` after completion form); `app/static/lecture.css` (changed `.section-end` to `flex-direction: column; align-items: flex-end`; added all `section-quiz-*` CSS classes).
**Files explicitly NOT modified:** `tests/**/test_*.py`, `design_docs/MANIFEST.md`, `design_docs/architecture.md`, `design_docs/decisions/**`, `CLAUDE.md`, `.claude/skills/manifest-conformance/SKILL.md`, `app/templates/base.html.j2`.
**Implementation decisions made:**
- `list_quizzes_for_chapter` uses `LIKE 'chapter_id#%'` prefix matching to stay as a single SQL query (rather than taking the Section-id list from the caller), keeping SQL in the persistence package (MC-10).
- `.section-end` CSS changed from `display: flex; justify-content: flex-end` to `flex-direction: column; align-items: flex-end` to stack completion form above quiz block vertically while preserving right-alignment.
- `PRAGMA foreign_keys = ON` added to `get_connection()` per ADR-033 §Schema bootstrap recommendation; it is a no-op for `notes`/`section_completions` (no `REFERENCES` clauses there).
- Topic tags stored as `'|'`-delimited TEXT per ADR-033 §Topic tags decision; exposed as `list[str]` to callers.
**Tests run:** `python3 -m pytest tests/` — 937 collected; **936 passed, 1 failed** (6m08s). All TASK-013 schema tests pass (21/21). All TASK-013 HTTP surface tests pass (64/65 — see PUSHBACK below). All Playwright tests pass (195/195, 4m10s).
**Lint / type-check:** No lint/type-check command configured (project_issue `tooling-lint-and-type-check.md` is open, eighth recurrence — not a TASK-013 deliverable). MC-10 boundary checks pass (grep: zero `import sqlite3` and zero SQL literals outside `app/persistence/`).
**Conformance result:** 0 blockers, 0 warnings, 1 dormant (MC-1 architecture portion — no AI-engine ADR yet; no AI call introduced; manifest portion PASS). MC-2, MC-3, MC-5, MC-6, MC-7, MC-8, MC-9, MC-10: all PASS.
**End-to-end verification:** Dev server started on port 8765; `curl GET /lecture/ch-01-cpp-refresher` returned 200, rendered HTML contained 77 `section-quiz` class occurrences (77 = ~15 sections × ~5 class uses), 14 "No quizzes yet" empty-state captions, 15 "Generate a Quiz" button texts. `curl POST /lecture/ch-01-cpp-refresher/sections/1-1/quiz` returned 303. `curl POST /lecture/ch-99-fake/sections/1-1/quiz` returned 404. `curl POST /lecture/ch-01-cpp-refresher/sections/999-999/quiz` returned 404. All 195 Playwright DOM tests pass (including `test_section_quiz_block_count_matches_section_count`, `test_quiz_block_comes_after_completion_form_in_dom`, `test_generate_quiz_click_url_contains_section_end_anchor`). Remaining defects: 1 (see PUSHBACK below).
**Adjacent bugs surfaced (not fixed):**
- The `.section-end` `flex-direction: column` change may affect the visual appearance of the completion button (previously right-aligned via `justify-content: flex-end`; now the column still has `align-items: flex-end` which preserves right-alignment). No test failures observed. Human review should confirm the completion button still appears visually correct.
**Pushback raised:**
- PUSHBACK: `test_no_javascript_in_quiz_surface` has a false premise and cannot be made to pass without breaking existing Playwright math-rendering tests. The test asserts `"<script" not in html` (zero `<script>` tags on the full rendered page), but `base.html.j2` has included MathJax `<script>` tags since prior tasks, and `test_ch04_texttt_span_contains_mjx_container_after_mathjax` (Playwright) explicitly depends on MathJax running. The test-writer's comment "Since no prior surface uses JavaScript, the rendered Lecture page must have zero `<script>` tags" is factually wrong. The test was ALREADY failing pre-implementation (confirmed via `git stash` + run). The test's INTENT aligns with ADR-034 ("the Quiz surface must not introduce JavaScript") but the implementation incorrectly checks the whole page rather than the quiz-surface-specific elements. Recommended fix: re-invoke the test-writer to revise this one test to check that no `<script>` tag references the quiz route or quiz-specific scripts, rather than asserting zero `<script>` tags globally. Document to revise: `tests/test_task013_quiz_surface_http.py` (test-writer; re-invocation required per ADR-021). This test was in the pre-implementation failing set; my implementation does not introduce or fix it.

### Run 005 — /auto stopped

**Time:** 2026-05-11T15:30:00Z
**Trigger:** `/auto` stop condition rule 6 — `PUSHBACK:` raised by the implementer (Run 004). The `/auto` loop does not get to interpret whether a pushback target is "auto-fixable"; it escalates.
**Surfaced text (implementer, Run 004):** `PUSHBACK: test_no_javascript_in_quiz_surface has a false premise and cannot be made to pass without breaking existing Playwright math-rendering tests. The test asserts "<script" not in html (zero <script> tags on the full rendered page), but base.html.j2 has included MathJax <script> tags since prior tasks, and test_ch04_texttt_span_contains_mjx_container_after_mathjax (Playwright) explicitly depends on MathJax running. ... The test's INTENT aligns with ADR-034 ("the Quiz surface must not introduce JavaScript") but the implementation incorrectly checks the whole page rather than the quiz-surface-specific elements. Recommended fix: re-invoke the test-writer (per ADR-021) to revise this one test to check that no <script> tag references the quiz route or quiz-specific scripts, rather than asserting zero <script> tags globally.`
**State at stop:** Phases 1–4 complete. Task file + audit created (Run 001); ADR-033 + ADR-034 written and auto-accepted (Run 002 + gates table); `architecture.md` rows moved to Accepted and project-structure summary regenerated; test-writer created 3 test files (Run 003); implementer created `app/persistence/quizzes.py` and modified `connection.py` / `__init__.py` / `main.py` / `lecture.html.j2` / `lecture.css` (Run 004). `python3 -m pytest tests/` = 936 passed, 1 failed (`test_no_javascript_in_quiz_surface` — the PUSHBACK target). Conformance walk in Run 004: 0 blockers. End-to-end smoke (Run 004): per-Section Quiz surface renders on ch-01; `POST .../quiz` → 303; unknown chapter/section → 404. Nothing has been staged or committed. Verify gate (Phase 5) not reached; review/commit (Phase 6) not reached.
**What the human needs to resolve:** Decide the disposition of the implementer's PUSHBACK. Two readings: (a) the test-writer flattened the project's "no JavaScript" posture — the project ships MathJax (`base.html.j2`), so "no JS" has always meant "no behavioral JS we author," not "zero `<script>` tags"; the fix is to re-invoke the test-writer with a delta brief to scope `test_no_javascript_in_quiz_surface` to the quiz surface (assert the quiz block introduces no new `<script>` and no quiz-route-referencing script), per ADR-021; or (b) the ambiguity is worth tightening in ADR-034 or the conformance skill before proceeding. After resolving, either re-run `/auto` (it will start a *new* task — not resume TASK-013) or finish TASK-013 manually: re-invoke the test-writer for the one test, re-run `python3 -m pytest tests/`, run the conformance walk + end-to-end verification, file the `pending human` rendered-surface verification row (per the task's `## Verification gates` section), then stage + reviewer + commit.

### Run 005 note — orchestrator adjacent findings carried from implementer Run 004 (not blocking, for human awareness)

- The `.section-end` CSS changed from `display: flex; justify-content: flex-end` to `display: flex; flex-direction: column; align-items: flex-end` to stack the new `.section-quiz` block below the completion form. Right-alignment of the completion button is preserved (`align-items: flex-end`), but the visual grouping at the bottom of each `<section>` changed; no test failures observed; the rendered-surface human review (the task's Verification gate) should confirm it reads correctly.
- `list_quizzes_for_chapter` uses `LIKE 'chapter_id#%'` prefix matching to stay a single SQL query inside the persistence package (MC-10) rather than taking the Section-id list from the caller.
- `PRAGMA foreign_keys = ON` was added to `get_connection()` per ADR-033; no-op for the pre-existing `notes` / `section_completions` tables (no `REFERENCES` clauses there).

### Run 006 — architect / human-directed architecture-hygiene cleanup (no-JS framing over-reach)

**Time:** 2026-05-11T16:30:00Z
**Trigger / authority:** Human-directed, arising directly from the implementer PUSHBACK in Run 004 / the `/auto` stop in Run 005 (the `test_no_javascript_in_quiz_surface` test exposed that "no JavaScript" was being treated as a binding posture it never was). Human instruction, verbatim: *"adr cleanup don't leave that till later and soften the language adr should be establishing architecture used not restricting it"* and earlier *"I never said I don't want javascript ... I never had demanded that this be a javascriptless project that would have been architecture smell which I have been intentionally designing around."* This is an authorized human-directed correction of over-stated decision *scope* (not a decision change), so in-place edits to Accepted ADRs are appropriate here, per the human's explicit direction.
**Input files read:** `design_docs/MANIFEST.md` (full — confirmed no no-JS / synchronous-form-posting-only / client-side-technology restriction anywhere); `CLAUDE.md` (full, from context — confirmed no no-JS rule); `.claude/skills/manifest-conformance/SKILL.md` (from context — confirmed no MC-N enforces no-JS); `design_docs/architecture.md` (full); `design_docs/decisions/` ADR list (via Glob, ADR-001 → ADR-034); `design_docs/decisions/ADR-006-navigation-surface-shape.md` (§Alternatives F); `ADR-023-notes-creation-and-read-surface.md` (§Form-handling-pattern + no-JS mentions); `ADR-025-section-completion-ui-surface.md` (no-JS mentions); `ADR-027-section-completion-affordance-bottom-of-section.md` (no-JS mentions); `ADR-028-notes-rail-resident-panel.md` (no-JS mentions); `ADR-029-notes-panel-rhs-rail-three-column-layout.md` (no-JS mentions); `ADR-030-section-completion-redirect-no-fragment.md` (no-JS framing); `ADR-031-section-completion-redirect-section-end-anchor-scroll-margin.md` (no-JS framing); `ADR-032-notes-save-must-not-relocate-the-reader.md` (header / "Read this first" block); `ADR-034-per-section-quiz-surface-placement.md` (line 89 + §"I am NOT pushing back on" + §Alternatives I); `ADR-010-playwright-ui-verification.md` (Cypress/Node mentions — reviewed, classified descriptive: about the *test toolchain* (Node vs Python), not client-side JS — left as-is); `design_docs/audit/TASK-013-...md` (full).
**Tools / commands used:** `Read` (MANIFEST.md, architecture.md, ADR-006/010/023/025/027/028/029/030/031/032/034 (relevant sections), TASK-013 audit); `Glob` (`design_docs/decisions/*.md`); `Grep` (no-JS / JavaScript / posture / synchronous-form-post patterns across `design_docs/decisions/` and `design_docs/architecture.md`); `Write` (`design_docs/decisions/ADR-035-adrs-describe-architecture-used-not-the-available-toolkit.md`); `Edit` (ADR-006 ×1, ADR-023 ×1, ADR-025 ×6, ADR-027 ×4, ADR-028 ×7, ADR-029 ×5, ADR-030 ×5, ADR-031 ×8, ADR-032 ×1, ADR-034 ×3; `design_docs/architecture.md` ×4; this audit file ×1).
**Files created:** `design_docs/decisions/ADR-035-adrs-describe-architecture-used-not-the-available-toolkit.md` (Status `Proposed`).
**Files modified:** `design_docs/decisions/ADR-006-navigation-surface-shape.md`; `ADR-023-notes-creation-and-read-surface.md`; `ADR-025-section-completion-ui-surface.md`; `ADR-027-section-completion-affordance-bottom-of-section.md`; `ADR-028-notes-rail-resident-panel.md`; `ADR-029-notes-panel-rhs-rail-three-column-layout.md`; `ADR-030-section-completion-redirect-no-fragment.md`; `ADR-031-section-completion-redirect-section-end-anchor-scroll-margin.md`; `ADR-032-notes-save-must-not-relocate-the-reader.md`; `ADR-034-per-section-quiz-surface-placement.md`; `design_docs/architecture.md`; this audit file.
**ADRs reframed (decisions unchanged; restrictive over-reach → descriptive, each with an inline note pointing to ADR-035):**
- **ADR-034 line 89** (the priority): "**No JavaScript.** ... the project's established posture (ADR-023/025/027/028/029/030/031)" → "**This surface needs no JavaScript.** It is template + CSS + a synchronous form post + a PRG redirect — the same shape ... because a no-JS recipe was clean and sufficient there, not because 'no JavaScript' is a project rule." Also ADR-034 §"I am NOT pushing back on" no-JS-posture line ("The no-JS posture (ADR-023/...) — preserved" → "The no-JS form-handling shape ... — followed here too, because ... clean and sufficient ... Not a constraint future surfaces must honor") and §Alternatives I (confirmation dialog: "requires JavaScript (the project's no-JS posture)" → "would need ... JavaScript (which this surface otherwise has no need for — not a hard 'no')").
- **ADR-006** §Alternatives F: SPA rejection now says it rejects a SPA *architecture* on cost-vs-benefit, "not 'JavaScript' — JavaScript is part of the available toolkit; see ADR-035".
- **ADR-023** §Form-handling-pattern header + opening sentence: "; no JavaScript" → "; no JavaScript needed for this surface" and an explicit "it does **not** declare a project-wide 'no JavaScript' rule (there is none — see ADR-035)".
- **ADR-025** ×6: §Form-handling header ("no JavaScript" → "no JavaScript needed for this surface"); §"Why synchronous PRG" ("ADR-023 already commits to the no-JavaScript posture" → "ADR-023's Notes surface uses synchronous PRG with no client-side code; mirroring that here ... 'mirror what worked next door,' not 'JavaScript is forbidden'"); fragment-anchor line ("on every browser the project's no-JS posture (ADR-023) supports" → "on every browser, and on the no-JS form-handling shape this surface and the Notes surface share"); §Alternatives E ("the project has no JS today" → "this surface has no need for it ... Rejected on cost-vs-benefit ... not because client-side code is off the table"); §"I am NOT pushing back on" ("synchronous PRG, no-JS, server-side validation" → "... no client-side code needed ... Mirroring what worked for Notes, not honoring a 'no JS' rule"); §"Becomes impossible" ("A completion toggle that requires JavaScript" → "A completion toggle that *requires* JavaScript to function at all ... a later ADR could add progressive-enhancement JS on top ... Not a ban on client-side code").
- **ADR-027** ×4: §decision-space (Option 4 floating affordance: "rejected because it requires JS, contradicting ADR-025's no-JS commitment" → "rejected because it would require JavaScript and asset-build machinery disproportionate to a placement refinement — not because client-side code is forbidden"); §"What of ADR-025 is retained" ("Synchronous PRG with no JavaScript" → "Synchronous PRG; no JavaScript is needed for this surface"); §Alternatives C ("Contradicts ADR-025's no-JS commitment (and ADR-023's project-wide no-JS posture). Adding JS here would force a project-wide ADR" → "disproportionate to a placement refinement ... Rejected on cost-vs-benefit, not because client-side code is off the table"); §"I am NOT pushing back on" ("The no-JS commitment ... — preserved" → "The no-JS form-handling shape ... — followed here too ... Not a project invariant").
- **ADR-028** ×7: §textarea-sizing header + commitment line ("no JavaScript" → "no JavaScript needed"; "The no-JS commitment from ADR-023/025/027 is preserved" → "`field-sizing: content` + `resize: vertical` is a clean CSS-only solution here, so none is needed ... not a project rule"); §"What of ADR-023 is retained" ("Synchronous PRG with no JavaScript" → "Synchronous PRG; no JavaScript needed for this surface"); §Alternatives B (corner-pinned panel: "Contradicts ADR-023's no-JS commitment" removed; reframed as cost-vs-benefit); §Alternatives G (JS auto-resize: "Contradicts the project's no-JS posture ... would force a project-wide ADR on JS infrastructure" → "the CSS-only solution is clean and sufficient ... not because client-side code is off the table"); §"What this ADR retains" list ("no-JS commitment" → "the no-JS form-handling shape"); §"I am NOT pushing back on" ("The no-JS commitment ... — preserved" → "The no-JS form-handling shape ... — followed here too ... Not a project invariant"); §"Becomes impossible" ("A Notes UI that requires JavaScript" → "A Notes UI that *requires* JavaScript to function ... a later ADR could layer progressive-enhancement JS on top ... Not a ban"); §supersedure-path ("a future ADR introduces JS infrastructure as a project-wide commitment" → "a future ADR adds the client-side JavaScript that workflow needs ... See ADR-032 ... and ADR-035").
- **ADR-029** ×5: §textarea-sizing header + commitment ("no JavaScript is introduced" reframed to "the CSS-only solution is clean and sufficient ... not because 'no JavaScript' is a project rule"); §"What of ADR-028 is retained" ("Synchronous PRG with no JavaScript" → "Synchronous PRG; no JavaScript needed for this surface"); §"My recommendation vs the user's apparent preference" textarea bullet ("the no-JS posture is preserved by this ADR entirely" → "because the CSS-only solution is clean and sufficient — not because JS is forbidden"); §"I am NOT pushing back on" ("The no-JS commitment ... — preserved" → "The no-JS form-handling shape ... — followed here too ... Not a project invariant"); §"Becomes impossible" + §supersedure-path reframed (cites ADR-032 / ADR-035).
- **ADR-030** ×5 (light touch — `Superseded by ADR-031`): §"Why Option 1 ... not Option 2" ("Option 1 ... the no-JS posture intact" + "Option 2 would be the **first JavaScript in the project** — a permanent architectural commitment ... deliberately avoided by [chain]" → "stays within the no-JS form-handling shape ..." + "a sizable step ... JavaScript is part of the available toolkit (see ADR-035), but reaching for it ... would be disproportionate" + an explicit reframing note that the earlier wording read as a standing prohibition the prior ADRs never established); §decision-commitments ("The no-JavaScript posture ... is **preserved**" → "The no-JS form-handling shape ... is **followed**"); §"What of ADR-025 is retained" ("Synchronous PRG with no JavaScript" → "Synchronous PRG; no JavaScript needed for this surface"); §"I am NOT pushing back on" ("The no-JS commitment ... — **preserved** ... The no-JS posture would be superseded *only* under the Option 2 contingency" → "The no-JS form-handling shape ... — **followed** ... Adding client-side JS would be its own ADR if a real need arises; that's a normal architectural step, not a forbidden threshold").
- **ADR-031** ×8: §"Not Option 2" ("a permanent no-JS-posture supersedure" struck from the cost list; added "The objection is 'this doesn't need JavaScript here,' not 'JavaScript is off-limits'"); §decision-commitments ("The no-JavaScript posture ... is **preserved**" → "The no-JS form-handling shape ... is **followed** ... the human's choice here was a deliberate 'this doesn't need JavaScript,' not the discharge of an invariant"); §"What of ADR-030 is retained" ("ADR-030's no-JS commitment. ADR-030 preserved the no-JS posture; this ADR also preserves it ... There is no longer a path by which the completion toggle forces client-side JS" → "ADR-030's no-JS form-handling shape ... but client-side JS was never forbidden; see ADR-035"); §"What this ADR retains from ADR-030" point 4 ("its no-JS commitment" → "the no-JS form-handling shape it stayed within ... not a project rule"); §"I am NOT pushing back on" ("The no-JS commitment ... — **preserved**" → "The no-JS form-handling shape ... — **followed** ... Following a clean recipe, not honoring a project rule"); §"Becomes impossible" ("Client-side JavaScript for the completion toggle ... — moot" → "A *pressure forcing* client-side JavaScript onto the completion toggle ... — moot ... A future ADR could still add progressive-enhancement JS on top"); §Consequences ("The adoption of client-side JavaScript. This ADR preserves the no-JS posture" → "The adoption of client-side JavaScript *for the completion toggle*. This ADR's mechanism needs none ... A future ADR could still add it for some other reason — that's a normal step"); §Consequences ("The no-JS posture is preserved — the project still has zero client-side JavaScript" → "This mechanism adds no client-side JavaScript — but that is a fact about this surface, not a project-wide ban"); §"Future surfaces" + §supersedure-path reframed (cites ADR-032).
- **ADR-032** ×1: a reframing note added to the "Read this first" block — adopting client-side JS for the Notes-save concern does **not** "break a posture" because there is no binding no-JS posture; a future task accepts *that JS itself* on its merits. (The `Status: Proposed` / deferred state is unchanged; no other edit to ADR-032.)
**ADRs reviewed and left as-is (classified descriptive, not over-reach):** ADR-010 (the Cypress/Node-toolchain rejection is about the *test* stack — Node vs Python — backed by ADR-003's Python-only `app/`; not a client-side-JS rule, not over-reach); ADR-006 §Alternatives F was lightly touched (added the "not 'JavaScript'" clarifier) but its decision and reasoning were already descriptive. Various "synchronous, no-JS, server-side validation" summary phrases inside §"What is being decided" lists in ADR-023/025/027 were left as-is (descriptive recaps of settled decisions, not standing prohibitions).
**ADRs proposed:** `ADR-035` — *ADRs describe the architecture used, not a restriction of the available toolkit; JavaScript is part of the toolkit; no-JS / CSS-only solutions are preferred where clean and sufficient, never mandatory; "no JavaScript" is not a project invariant.* Reverses no decision; records the framing principle and that the over-reaching language in ADR-006/023/025/027/028/029/030/031 (+ ADR-034 line 89) was softened in place. `Status: Proposed` — **awaits the human's acceptance gate**.
**Project issues opened/resolved:** none. (No project_issue covered this; the human directed the cleanup directly. The implementer PUSHBACK in Run 004 and the `/auto` stop in Run 005 are the trigger record; this run resolves the underlying smell those surfaced.)
**architecture.md changes:** one row added to "Proposed ADRs (awaiting human acceptance)" — ADR-035. The ADR-032 row's note softened ("The no-JS-posture supersedure this implies ... not effected by ADR-032" → "Adopting that JS does not 'break a posture' — there is no binding no-JS posture; see ADR-035"). Project-structure summary softened mechanically in three paragraphs ("Per-Section Quiz surface" — "No JavaScript." → "This surface needs no JavaScript — ... a description of this surface, not a project-wide rule (see ADR-035)"; "Notes surface" — "and no JavaScript" → "(a clean CSS-only auto-resize, so no JavaScript is needed here)" + the parked-ADR-032 sentence reframed; "Section completion surface" — "No JavaScript; synchronous form posting throughout." → "This surface uses a synchronous form post + PRG redirect and needs no JavaScript — a description of what was clean and sufficient here, not a project-wide constraint (see ADR-035)"). No new architectural content introduced into architecture.md beyond mirroring the reframed ADRs; the ADR-031 row title (which contains "with no JavaScript") was left as-is — it mirrors that ADR's own title.
**Write-path check:** clean. Files created/modified are confined to `design_docs/decisions/**`, `design_docs/architecture.md`, and `design_docs/audit/**` — all within the architect's allowed write paths. No `app/`, no `tests/`, no `CLAUDE.md`, no `MANIFEST.md`, no skill file, no scratch file created or modified.
**Architecture leaks found:** the leak this cleanup *closes* — ADR-034 line 89's assertion of "the project's established posture (ADR-023/025/027/028/029/030/031)" for an invariant no binding document established, plus the same accreted "no-JS posture" framing in the cited chain. Reported here as resolved (the framing was the leak; ADR-035 + the in-place softening is the fix). No *new* leaks found. No `.md` file now introduces an architectural claim without an Accepted-ADR backing — and ADR-035 (Proposed) is the artifact that will back the reframed sentences once the human accepts it; until then, the reframed sentences are descriptive ("this surface needs no JS"), which needs no ADR backing at all.
**Pushback raised:** none beyond agreeing with and executing the human's directed correction. No `MANIFEST TENSION` (the manifest is the *clean* document here — it never had a no-JS rule; that cleanliness is the evidence the ADRs over-reached). No `> NEEDS HUMAN` on the cleanup itself (the human directed it). ADR-035 awaits the routine acceptance gate. (Self-critique recorded inside ADR-035's "My recommendation vs the user's apparent preference": the over-reach accreted through the architect's own prior `/design` cycles (ADR-025 → 027 → 028 → 029 → 030 → 031 → 034, each citing the prior); the lesson for future cycles is to phrase tool non-use as "this surface didn't need X," not "the project's posture is no-X.")
**Implementation blocked pending human acceptance:** ADR-035 is `Proposed` and awaits the human's `Accepted` gate before the architecture.md row moves to "Accepted ADRs". This does not block TASK-013's remaining work (the TASK-013 PUSHBACK disposition in Run 005 is the separate open item); ADR-035 being Proposed-not-Accepted is the normal state for a freshly-written ADR.
**Output summary:** Human-directed architecture-hygiene cleanup of accreted "no JavaScript posture" over-reach. New ADR-035 (Proposed) establishes that ADRs describe architecture *used*, not the toolkit; JavaScript is in the toolkit; no-JS is preferred-where-clean, never mandatory; no manifest/CLAUDE.md/conformance entry establishes a no-JS rule. Restrictive framing softened in place across ADR-006/023/025/027/028/029/030/031 (decisions unchanged), ADR-034 line 89 specifically corrected, ADR-032's "breaks a posture" framing removed, architecture.md project-structure summary softened mechanically + ADR-035 row added to "Proposed ADRs". No code/test/template/CSS/schema change. ADR-035 awaits human acceptance.

### Run 008 — test-writer (delta: delete test_no_javascript_in_quiz_surface)

**Time:** 2026-05-11T17:00:00Z
**Input files read:** `tests/test_task013_quiz_surface_http.py` (full); `design_docs/audit/TASK-013-quiz-domain-model-and-per-section-quiz-surface.md` (full).
**Tools / commands used:** Read (×2 files); Write (`tests/test_task013_quiz_surface_http.py` — full rewrite with the one test removed); Bash (`python3 -m pytest tests/test_task013_quiz_surface_http.py -q`).
**Files created:** none
**Files modified:** `tests/test_task013_quiz_surface_http.py` — removed `test_no_javascript_in_quiz_surface` (the function body, lines 698–734 of the pre-edit file) and its section-comment header block (lines 691–695), plus the corresponding Edge-matrix bullet in the module docstring (line 33).
**Test removed:** `test_no_javascript_in_quiz_surface`
**Reason for removal:**
  1. **ADR-035 (Accepted, 2026-05-11):** ADRs describe the architecture *used*, not a restriction of the available toolkit; "no JavaScript" is not a project invariant. The test's premise ("the rendered Lecture page must have zero `<script>` tags") has no basis in the manifest, CLAUDE.md, or any conformance-skill rule.
  2. **Implementer Run 004 PUSHBACK:** `base.html.j2` has loaded MathJax `<script>` tags since prior tasks; Playwright tests depend on MathJax running; the assertion `"<script" not in html` can therefore never pass without removing MathJax, which would break a suite of existing passing tests.
  3. **ADR-034 line 89 softened (Run 006):** the original "No JavaScript … the project's established posture" language — on which the test's AC trace rested — was reframed to "This surface needs no JavaScript … a description of this surface, not a project-wide rule." The test enforced the over-reaching framing, not the softened description.
  Nothing was added to replace this test; the delta brief explicitly prohibited replacement.
**No other test in the file was touched.**
**Pytest result:** 63 passed, 0 failed (15.39 s) — `tests/test_task013_quiz_surface_http.py` fully green.
**Pushback raised:** none.
**Architecture leaks found:** none.
**Assumptions:** none.

### Run 009 — verify + PUSHBACK resolution (orchestrator)

**Time:** 2026-05-11T17:30:00Z
**Context:** Resolves the Run 005 `/auto`-stop trigger (implementer Run 004 `PUSHBACK:` on `test_no_javascript_in_quiz_surface`). The human directed the disposition: confirm "no JavaScript" is not a project constraint → architect did the architecture-hygiene cleanup (Run 006: ADR-035 + 9 ADRs softened in place), the human accepted ADR-035 (Human-gates row, 16:30), the test-writer deleted the over-reaching test (Run 008), and the human directed a guardrail tweak to the architect agent so the over-reach does not recur. The human also directed a database cleanup. *(Run-numbering note: there is no Run 007 — the header's earlier "see Run 007" forward-reference was wrong and has been corrected to "Run 006 + Run 008 + Run 009"; Run 008, written by the test-writer, was numbered before this entry, so this orchestrator entry is Run 009. No entry was rewritten; the 007 gap is documented here.)*
**Input files read:** `design_docs/audit/TASK-013-...md` (full); `design_docs/decisions/ADR-035-...md` (header); `design_docs/architecture.md` (Accepted/Proposed tables + project-structure summary); `.claude/agents/architect.md` (full); `tests/conftest.py`; `app/persistence/connection.py`; `data/notes.db` (table list + row contents, pre-deletion).
**Tools / commands used:** `Edit` (`design_docs/decisions/ADR-035-...md` Status → `Accepted`; `design_docs/architecture.md` — moved ADR-035 row Proposed→Accepted, updated the Accepted-set derivation line to add 035, added an "ADR scope" project-structure paragraph mirroring ADR-035; `design_docs/audit/...` — header Status/phase, ADR-035 Human-gates row, this entry; `.claude/agents/architect.md` — added the "ADR records architecture used, not a restriction of the toolkit (ADR-035)" rule to the Mode 2 critical pass and the Mode 2 Rules list; `tests/conftest.py` — added `pytest_configure`/`pytest_unconfigure` test-DB isolation); `Bash` (`rm -f data/notes.db`; `python3 -m pytest tests/ -q` ×2; `python3 -m pytest tests/playwright/ -q`; `python3 -m pytest tests/test_task013_quiz_surface_http.py -q` via test-writer; end-to-end smoke — `uvicorn` on a temp `NOTES_DB_PATH`, `curl` GET `/lecture/ch-01-cpp-refresher` + POST `.../sections/1-1/quiz` + error cases).
**Files created:** none
**Files modified (by the orchestrator, this run):** `design_docs/decisions/ADR-035-adrs-describe-architecture-used-not-the-available-toolkit.md` (Status → `Accepted` + acceptance note); `design_docs/architecture.md` (ADR-035 row moved to "Accepted ADRs"; derivation line updated to include 035; new "ADR scope" paragraph in the project-structure summary); `.claude/agents/architect.md` (new guardrail bullet in Mode 2 critical pass + Mode 2 Rules — human-directed process tweak so the no-JS-style over-reach does not recur); `tests/conftest.py` (test-DB isolation via `pytest_configure`/`pytest_unconfigure` → `NOTES_DB_PATH` points at a per-session temp file, removed at session end; inherited by both the in-process `TestClient` and the `live_server` uvicorn subprocess); this audit file (header, Human-gates row, this entry). **Files deleted:** `data/notes.db` (gitignored dev DB — contents were dev/test cruft: 22 notes ("End-to-end verification note from TASK-009 implementer", "Rail-resident-note-roundtrip-TASK011-…", "RHS-rail-round-trip-PW-TASK012-…", "bbbbb", "b", "quick test 1", …), 5 test-run `section_completions`, 6 `requested` quiz rows from smoke tests; the schema — including the five Quiz tables — re-bootstraps empty on next app launch).
**Note on `tests/conftest.py`:** root cause was that the suite had always written to the real `data/notes.db` (e.g. `tests/playwright/test_task012_*` carried the comment "Tests must not assume a clean DB"); the new `pytest_configure` hook closes that — `data/notes.db` is no longer touched by the test suite and no test data persists after a run.
**Test results (this run):** `python3 -m pytest tests/` → **936 passed, 0 failed** (6m12s) — exactly one fewer collected than the prior 937 (936 pass + 1 fail), confirming the test-writer removed exactly `test_no_javascript_in_quiz_surface` and nothing broke. `python3 -m pytest tests/playwright/` → **195 passed** (4m11s, verified with the new conftest before the test deletion; deleting a non-Playwright test + editing markdown/agent-config cannot affect it). `data/` stays empty across both runs. End-to-end smoke (temp DB): `GET /lecture/ch-01-cpp-refresher` → 200, 15 `.section-quiz` blocks / 15 empty-state captions / 15 "Generate a Quiz" buttons; `POST .../sections/1-1/quiz` → 303 `Location: /lecture/ch-01-cpp-refresher#section-1-1-end`; `POST` unknown chapter → 404; `POST` out-of-range section → 404; re-GET → the posted Section now shows `section-quiz-item--requested` / "Requested" (never "ready"), the other 14 still empty-state.
**Lint / type-check:** still not configured (`tooling-lint-and-type-check.md` project_issue, Open — ninth recurrence; not a TASK-013 deliverable).
**Conformance walk:** deferred to the reviewer (invoked next), which runs the `manifest-conformance` skill as part of its protocol. Orchestrator spot-check: no `import sqlite3` / SQL literals outside `app/persistence/`; `quizzes.section_id` declared `NOT NULL`; no `user_id` on any Quiz-domain table; no AI call introduced; the `requested`-status surface never presents a Quiz as finished; no `content/latex/` writes — all consistent with MC-1..MC-10.
**Architecture leaks found:** none. The accreted "no-JS posture" framing — the leak this whole episode surfaced — is closed by ADR-035 + the in-place softening (Run 006) and the architect-agent guardrail added this run.
**Pushback raised:** none. The Run 005 trigger is resolved; no new pushback.
**Output summary:** PUSHBACK resolved (ADR-035 accepted; over-reaching test deleted; architect-agent guardrail added so it does not recur). Database cleaned (`data/notes.db` wiped — dev cruft). Test suite given DB isolation so test data no longer persists. Full suite green (936 passed, 0 failed; 195 Playwright passed); end-to-end smoke of the per-Section Quiz surface clean. Next: stage the changeset and invoke the reviewer; commit awaits the human's go-ahead.

### Run 010 — reviewer

**Time:** 2026-05-11T18:00:00Z
**Staged files reviewed:** `.claude/agents/architect.md`, `app/main.py`, `app/persistence/__init__.py`, `app/persistence/connection.py`, `app/persistence/quizzes.py`, `app/static/lecture.css`, `app/templates/lecture.html.j2`, `design_docs/architecture.md`, `design_docs/audit/TASK-013-...md`, `design_docs/decisions/ADR-006/023/025/027/028/029/030/031/032` (framing-only softenings), `design_docs/decisions/ADR-033`, `ADR-034`, `ADR-035` (new), `design_docs/tasks/TASK-013-...md` (new), `tests/conftest.py`, `tests/playwright/test_task013_quiz_surface_dom.py`, `tests/test_task013_quiz_schema.py`, `tests/test_task013_quiz_surface_http.py`.
**Unstaged source/test warning:** none — `git diff --name-only` is empty; the staged set is the whole working-tree change.
**Conformance skill result:** 0 blockers, 0 warnings, 2 dormant (MC-1 architecture portion — `cannot evaluate (ADR pending)`, no AI-engine ADR yet, and no AI call introduced; MC-3 architecture portion — canonical-mapping source remains ADR-004's, unchanged). MC-2/5/6/7/8/9/10 walked against the staged diff — all PASS. MC-10's architecture portion is active (ADR-022) and at blocker severity now that `app/persistence/` exists — verified clean: `import sqlite3` only in `app/persistence/connection.py`; SQL string literals only in `connection.py` + `quizzes.py`; `app/main.py` + `lecture.html.j2` call only typed public functions (`request_quiz`, `list_quizzes_for_chapter`).
**Architecture leaks found in .md files:** none. `architecture.md` remains index-only; the three new project-structure paragraphs (Quiz domain schema, Per-Section Quiz surface, ADR scope) and the two reframed surface paragraphs mirror Accepted ADRs 033 / 034 / 035 verbatim-in-substance — no architectural claim outside an Accepted ADR. The "no-JS posture" leak this episode surfaced is closed (ADR-035 + in-place softening of ADR-006/023/025/027/028/029/030/031/034 + ADR-032 reframing note + architect-agent guardrail); spot-checked ADR-006/023/031/034 softenings — framing-only, no decision reversed.
**Blocking findings:** none.
**Non-blocking findings:**
  - `tests/playwright/test_task013_quiz_surface_dom.py` module docstring still says "The live_server uses the real data/notes.db. Tests must not assume a clean database" — stale after the `tests/conftest.py` `pytest_configure` change points `NOTES_DB_PATH` at a per-session temp file (the subprocess inherits it). The tests still pass (they only assert empty-state presence + button presence, both true on a fresh DB); the comment is just out of date. Tidy on a future touch.
  - `app/persistence/quizzes.py` `_row_to_quiz` / `_row_to_question` annotate `row: object` then subscript `row["..."]` — fine at runtime (and these are private helpers, so the type-hint convention doesn't bite), but a future mypy enablement (the standing `tooling-lint-and-type-check` project_issue) will want `sqlite3.Row` or a Protocol there.
  - `request_quiz` uses `cursor.lastrowid` without a None-guard — harmless for an `INSERT` against an AUTOINCREMENT PK, but a defensive `assert quiz_id is not None` would silence a future type-checker.
**Verification-gate note:** TASK-013's `## Verification gates (human-only)` rendered-surface review (per-Section Quiz surface reads clearly in the reading flow; empty-state legible; trigger button visually clear; no layout regression on Mandatory + Optional Chapters) is **pending human** — not the reviewer's to sign off. The audit Human-gates table needs a `rendered-surface verification — pass (TASK-013 per-Section Quiz surface)` row filled in post-commit by the human.
**Final result:** READY TO COMMIT
