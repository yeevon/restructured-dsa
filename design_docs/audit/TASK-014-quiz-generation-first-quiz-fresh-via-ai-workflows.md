# LLM Audit — TASK-014: Quiz generation — the first Quiz for a Section, fresh-only, via `ai-workflows`, async, walking a `requested` Quiz to `ready` (slice 2 of the Quiz pillar)

**Task file:** `design_docs/tasks/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md`
**Started:** 2026-05-11T00:00:00Z
**Status:** Reviewed
**Current phase:** review

---

## Human gates

| Time | Gate | Result | Notes |
|---|---|---|---|
| 2026-05-11T19:00:00Z | Task reviewed | auto-accepted | /auto run |
| 2026-05-12T01:00:00Z | `> NEEDS HUMAN` (ADR-036 dependency mechanism) resolved | resolved | Human resolved: CS-300 depends on `jmdl-ai-workflows` as a normal PyPI version pin in `pyproject.toml` `[project] dependencies` (the co-located `/home/papa-jochy/prj/ai-workflows` clone is read-only reference only, not a path dep). Architect re-invoked (Run 004) to fold the resolution into ADR-036 + correct the integration shape against the actual framework. |
| 2026-05-12T01:30:00Z | ADR-036 reviewed | accepted | Human reviewed the revised ADR-036 (post-`NEEDS HUMAN`-resolution; PyPI pin + `aiw run` CLI-subprocess invocation + CS-300-processor-boundary test seam) and directed acceptance — chose "accept as-is" over adjusting the invocation path. `/auto` resumption. |
| 2026-05-12T01:30:00Z | ADR-037 reviewed | accepted | Human reviewed the revised ADR-037 (invocation-detail references updated to ADR-036's `aiw run` path; decisions unchanged) and directed acceptance. `/auto` resumption. |
| 2026-05-12T02:30:00Z | Tests reviewed | auto-accepted | /auto run — `tests/test_task014_quiz_generation.py` (34 tests) + `tests/playwright/test_task014_quiz_generation_dom.py` (11 tests); non-Playwright run: 21 task-014 tests failing pre-implementation (for the right reasons — `ModuleNotFoundError: No module named 'app.workflows'`, missing `quizzes.generation_error` column, 409 guard not implemented), 754 passing (full pre-existing suite green + the 13 legit-passing task-014 tests). Test-writer raised no PUSHBACK / CANNOT-TEST / leak; all 14 programmatic ACs covered. |
| 2026-05-12T03:30:00Z | rendered-surface verification — pass (TASK-014 Quiz-generation surface states) | pending human | /auto run — visual check post-commit. Human reviews the per-Section Quiz surface on ≥1 Mandatory + ≥1 Optional Chapter with Quizzes driven (via the test seam) into `generating` / `ready` / `generation_failed` (last-run Playwright screenshots under `tests/playwright/artifacts/` or the dev server directly): the four states clearly distinguished; `ready` not misleading (no fake take-affordance — just "Ready"); `generation_failed` reads as a failure; the existing Lecture surfaces (3-column layout, RHS Notes rail, bottom-of-Section completion affordance, the Quiz block's empty-state for Sections with no Quizzes) unchanged — no layout regression. Edit `pending human` → `pass` (or file a finding) post-commit. (Programmatic structure covered: 982 pytest tests green incl. 12 task-014 Playwright DOM tests asserting the labels + the no-take-affordance; the *visual* review is human-only per ADR-010 / the task's Verification-gates section.) |
| 2026-05-12T03:30:00Z | ai-workflows generation sanity — pass (TASK-014 first-Quiz fresh generation) | pending human | /auto run — real-engine check post-commit. Human runs the out-of-band processor (`python -m app.workflows.process_quiz_requests`) against a `requested` Quiz for a real Section with the *actual* `ai-workflows` engine (needs `GEMINI_API_KEY` exported for the default `gemini/gemini-2.5-flash` tier, or whichever tier `app/workflows/question_gen.py` declares) — NOT the test stub: a set of hands-on coding-task Questions recognizably *about the Section's content* is produced; the Quiz reaches `ready`; the per-Section surface shows "Ready". (Cannot be a programmatic AC — depends on a real AI call (tests must not make one) + human judgment of "is this a sensible coding-task Question about this Section". If "not coding tasks / not about the Section / low quality" → `fail`, the workflow's prompt needs work — a follow-up, not a re-design.) Edit `pending human` → `pass` (or file a finding) post-commit. |

---

## Agent runs

### Run 001 — architect / Mode 1 `/next`

**Time:** 2026-05-11T00:00:00Z
**Input files read:** `design_docs/MANIFEST.md`; `CLAUDE.md`; `design_docs/architecture.md`; `.claude/skills/manifest-conformance/SKILL.md`; `.claude/skills/ui-task-scope/SKILL.md`; `design_docs/tasks/` (all 13 task files; full read of TASK-013); `design_docs/decisions/` (full read of ADR-034, ADR-033 (in parts — large file); index/title scan of the rest); `design_docs/project_issues/` (all 18 files; full read of `tooling-lint-and-type-check.md`); `app/main.py`; glob of `app/**/*.py` and `app/templates/*.j2`; glob for `**/ai-workflows/**` (confirmed: `ai-workflows` is not present in the repo).
**Tools / commands used:** Read (MANIFEST.md, architecture.md, manifest-conformance SKILL.md, ui-task-scope SKILL.md, ADR-034, ADR-033 ×2 ranges, TASK-013 ×2 ranges, tooling-lint-and-type-check.md, app/main.py); Glob (`design_docs/tasks/*.md`, `design_docs/decisions/*.md`, `design_docs/project_issues/*.md`, `app/**/*.py`, `app/templates/*.j2`, `**/ai-workflows/**`); Write (`design_docs/tasks/TASK-014-...md`, `design_docs/audit/TASK-014-...md`).
**Files created:** `design_docs/tasks/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md`, `design_docs/audit/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md`
**Files modified:** none
**Task alternatives considered:**
- (Chosen) Quiz generation — first Quiz, fresh-only, via `ai-workflows`, async (slice 2 of the Quiz pillar's planned 3).
- Quiz generation in full (fresh + replay-+-fresh composition + Weak-Topic targeting all at once) — rejected: the composition presupposes Attempt history, which presupposes the Quiz-taking surface + the grading workflow (3 more slices of machinery); multi-session by construction; TASK-013 already forecast the split.
- The Quiz-taking surface first — rejected as premature: no `ready` Quizzes exist yet (no generation built); cannot be a true vertical slice now; it is slice 3.
- The `ai-workflows` integration foundation only (AI-engine ADR + async mechanism + a smoke workflow, no Quiz feature) — rejected: an integration with no AI feature is architecture-on-spec; the AI-engine ADR should be forced by the first real AI feature; §4's secondary objective "never justifies adding a feature".
- The grading workflow first — rejected as out of order: grading presupposes Attempts (the Quiz-taking surface), which presuppose `ready` Quizzes (this task); grading is slice 4.
- Continue polishing the Lecture page / pick up the parked ADR-032 / parser-fidelity issues — rejected: Lecture page is settled (TASK-012 committed); ADR-032 is human-deferred; parser-fidelity is low-priority; none advances the primary objective.
- A smaller slice: AI-engine ADR + a workflow that runs but doesn't persist / doesn't wire the lifecycle — rejected as a horizontal layer, not a vertical slice (the user clicks Generate and nothing changes).
- Lint/type-check tooling task — rejected as a task (ninth recurrence); doesn't advance the primary objective; surfaced as a recommendation instead.
**Decisions surfaced:** TASK-014's "Architectural decisions expected" forecasts 2–4 ADRs for `/design TASK-014`: (1) the `ai-workflows` integration ADR (the AI-engine ADR — what `ai-workflows` is, the dependency mechanism, the workflow module path, the integration shape, the forbidden-SDK list; activates MC-1's architecture portion; the `ai-workflows`-dependency-mechanism decision is flagged as the most likely `> NEEDS HUMAN`); (2) the async-delivery / out-of-band-processing ADR (may be folded into the AI-engine ADR — where the processing runs, the trigger handoff, how the learner is notified, the failure-handling discipline); (3) (likely) a Topic-vocabulary ADR (the relational `topics` table + `question_topics` join, honoring ADR-033 §Topic tags' deferral "to the generation task" — or an explicit re-deferral to the Weak-Topic-identification slice); (4) (possibly) a Notification-entity + surface ADR (or a section of the async-delivery ADR). No new project_issue filed by this `/next` cycle (the Topic-vocabulary question already has a named home per ADR-033; the Notification question is folded into the task's `/design` decisions). Pointer: `design_docs/project_issues/tooling-lint-and-type-check.md` (Open, ninth recurrence) surfaced again with a direct recommendation; `design_docs/decisions/ADR-032-notes-save-must-not-relocate-the-reader.md` (`Proposed`, deferred) noted as not picked up by this task.
**Architecture leaks found:** none. Every `.md` in the prerequisite set classified correctly per the CLAUDE.md tier table. `architecture.md` is index-only with the project-structure summary derived from Accepted ADRs (carries the ADR-033/034/035 additions from TASK-013 correctly). CLAUDE.md's `<project lint command>` / `<project type-check command>` placeholders are a known project-setup gap (tracked as a project_issue), not an architecture leak. The conformance skill correctly reflects MC-7/MC-10 as no-longer-dormant per ADR-022 and MC-1's architecture portion as still dormant (the AI-engine ADR doesn't exist yet — TASK-014 lands it). ADR-032 is `Proposed` (Tier-1, no authority until gated); the `notes-save-relocates-reader.md` project_issue correctly points at ADR-032.
**Pushback raised:** none (`MANIFEST TENSION:` — none; `PRIMARY OBJECTIVE COMPLETE:` — none; `ARCHITECTURE LEAK:` — none). The manifest is internally consistent with TASK-014. Architectural concerns recorded in the task file (not pushback, but flagged for the architect): (a) this is the largest task the project has attempted — the `ai-workflows` dependency mechanism is a likely `> NEEDS HUMAN`; if `/design` finds the slice still too large, split (defer the Notification *entity* to the grading slice; ship the AI-engine ADR + workflow + async-via-status-flip + tests this task); (b) MC-1's architecture portion activates — the conformance skill must be updated in the same commit (the human's edit; the AI-engine ADR flags it); (c) don't over-build the async mechanism — the simplest out-of-band mechanism that satisfies MC-4/MC-9, not a production job queue (§5 "no remote deployment"); (d) "the learner is notified when results are ready" (MC-4/§8) — is the per-Section status-flip enough or does it need an active Notification surface? `/design` reasons it through (defensible middle: status-flip sufficient for this slice, Notification entity ships with the grading slice); (e) MC-8's "fresh-only Quizzes are a bug" must not be triggered — this task ships fresh-only generation, which is *correct* for a first Quiz and a *bug* for any later Quiz; the first-Quiz-only guard is load-bearing; (f) no coupling to the later slices' surfaces (no take-button, no grading hooks, no Weak-Topic plumbing); (g) `architecture.md` summary regenerated post-gate per the maintenance protocol; (h) inherited ninth-recurrence tooling project-setup gap — direct recommendation to the user.
**Output summary:** Proposed TASK-014 — Quiz generation (first Quiz, fresh-only) via `ai-workflows`, async, walking a `requested` Quiz to `ready`: the slice that adds the processor TASK-013's `requested` row was waiting for, forces the AI-engine ADR (§4; activates MC-1's architecture portion) + the async-delivery mechanism (MC-4), and makes the §7 reinforcement loop's generation half real.

### Run 002 — architect / Mode 2 `/design`

**Time:** 2026-05-11T20:30:00Z
**Input files read:** `design_docs/MANIFEST.md`; `CLAUDE.md`; `.claude/skills/manifest-conformance/SKILL.md`; `design_docs/architecture.md`; `design_docs/decisions/` (full read of ADR-034, ADR-033 in parts, ADR-022 in parts; index/title scan of the rest); `design_docs/project_issues/` (glob; full read of `tooling-lint-and-type-check.md`); `design_docs/tasks/TASK-014-...md` (full, in parts); `design_docs/audit/TASK-014-...md`; `app/main.py`; `app/persistence/quizzes.py`; `app/persistence/__init__.py`; `app/persistence/connection.py`; `app/config.py`; `app/templates/lecture.html.j2`; `pyproject.toml`. **`ai-workflows` framework investigation:** `/home/papa-jochy/prj/Restructured-CS-300/.venv/lib/python3.13/site-packages/_editable_impl_jmdl_ai_workflows.pth` (→ `/home/papa-jochy/prj/ai-workflows`); `/home/papa-jochy/prj/ai-workflows/pyproject.toml`; `/home/papa-jochy/prj/ai-workflows/ai_workflows/__init__.py`; `/home/papa-jochy/prj/ai-workflows/ai_workflows/workflows/__init__.py`; `.../workflows/spec.py`; `.../workflows/_dispatch.py`; `.../workflows/loader.py`; `.../workflows/testing.py`; `.../workflows/summarize_tiers.py`; `.../evals/_stub_adapter.py`; `/home/papa-jochy/prj/ai-workflows/docs/writing-a-workflow.md`.
**Tools / commands used:** Read (the files above, several in offset/limit ranges due to size); Glob (`design_docs/decisions/*.md`, `design_docs/project_issues/*.md`, `pyproject.toml`, `requirements*.txt`, `.venv/.../site-packages/*workflow*`, `.venv/.../site-packages/__editable__*`, `/home/papa-jochy/prj/ai-workflows/**/*.py`, `/home/papa-jochy/prj/ai-workflows/ai_workflows/**/*.py`, `/home/papa-jochy/prj/ai-workflows/docs/*.md`, `/home/papa-jochy/prj/ai-workflows/pyproject.toml`); Write (`design_docs/decisions/ADR-036-ai-workflows-integration.md`, `design_docs/decisions/ADR-037-async-quiz-generation-processing.md`); Edit (`design_docs/architecture.md` — Proposed-ADRs row for 037, Pending-resolution row for 036; `design_docs/audit/TASK-014-...md` — header Status/phase + this Run entry). No `git status --short` run via a tool (no Bash tool invoked); write-path self-audited below.
**Files created:** `design_docs/decisions/ADR-036-ai-workflows-integration.md` (`Status: Pending Resolution`); `design_docs/decisions/ADR-037-async-quiz-generation-processing.md` (`Status: Proposed`).
**Files modified:** `design_docs/architecture.md` (added ADR-037 to "Proposed ADRs"; added ADR-036 to a new "Pending resolution (need human input)" table — the section previously read "(none)"); `design_docs/audit/TASK-014-...md` (header Status → Blocked, phase → design; this Run 002 entry).
**ADRs proposed:** `ADR-036` — `ai-workflows` integration: depend on `jmdl-ai-workflows >= 0.4` (mechanism — PyPI pin vs local path/editable — is `Pending Resolution`, the human's call); CS-300-owned `WorkflowSpec` module at `app/workflows/question_gen.py`; discovered via `AIW_EXTRA_WORKFLOW_MODULES` / direct import; invoked through `ai_workflows.workflows._dispatch.run_workflow`; the workflow returns data, the CS-300 processor persists it via `app.persistence.*` (new `mark_quiz_*` / `add_questions_to_quiz` / read accessors in `app/persistence/quizzes.py` — SQL stays there, MC-10); declarative authoring surface (one `LLMStep` with `prompt_fn` + the framework's required `response_format` validator); `QuestionGenOutput` has no choice/recall/describe field (non-coding Question unexpressible); test seam = the framework's `StubLLMAdapter` monkeypatch (CS-300-side processor-boundary fake the fallback); `ai-workflows` persistence cohabitation = ADR-022 §`ai-workflows`-cohabitation option (c) (the framework owns its own run/checkpoint store; no `ai-workflows` tables in `data/notes.db`); forbidden-SDK list enumerated (`openai`/`anthropic`/`google.generativeai`/`google.genai`/`cohere`/`litellm`/`langchain*`/`langgraph*`/raw provider HTTP) — goes into the conformance skill's MC-1 entry in the same commit that Accepts the ADR (the human's edit; activates MC-1's architecture portion); Topic-vocabulary relational form deferred again to the Weak-Topic-identification slice (honoring ADR-033 §Topic tags — no new ADR, no project_issue). `ADR-037` — async Quiz-generation processing: out-of-band manual command `python -m app.workflows.process_quiz_requests` (poll-and-process `requested` rows — not a background worker, not a daemon, not a job queue); `POST .../quiz` route unchanged (no enqueue; the processor polls independently); failure → `generation_failed` + the detail persisted in a new nullable `quizzes.generation_error TEXT` column (additive `ALTER TABLE ADD COLUMN` per ADR-022; `mark_quiz_generation_failed` gains an optional `error` param); no fabricated Questions, no silent unbounded retry (the framework's `RetryingEdge` does bounded retry inside the workflow; the command doesn't re-process `generation_failed` rows; a re-attempt is a new user-triggered `requested` row — MC-5/MC-9); the first-Quiz-only guard rejects (HTTP 409) a second `requested` request for a Section that already has a non-failed Quiz (a `generation_failed` Quiz doesn't count) — keeps MC-8 clean by construction; the "notified when ready" obligation met for this slice by the per-Section status-flip to "Ready" (ADR-034's surface, now exercised) — the active Notification entity (the `notifications` table + a learner-visible surface) ships with the grading slice (where there's a Grade to notify about too and a real "the learner is elsewhere" case); flag (not an edit): CLAUDE.md "Commands" should gain "Process pending Quiz requests: `python -m app.workflows.process_quiz_requests`" — the human owns that.
**Project issues opened/resolved:** none. (The Topic-vocabulary relational-form question has a named home — the Weak-Topic-identification slice's `/design` — per ADR-033 §Topic tags and ADR-036 §Topic tags, so no project_issue is filed. `tooling-lint-and-type-check.md` stays Open — ninth recurrence — and is not resolved by TASK-014; the codebase grows `app/workflows/` and more persistence functions this task, so ADR-022's "would benefit from mypy on the public API" applies more — re-flagged for the human, not a TASK-014 deliverable.)
**architecture.md changes:** ADR-037 row added to "Proposed ADRs (awaiting human acceptance)"; ADR-036 row added to a newly-populated "Pending resolution (need human input)" table (previously "(none)"). No project-structure-summary change this cycle (no ADR state changed to `Accepted`; per the maintenance protocol the summary is regenerated only when the Accepted-ADR set changes — so it stays as it was through ADR-034/035 in TASK-013; it will be regenerated after the human gates ADR-036/037, adding an "AI integration / Quiz generation" paragraph and noting the manual command + the deferred Notification/Topic-vocabulary decisions).
**Write-path check:** clean. All writes were under `design_docs/{decisions,architecture.md,audit}/**` — within the Mode-2 allowed set (`design_docs/{tasks,audit,decisions,project_issues}/**`). No scratch/test/source file was created; nothing under `app/`, `tests/`, `content/latex/`, `CLAUDE.md`, `MANIFEST.md`, or a skill file was touched; `/home/papa-jochy/prj/ai-workflows` was read-only (investigated for the AI-engine ADR, never written).
**Architecture leaks found:** none. Every `.md` in the input set classified correctly per the CLAUDE.md tier table. `architecture.md` is index-only (the project-structure summary derives from Accepted ADRs; this cycle adds only Proposed/Pending rows — no new architectural claim introduced into `architecture.md`). CLAUDE.md is process-and-conventions only (its `<project lint command>` / `<project type-check command>` placeholders are a known project-setup gap tracked as a project_issue, not a leak; its inline ADR-016 citation is correct). The conformance skill correctly reflects MC-1's architecture portion as still dormant (`cannot evaluate (ADR pending)`) — TASK-014 lands the AI-engine ADR (ADR-036), at which point (on the human's Acceptance) MC-1's architecture portion becomes a `blocker` and the skill's MC-1 entry is updated in the same commit (the human's edit). ADR-033 and ADR-034 carry no leak — consumed unchanged here; no genuine schema flaw found, so no supersedure proposed. The `ai-workflows` framework's own docs (`docs/writing-a-workflow.md`) are Tier-4 reference for *this* project (they document a different repo's architecture) — read as evidence for the integration shape, not treated as authority over CS-300.
**Pushback raised:** none as `MANIFEST TENSION:` / `ARCHITECTURE FLAW:` / `ARCHITECTURE LEAK:`. One `> NEEDS HUMAN:` raised — see "Implementation blocked" below — for the `ai-workflows` dependency mechanism (PyPI pin vs local path/editable), which the task explicitly anticipated as the most-likely-needs-human decision and which gates implementation. Recommendation recorded in ADR-036 (local editable/path dependency for now, revisited to a PyPI pin once the framework stabilises) per the project's "argue your case but the human decides" protocol; the choice is the author's because it has real reproducibility tradeoffs and depends on how they want to manage the two co-developed repos. No disagreement with the user's apparent direction surfaced beyond that — the task's forecasts (the AI-engine ADR, the async-delivery ADR, the Topic-vocabulary-deferred call, the Notification-deferred call) are all followed; the architect made the calls the task's framing granted it the authority to make (the async mechanism = manual command, the notification = status-flip for this slice, the first-Quiz-only guard = route-level reject, the failure detail = persisted).
**Implementation blocked pending human acceptance:** **yes.** ADR-036 is `Pending Resolution` — implementation cannot proceed past it until the human resolves the dependency-mechanism question. ADR-037 is `Proposed` (awaiting the human's Acceptance gate, like every Proposed ADR) but does not *itself* need the human's input — it builds on ADR-036, so it is blocked by ADR-036's resolution anyway. Also flagged (the human's edits, not the architect's): (1) the conformance skill's MC-1 entry must be updated in the same commit that Accepts ADR-036 (enumerate the forbidden-SDK list; name the workflow module path `app/workflows/`); (2) CLAUDE.md's "Commands" section should gain a "Process pending Quiz requests: `python -m app.workflows.process_quiz_requests`" line; (3) `pyproject.toml`'s `requires-python` moves to `>= 3.12` (to match `jmdl-ai-workflows`'s floor) and gains the `jmdl-ai-workflows` dependency in whatever form the human picks.
**Output summary:** `/design TASK-014` recorded ADR-036 (`ai-workflows` integration — `Pending Resolution`: the dependency mechanism is the human's call) and ADR-037 (async Quiz-generation processing — `Proposed`); architecture.md updated mechanically (ADR-037 → Proposed, ADR-036 → Pending resolution); no project_issue opened/resolved; one `> NEEDS HUMAN` (the `ai-workflows` dependency mechanism); implementation blocked pending human acceptance of ADR-036.

### Run 003 — /auto stopped (Phase 2)

**Time:** 2026-05-11T21:00:00Z
**Trigger:** `/auto` stop condition rule 2 — `> NEEDS HUMAN:` from the architect during Phase 2 (`/design`). ADR-036 (`design_docs/decisions/ADR-036-ai-workflows-integration.md`) carries `Status: Pending Resolution`; ADR-037 is `Proposed` but is blocked behind ADR-036 (it builds on it). Implementation cannot proceed.
**Surfaced text (verbatim from the architect):** `> NEEDS HUMAN: ADR-036 — how should this project depend on `ai-workflows`? (declare `"jmdl-ai-workflows>=0.4"` in `pyproject.toml` `[project] dependencies` for a PyPI pin / use a local path or editable dependency to the co-located `/home/papa-jochy/prj/ai-workflows` clone). The framework is currently editable-installed in the dev `.venv` from that clone; the architect recommends keeping a local editable/path dependency for now, revisited to a PyPI pin once the framework stabilises — but the choice has real reproducibility tradeoffs and is the author's call. Also requires: `requires-python` moves to `>= 3.12` (the framework's floor); and, when ADR-036 is Accepted, the conformance skill's MC-1 entry must be updated in the same commit to enumerate the forbidden-SDK list and name the workflow module path `app/workflows/`.`
**Loop state:** stopped at Phase 2 (Architect Mode 2). Phase 1 completed (TASK-014 + audit created, task-review gate auto-accepted). Phases 3–6 (tests, implementation, verify, review+commit) not run.
**Audit Status:** `Blocked` (set by Run 002; unchanged).
**What the human must resolve before re-running `/auto` or running per-phase commands:** (1) decide the `ai-workflows` dependency mechanism (PyPI pin `jmdl-ai-workflows>=0.4` vs local path/editable to `/home/papa-jochy/prj/ai-workflows`) and record it into ADR-036, then change ADR-036 `Status: Pending Resolution` → `Accepted`; (2) review + Accept ADR-037 (`Status: Proposed` → `Accepted`); (3) move both ADR rows in `architecture.md` from "Proposed"/"Pending resolution" to "Accepted ADRs" and regenerate the project-structure summary (mechanical — or ask the architect to); (4) update the conformance skill's MC-1 entry in the same commit that Accepts ADR-036 (enumerate the forbidden-SDK list; name `app/workflows/`); (5) optionally add the `python -m app.workflows.process_quiz_requests` line to CLAUDE.md "Commands" and bump `pyproject.toml` `requires-python` to `>= 3.12`; (6) add the Human-gates rows for ADR-036/ADR-037 acceptance; then run `/implement TASK-014` (or re-run `/auto`, which will resume from Phase 3 once the ADRs are Accepted).

> **Update (2026-05-12):** step (1)'s dependency-mechanism question was **resolved by the human** — a PyPI version pin (`jmdl-ai-workflows>=0.4` in `pyproject.toml` `[project] dependencies`; the co-located clone is read-only reference only; `requires-python` → `>= 3.12`). The architect re-ran `/design` Mode 2 in **Run 004** (below): ADR-036 revised (dependency mechanism = PyPI pin; invocation path switched from the framework's internal `_dispatch.run_workflow` to the documented `aiw run` CLI subprocess; test seam = CS-300's own processor boundary) and `Status: Pending Resolution` → `Proposed`; ADR-037 revised (invocation-detail references updated to ADR-036's `aiw run` path; decisions unchanged), `Status` stays `Proposed`. `architecture.md` updated mechanically (ADR-036 row moved from "Pending resolution" to "Proposed ADRs" with the revised title; ADR-037 row text amended for the `aiw run` reference; "Pending resolution" table reset to "(none)"). No `Pending Resolution` portion remains in either ADR. Implementation is now **unblocked of the `> NEEDS HUMAN`** — it is gated only by the normal human Acceptance of the two `Proposed` ADRs (plus the human's same-commit MC-1 conformance-skill edit and the implementer's `pyproject.toml` edits). Items (2)–(6) above still stand (with (1) now done and (3)'s ADR-036 source already in "Proposed ADRs", needing only the move to "Accepted ADRs" once gated).

### Run 004 — architect / Mode 2 `/design` (resumption after `> NEEDS HUMAN` resolution)

**Time:** 2026-05-12T00:00:00Z
**Input files read:** `design_docs/MANIFEST.md` (§4 / §5 phrasing re-checked); `CLAUDE.md`; `.claude/skills/manifest-conformance/SKILL.md` (MC-1 entry — still `cannot evaluate (ADR pending)`); `design_docs/architecture.md` (full); `design_docs/decisions/` (full read of ADR-036 in two ranges, ADR-037 full; title/index scan of the rest); `design_docs/project_issues/` (glob; no new issue forced); `design_docs/tasks/TASK-014-...md` (the AI-engine-ADR + async-delivery-ADR forecast sections, in parts); `design_docs/audit/TASK-014-...md` (full — Runs 001–003 + the human's resolution directive). **`ai-workflows` framework re-investigation (read-only — Tier-4 reference; the clone is NOT a dependency):** `/home/papa-jochy/prj/ai-workflows/README.md`; `docs/writing-a-workflow.md` (full — esp. §"External workflows from a downstream consumer", §"Worked example — the CS-300 shape", §"Running your workflow", §"Testing your workflow"); `docs/architecture.md`; `ai_workflows/workflows/__init__.py`; `ai_workflows/workflows/spec.py`; `ai_workflows/workflows/_dispatch.py` (in two ranges — confirmed `run_workflow` signature + return-dict shape + the `FINAL_STATE_KEY = "questions" from CS-300` comment + the spec-API `FINAL_STATE_KEY`/`<name>_tier_registry` synthetic-module path); `ai_workflows/workflows/_compiler.py` (the synthetic-module injection — confirms a `WorkflowSpec`-authored downstream workflow is fully dispatch-able); `ai_workflows/cli.py` (full — the `aiw run` stdout/stderr contract: `json.dumps(artifact, indent=2)` then `total cost: $X.XXXX` on completion; `error: ...` on stderr + non-zero exit on failure; structured logs to stderr; `--input KEY=VALUE`; optional `--run-id`); `ai_workflows/evals/_stub_adapter.py` (the `StubLLMAdapter` monkeypatch shape — confirms it's in-process-only); `ai_workflows/workflows/testing.py` (`compile_step_in_isolation`).
**Tools / commands used:** Read (the files above, several in offset/limit ranges due to size); Grep (`design_docs/decisions/*.md` for `FINAL_STATE_KEY` / `_dispatch` / `run_workflow` / `StubLLMAdapter` references in ADR-037 to scope the edits; `ai-workflows` `_dispatch.py` for `FINAL_STATE_KEY`/`CS-300`); Glob (`design_docs/decisions/ADR-03*`, `design_docs/tasks/TASK-014*`, `design_docs/audit/TASK-014*`, `design_docs/project_issues/*`); Write (`design_docs/decisions/ADR-036-ai-workflows-integration.md` — full rewrite); Edit (`design_docs/decisions/ADR-037-async-quiz-generation-processing.md` — ~8 surgical edits replacing `_dispatch.run_workflow`/`asyncio.run`/`StubLLMAdapter`-as-CS-300-seam references with the `aiw run` CLI-subprocess path + the CS-300-processor-boundary test seam; `design_docs/architecture.md` — ADR-036 row moved from "Pending resolution" to "Proposed ADRs" with the revised title, ADR-037 row text amended for the `aiw run` reference, "Pending resolution" table reset to "(none)"; `design_docs/audit/TASK-014-...md` — the Run-003 "Update (2026-05-12)" note + this Run 004 entry). No `git status --short` via a Bash tool (no Bash tool invoked); write-path self-audited below.
**Files created:** none (ADR-036 and ADR-037 already existed from Run 002; this run revises them in place).
**Files modified:** `design_docs/decisions/ADR-036-ai-workflows-integration.md` (full rewrite — `Status: Pending Resolution` → `Proposed`; title rewritten to drop the "…needs the human" clause and name the `aiw run` invocation; revision note added; Decision §dependency-mechanism = PyPI pin (the human's resolution; the local-path/editable option moved to rejected Alternative B; vendoring stays rejected Alternative A); Decision §invocation = `aiw run` CLI subprocess (the in-process `_dispatch.run_workflow` path moved to rejected Alternative D with the trade-off named); Decision §test-seam = CS-300's own processor boundary (the framework's `StubLLMAdapter` moved to rejected Alternative G — unusable across a subprocess); workflow-module-shape §, persistence-cohabitation-(c) §, forbidden-SDK-list §, scope §, manifest-reading §, conformance-check §, consequences § all updated for the `aiw run` shape; the `requires-python >= 3.12` bump and the conformance-skill MC-1 update kept as flags for the implementer / human respectively; the CLAUDE.md "Commands" line stays ADR-037's flag). `design_docs/decisions/ADR-037-async-quiz-generation-processing.md` (revision note added; `Supersedes`/`Context`/processor-step-2c/§Why-manual-command/§Why-persist-failure-detail/§no-silent-retry/§not-pushing-back-on-ADR-036/§does-not-decide/§Alternative-A/§Consequences/§supersedure-path/§Conformance-check-MC-1/MC-5 all updated to reference ADR-036's `aiw run` CLI-subprocess invocation path instead of the in-process `run_workflow`; the *decisions* — manual command, no enqueue, route unchanged, `generation_failed` + `quizzes.generation_error` column, route-level HTTP-409 first-Quiz guard, status-flip = the "notified" mechanism for this slice — are unchanged; `Status` stays `Proposed`). `design_docs/architecture.md` (ADR-036 row moved from "Pending resolution (need human input)" to "Proposed ADRs (awaiting human acceptance)" with the revised title; ADR-037 row text amended; "Pending resolution" table reset to "(none)" with a note pointing at ADR-036's new home; the project-structure summary is unchanged — no ADR state changed to `Accepted` this cycle, so per the maintenance protocol the summary stays as it was through ADR-033/034/035; it will be regenerated after the human gates ADR-036/037, adding an "AI integration / Quiz generation" paragraph). `design_docs/audit/TASK-014-...md` (the Run-003 "Update (2026-05-12)" note + this Run 004 entry; header Status/phase unchanged — `Blocked` / `design`, still correct: two `Proposed` ADRs await the human's Acceptance gate).
**ADRs proposed:** `ADR-036` — revised: `ai-workflows` integration — depend on `jmdl-ai-workflows >= 0.4` as a normal PyPI pin in `pyproject.toml` `[project] dependencies` (the human's resolution; the co-located clone is read-only reference only; `requires-python` → `>= 3.12` — both `pyproject.toml` edits the implementer's); CS-300-owned `WorkflowSpec` module `app/workflows/question_gen.py` (declarative surface — one `LLMStep` with `prompt_fn` + the required `response_format=QuestionGenOutput` validator; `question_gen_tier_registry()` with one LLM tier (`LiteLLMRoute(model="gemini/gemini-2.5-flash")` or `ClaudeCodeRoute` — provider/model is a tuning decision); `QuestionGenInput(section_content, section_title)` — no learner-controlled `num_questions` this slice; `QuestionGenOutput.questions: list[GeneratedQuestion]` where `GeneratedQuestion` = `prompt: str` + `topics: list[str]`, no choice/recall/describe/`choices`/`correct_choice`/`answer_text` field anywhere — a non-coding Question unexpressible, mirroring ADR-033's `questions` table); the out-of-band processor invokes the workflow by **shelling out to `aiw run question_gen --input section_content=... --input section_title=... --run-id quiz-{quiz_id}`** as a subprocess with `AIW_EXTRA_WORKFLOW_MODULES=app.workflows.question_gen` in the env (the framework's documented downstream surface — `docs/writing-a-workflow.md` models exactly this with a `question_gen` worked example), reading the terminal artefact from stdout (`json.dumps(artifact, indent=2)` then a `total cost: $X.XXXX` trailer; logs on stderr) and treating a non-zero exit / `error: ...` on stderr as failure — **not** the in-process `ai_workflows.workflows._dispatch.run_workflow` (a `_`-prefixed private internal not under the semver contract — rejected, trade-off named), **not** an `aiw-mcp` HTTP hop (rejected); orchestration boundary unchanged (the workflow returns data; the CS-300 processor persists via `app.persistence.*` — new `mark_quiz_*` / `add_questions_to_quiz` / `list_requested_quizzes` / `get_quiz` in `app/persistence/quizzes.py`, SQL stays there, MC-10); `ai-workflows` persistence cohabitation = option (c) (the framework owns `~/.ai-workflows/*.sqlite3`; no `ai-workflows` tables in `data/notes.db`) — unchanged; test seam = **CS-300's own processor boundary** (mock the `_invoke_question_gen` / `subprocess.run` call to return a canned artefact / raise a failure; an optional `register_workflow`-succeeded sanity test; the framework's in-process `StubLLMAdapter` monkeypatch is *not usable across a subprocess* — stated as the consequence of choosing the documented CLI surface; the real-engine run is a human-only verification gate; no real LLM/provider call in any test; the MC-1 boundary grep); forbidden-SDK list enumerated (`openai`/`anthropic`/`google.generativeai`/`google.genai`/`cohere`/`mistralai`/`groq`/`together`/`replicate`/`litellm`/`langchain*`/`langgraph*`/a raw provider-API HTTP call) — goes into the conformance skill's MC-1 entry in the same commit that Accepts the ADR (the human's edit; activates MC-1's architecture portion); Topic-vocabulary relational form deferred again to the Weak-Topic-identification slice (honoring ADR-033 §Topic tags — no new ADR, no project_issue). `ADR-037` — revised references only: invocation-detail references throughout updated to ADR-036's `aiw run` CLI-subprocess path; **all decisions unchanged** (out-of-band manual command `python -m app.workflows.process_quiz_requests`; `POST .../quiz` route unchanged, no enqueue; failure → `generation_failed` + the detail in a new nullable `quizzes.generation_error TEXT` column, additive `ALTER TABLE ADD COLUMN`, `mark_quiz_generation_failed` gains an optional `error` param; no fabricated Questions, no silent unbounded retry — the framework's `RetryingEdge` does bounded retry inside the subprocess's workflow run; the first-Quiz-only guard rejects HTTP 409 a second `requested` request for a Section with a non-failed Quiz; the per-Section status-flip to "Ready" is the "notified" mechanism for this slice — the active Notification entity ships with the grading slice; CLAUDE.md "Commands" should gain a "Process pending Quiz requests" line — the human owns that).
**Project issues opened/resolved:** none. (The Topic-vocabulary relational-form question still has a named home — the Weak-Topic-identification slice's `/design` — per ADR-033 §Topic tags and ADR-036 §Topic tags; no project_issue is filed. `tooling-lint-and-type-check.md` stays Open — not resolved by TASK-014; re-flagged for the human, not a TASK-014 deliverable, as in Runs 001/002.)
**architecture.md changes:** ADR-036 row moved from "Pending resolution (need human input)" to "Proposed ADRs (awaiting human acceptance)" with the revised title (PyPI pin; `aiw run` CLI-subprocess invocation; CS-300-processor-boundary test seam; no choice/recall/describe field in the output schema; persistence option (c); MC-1 activation note); ADR-037 row text amended (note that the processor invokes the workflow via ADR-036's `aiw run` CLI subprocess; decisions otherwise unchanged); "Pending resolution" table reset to "(none)" with a note pointing at ADR-036's new home. No project-structure-summary change this cycle (no ADR state changed to `Accepted` — per the maintenance protocol the summary is regenerated only when the Accepted-ADR set changes).
**Write-path check:** clean. All writes were under `design_docs/{decisions,architecture.md,audit}/**` — within the Mode-2 allowed set (`design_docs/{tasks,audit,decisions,project_issues}/**`). No scratch/test/source file was created; nothing under `app/`, `tests/`, `content/latex/`, `pyproject.toml`, `CLAUDE.md`, `MANIFEST.md`, or a skill file was touched; `/home/papa-jochy/prj/ai-workflows` was read-only (re-investigated as Tier-4 reference for the revised integration shape, never written, and explicitly NOT wired in as a path/editable dependency — per the human's resolution it stays read-only reference, CS-300 depends on the PyPI package).
**Architecture leaks found:** none. Every `.md` in the input set classified correctly per the CLAUDE.md tier table. `architecture.md` stays index-only (this cycle moves a row between the Proposed / Pending tables and amends two row texts — no new architectural claim introduced into `architecture.md`; the project-structure summary is unchanged). CLAUDE.md is process-and-conventions only (the `<project lint command>` / `<project type-check command>` placeholders are a known project-setup gap, not a leak; the inline ADR-016 citation is correct). The conformance skill correctly reflects MC-1's architecture portion as still dormant (`cannot evaluate (ADR pending)`) — it becomes a `blocker` and the skill's MC-1 entry is updated only when the human Accepts ADR-036, in the same commit (the human's edit). The `ai-workflows` framework's own docs (`docs/writing-a-workflow.md`, `docs/architecture.md`) are Tier-4 reference for *this* project (they document a different repo's architecture) — read as evidence for the integration shape, not treated as authority over CS-300. ADR-033 and ADR-034 carry no leak — consumed unchanged; no genuine schema flaw found, so no supersedure proposed.
**Pushback raised:** none as `MANIFEST TENSION:` / `ARCHITECTURE FLAW:` / `ARCHITECTURE LEAK:`. No `> NEEDS HUMAN:` this run — the prior one (the dependency mechanism) has been resolved by the human and folded into ADR-036's Decision; no `Pending Resolution` portion remains. The invocation-path call (the documented `aiw run` CLI subprocess over the in-process private `_dispatch.run_workflow`) and the test-seam call (CS-300's own processor boundary over the framework's in-process `StubLLMAdapter`) are recorded in ADR-036 §My-recommendation-vs-user's-preference as *aligned with the human's directive* (which explicitly asked the architect to reconsider the `_dispatch` choice, make the call, name the trade-off, and put the rejected option in "Alternatives considered" — done: Alternative D rejects the in-process path with the trade-off named; Alternative G explains why `StubLLMAdapter` isn't usable here; §The test seam states the consequence — CS-300 tests don't exercise the real framework dispatch path, which is the human-only gate's job).
**Implementation blocked pending human acceptance:** **partially — the `> NEEDS HUMAN` is resolved; only the normal Acceptance gate remains.** ADR-036 is now `Proposed` (no `Pending Resolution` portion); ADR-037 is `Proposed`. Implementation is gated by: (1) the human reviewing + Accepting ADR-036 and ADR-037 (`Status: Proposed` → `Accepted`); (2) the human updating the conformance skill's MC-1 entry in the same commit that Accepts ADR-036 (enumerate the forbidden-SDK list; name `app/workflows/`); (3) the implementer's `pyproject.toml` edits (the `jmdl-ai-workflows>=0.4` dependency line + `requires-python >= 3.12`) — made during the implementation phase per ADR-036; (4) optionally the human adding the `python -m app.workflows.process_quiz_requests` line to CLAUDE.md "Commands" (ADR-037's flag); (5) the human/orchestrator moving both ADR rows in `architecture.md` from "Proposed ADRs" to "Accepted ADRs" and regenerating the project-structure summary on Acceptance; (6) the Human-gates rows for the ADR acceptances. Then `/implement TASK-014` (or re-run `/auto`, which resumes from Phase 3 once the ADRs are Accepted). No subagent failed to produce an expected output this run (the architect is the only agent that ran).
**Output summary:** `/design TASK-014` resumed after the `> NEEDS HUMAN` resolution — ADR-036 revised (dependency mechanism = PyPI pin per the human's resolution; invocation path = the documented `aiw run` CLI subprocess, replacing the in-process `_dispatch.run_workflow`; test seam = CS-300's own processor boundary) and `Status: Pending Resolution` → `Proposed`; ADR-037 revised (invocation-detail references updated; decisions unchanged), `Status` stays `Proposed`; `architecture.md` updated mechanically; no project_issue opened/resolved; no `> NEEDS HUMAN`; implementation unblocked of the escalation, awaiting only the normal human Acceptance gate (+ the human's MC-1 conformance-skill edit and the implementer's `pyproject.toml` edits).

### Run 005 — orchestrator: ADR acceptance + human-owned-edit remediation (`/auto` resumption, Phase 2 close-out)

**Time:** 2026-05-12T01:30:00Z
**Context:** `/auto` resumed after the human (1) resolved the `> NEEDS HUMAN` on ADR-036's dependency mechanism (= a PyPI version pin), (2) reviewed the architect-revised ADR-036/037 (Run 004), and (3) directed acceptance — "accept both ADRs as-is and let `/auto` continue" (chose not to adjust the `aiw run`-CLI-subprocess invocation path), and approved the conformance-skill MC-1 update and the CLAUDE.md "Commands" line. The orchestrator performed the Phase-2 close-out + the human-owned edits the ADRs flag (the human's standing approval for these specific edits is recorded in the Human-gates table above).
**Files modified:**
- `design_docs/decisions/ADR-036-ai-workflows-integration.md` — `Status: Proposed` → `Accepted`; acceptance note added (human resolved the `> NEEDS HUMAN`, reviewed the revision, directed acceptance during the `/auto` resumption).
- `design_docs/decisions/ADR-037-async-quiz-generation-processing.md` — `Status: Proposed` → `Accepted`; acceptance note added.
- `.claude/skills/manifest-conformance/SKILL.md` — **MC-1 entry updated** (the human's edit, per ADR-036's flag and the human's "4. yes" approval): Trace now cites ADR-036 (Accepted 2026-05-12); the architecture portion is now **active** — enumerates the forbidden-SDK set (`openai`/`anthropic`/`google.generativeai`/`google.genai`/`cohere`/`mistralai`/`groq`/`together`/`replicate`/`litellm`/`langchain*`/`langgraph*`/a raw provider-API HTTP call), states the only AI import in `app/` is `ai_workflows.*` with invocation via the `aiw` CLI subprocess, and names the workflow path `app/workflows/` (the question-gen workflow at `app/workflows/question_gen.py`); severity is **blocker** for both the manifest portion and the package-name/module-path check. MC-10's pre-existing wording (still says "pending `/design`") was left untouched — out of scope for this task.
- `CLAUDE.md` — "Commands" section gained a line: `- Process pending Quiz requests (out-of-band Quiz-generation processor, ADR-037): \`python -m app.workflows.process_quiz_requests\`` (the human's edit, per ADR-037's flag and the human's "5. fine" approval). The `<project lint command>` / `<project type-check command>` placeholders were left untouched.
- `design_docs/architecture.md` — ADR-036 and ADR-037 rows moved from "Proposed ADRs (awaiting human acceptance)" to "Accepted ADRs"; the leftover "Pending resolution" note reset to "(none)"; the project-structure-summary derivation line updated to include 036/037 (and note ADR-032 `Proposed`/deferred); the "Quiz domain schema" paragraph updated for the new nullable `quizzes.generation_error` column; the "Per-Section Quiz surface" paragraph updated for ADR-037's first-Quiz-only HTTP-409 guard and the four status labels; a new "AI integration and Quiz generation" paragraph added summarising ADR-036 + ADR-037. Mechanical regeneration only — no new architectural claim beyond what the now-Accepted ADRs decided.
- `design_docs/audit/TASK-014-...md` — header `Status` → `In progress`, `Current phase` → `test`; Human-gates rows appended for the `> NEEDS HUMAN` resolution + ADR-036/037 acceptance; this Run 005 entry.
**Not touched:** `pyproject.toml` (the `jmdl-ai-workflows>=0.4` dependency line + `requires-python >= 3.12` bump are the implementer's edit, made in Phase 4 per ADR-036); `app/**`, `tests/**`, `content/latex/**` (Phase 3/4).
**Verification of architect Run 004 outputs:** confirmed via inspection — ADR-036 `Status` was `Proposed` (now flipped to `Accepted`), title rewritten, revision note present; ADR-037 `Status` was `Proposed` (now `Accepted`), revision note present, decisions unchanged; architecture.md ADR-036 row was in "Proposed ADRs", ADR-037 row amended, "Pending resolution" reset. No gap; no remediation of a missing architect output needed.
**Result:** all TASK-014 ADRs (ADR-036, ADR-037) are `Accepted`; MC-1's architecture portion is active; `architecture.md` reflects the Accepted set; CLAUDE.md documents the processor command. Implementation is unblocked. Proceeding to Phase 3 (test-writer).

### Run 006 — test-writer (`/implement` Phase 3)

**Time:** 2026-05-12T00:00:00Z
**Input files read:** `design_docs/tasks/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md` (two ranges: offset 0–100 + offset 33–20); `design_docs/decisions/ADR-036-ai-workflows-integration.md` (with limit 150); `design_docs/decisions/ADR-037-async-quiz-generation-processing.md` (full — via context); `.claude/skills/manifest-conformance/SKILL.md` (full — via context); `CLAUDE.md` (via context); `tests/conftest.py` (full); `tests/playwright/conftest.py` (full); `tests/playwright/test_task013_quiz_surface_dom.py` (partial — first 160 lines, conventions); `tests/test_task013_quiz_schema.py` (conventions); `tests/test_task013_quiz_surface_http.py` (conventions); `app/persistence/__init__.py` (public API surface); `app/persistence/quizzes.py` (first 60 lines, schema/docstring); `app/main.py` (first 50 lines, route signatures); `pyproject.toml` (markers, testpaths, addopts); `design_docs/audit/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md` (Runs 001–005 context); `design_docs/MANIFEST.md` (§4/§5/§6/§7/§8 binding clauses — via context).
**Tools / commands used:** Read (files listed above); Bash (`python3 -m pytest tests/test_task014_quiz_generation.py -v --tb=short`; `python3 -c "import ast; ast.parse(…)"` syntax checks; grep/python debugging of f-string and take-button false-positive issues); Write (`tests/test_task014_quiz_generation.py`, `tests/playwright/test_task014_quiz_generation_dom.py`); Bash (inline python3 for fixing f-string ternary syntax errors and the stale assert block).
**Files created:**
- `tests/test_task014_quiz_generation.py` (main pytest file — 34 tests covering AC-1 through AC-14 plus boundary/edge/negative/performance)
- `tests/playwright/test_task014_quiz_generation_dom.py` (Playwright DOM tests — 11 tests for rendered surface states)
**Files modified:** `pyproject.toml` — not modified (marker `task(id)` already registered); `tests/conftest.py` — not modified.
**Tests added:**
- `test_processor_happy_path_requested_to_ready` → AC-1 (lifecycle walk, questions + quiz_questions persisted)
- `test_processor_happy_path_questions_have_nonempty_prompt` → AC-1 (boundary: every prompt non-empty)
- `test_processor_happy_path_quiz_questions_position_set` → AC-1 (boundary: position ≥1)
- `test_processor_happy_path_questions_section_id_matches` → AC-1, AC-6 (MC-2: section_id matches)
- `test_processor_happy_path_surface_shows_ready` → AC-1 (rendered surface: "Ready" after processor)
- `test_mc1_no_forbidden_lm_sdk_import_in_app` → AC-2 (MC-1 grep: forbidden SDKs absent)
- `test_route_returns_303_immediately_quiz_still_requested` → AC-3 (MC-4: no subprocess in request handler)
- `test_processor_failure_path_quiz_status_generation_failed` → AC-4 (MC-5: failure → generation_failed)
- `test_processor_failure_path_zero_questions_persisted` → AC-4 (MC-5: no fabricated Questions)
- `test_processor_failure_path_generation_error_persisted` → AC-4 (ADR-037: generation_error populated)
- `test_processor_failure_path_surface_shows_generation_failed` → AC-4 (rendered surface: "Generation failed")
- `test_processor_does_not_reprocess_generation_failed_rows` → AC-4 (MC-5: no silent retry)
- `test_question_gen_output_schema_has_prompt_and_topics_no_choices` → AC-5 (QuestionGenOutput/GeneratedQuestion schema)
- `test_question_gen_output_schema_on_quizzes_table` → AC-5 (questions table has no non-coding columns)
- `test_no_cross_section_questions_in_quiz` → AC-6 (MC-2: no cross-section composition)
- `test_first_quiz_guard_boundary_ready_blocks_second` → AC-7 (MC-8: 'ready' → 409)
- `test_first_quiz_guard_boundary_generating_blocks_second` → AC-7 (MC-8: 'generating' → 409)
- `test_first_quiz_guard_requested_blocks_second` → AC-7 (MC-8: 'requested' → 409)
- `test_first_quiz_guard_boundary_generation_failed_allows_retry` → AC-7 (MC-8: 'generation_failed' allows retry)
- `test_processor_module_exists_as_main_module` → AC-8 (MC-9: processor module exists at correct path)
- `test_schema_generation_error_column_exists` → AC-9 (ADR-037: generation_error column added)
- `test_no_user_id_on_generation_error_column` → AC-9 (MC-7: no user_id)
- `test_generation_error_column_is_nullable_for_nonfailed_quizzes` → AC-9 (ADR-037: nullable column is NULL for ready)
- `test_mc10_no_sqlite3_import_in_workflows` → AC-10 (MC-10: no sqlite3 outside persistence)
- `test_mc10_no_sql_literals_in_workflows` → AC-10 (MC-10: no SQL literals outside persistence)
- `test_surface_shows_generating_status` → AC-11 (rendered surface: "Generating…")
- `test_surface_ready_has_no_takeable_affordance` → AC-11 (ADR-034: no take-button for 'ready')
- `test_surface_mandatory_and_optional_both_render_statuses` → AC-11 (MC-3: M+O both render)
- `test_no_notifications_table_in_schema` → AC-12 (ADR-037: no notifications table this task)
- `test_no_fabricated_notification_on_non_ready_quiz` → AC-12 (MC-5: no stand-in notification)
- `test_adr036_exists_and_is_accepted` → AC-14 (ADR-036 exists and is Accepted)
- `test_adr037_exists_and_is_accepted` → AC-14 (ADR-037 exists and is Accepted)
- `test_processor_failure_empty_artefact_treated_as_failure` → Edge (empty questions list = failure)
- `test_processor_happy_path_with_multiple_questions_within_budget` → Performance (5 questions within 5s)
- Playwright: `test_section_quiz_block_visible_on_mandatory_chapter` → AC-11 (DOM: .section-quiz present on mandatory)
- Playwright: `test_section_quiz_block_inside_section_end_mandatory` → AC-11 (DOM: .section-quiz inside .section-end)
- Playwright: `test_section_quiz_block_visible_on_optional_chapter` → AC-11, MC-3 (DOM: .section-quiz on optional)
- Playwright: `test_ready_state_label_renders_as_ready_not_take_button` → AC-11 (DOM: ready label + no take-button)
- Playwright: `test_generating_state_label_renders_on_quiz_surface` → AC-11 (DOM: generating label contract)
- Playwright: `test_generation_failed_label_contract` → AC-11, MC-5 (DOM: generation_failed label contract)
- Playwright: `test_no_take_affordance_on_optional_chapter` → AC-11, MC-3 (DOM: no take-button on optional)
- Playwright: `test_generate_quiz_form_still_present_mandatory` → AC-11 (DOM: generate form not regressed)
- Playwright: `test_generate_quiz_form_still_present_optional` → AC-11, MC-3 (DOM: generate form on optional)
- Playwright: `test_post_quiz_redirect_preserves_section_anchor` → AC-3, ADR-031 (DOM: anchor preserved in redirect)
- Playwright: `test_no_layout_regression_mandatory_chapter` → AC-11 (DOM: no layout regression, mandatory)
- Playwright: `test_no_layout_regression_optional_chapter` → AC-11, MC-3 (DOM: no layout regression, optional)

**Coverage matrix:**
- Boundary: `test_processor_happy_path_requested_to_ready` (full lifecycle); `test_processor_happy_path_questions_have_nonempty_prompt` (≥1 item); `test_processor_happy_path_quiz_questions_position_set` (position ≥1); `test_first_quiz_guard_boundary_ready_blocks_second` (exact guard flip at 'ready'); `test_first_quiz_guard_boundary_generating_blocks_second` ('generating'); `test_first_quiz_guard_requested_blocks_second` ('requested'); `test_first_quiz_guard_boundary_generation_failed_allows_retry` (guard does NOT count 'generation_failed').
- Edge: `test_processor_failure_empty_artefact_treated_as_failure` (empty questions list = failure, not ready); `test_processor_does_not_reprocess_generation_failed_rows` (second run with success mock leaves generation_failed unchanged); `test_generation_error_column_is_nullable_for_nonfailed_quizzes` (nullable column is NULL for ready); `test_no_user_id_on_generation_error_column` (no user_id after schema addition); `test_schema_generation_error_column_exists` (additive column present after bootstrap); `test_question_gen_output_schema_has_prompt_and_topics_no_choices` (schema shape: prompt+topics, no choice/recall/describe).
- Negative: `test_processor_failure_path_zero_questions_persisted` (no fabricated Question on failure — MC-5); `test_processor_failure_path_quiz_status_generation_failed` (status → generation_failed on failure — MC-5); `test_processor_failure_path_generation_error_persisted` (generation_error populated); `test_route_returns_303_immediately_quiz_still_requested` (no subprocess during POST request — MC-4); `test_mc1_no_forbidden_lm_sdk_import_in_app` (no forbidden SDK imports — MC-1); `test_mc10_no_sqlite3_import_in_workflows` (no sqlite3 outside persistence — MC-10); `test_mc10_no_sql_literals_in_workflows` (no SQL literals outside persistence — MC-10); `test_no_user_id_on_generation_error_column` (no user_id — MC-7).
- Performance: `test_processor_happy_path_with_multiple_questions_within_budget` (5 questions within 5s wall-clock; catches O(n²) per-question connection reopening or SELECT-per-insert loops).

**Pytest red result:** Collected: 34, Failing: 21, Passing: 13. Failures are all for the right reason: `ModuleNotFoundError: No module named 'app.workflows'` (processor and workflow module don't exist yet); `AssertionError: The quizzes table has no generation_error column` (ADR-037 column not added yet); `AssertionError: Second POST … returned 303; expected 409` (first-Quiz-only guard not implemented); `ImportError` on `QuestionGenOutput`/`GeneratedQuestion`; `AssertionError: processor module not found at app/workflows/process_quiz_requests.py`. The 13 passing tests verify existing shipped behavior: no forbidden SDK imports (app code doesn't exist yet), route returns 303 (TASK-013 shipped this), questions table has no non-coding columns (TASK-013 schema), no user_id on quizzes (TASK-013 schema), no sqlite3 outside persistence (app/workflows/ doesn't exist yet), surface shows status labels (TASK-013 template already handles all four status strings), no notifications table (correct), no fabricated notification (correct), ADR-036/ADR-037 are Accepted (correct). Playwright tests not run in this pass (require live_server + Playwright browser install — run separately with `python3 -m pytest tests/playwright/test_task014_quiz_generation_dom.py`).
**Assumptions:**
- ASSUMPTION: The processor module lives at `app.workflows.process_quiz_requests` (ADR-037 §Decision) with a callable entry point (`process_pending()` or `main()` or `run()`).
- ASSUMPTION: `app.persistence` exports TASK-014 persistence functions: `mark_quiz_generating`, `mark_quiz_ready`, `mark_quiz_generation_failed`, `add_questions_to_quiz`, `list_requested_quizzes`.
- ASSUMPTION: `app.workflows.question_gen` exports `QuestionGenOutput` (pydantic model, first field `questions: list[GeneratedQuestion]`) and `GeneratedQuestion` (fields `prompt: str`, `topics: list[str]`, no choice/recall/describe).
- ASSUMPTION: The thin helper the processor uses to invoke the workflow is named `_invoke_question_gen` or similar, and the subprocess seam is `subprocess.run` in the `app.workflows` namespace.
- ASSUMPTION: ADR-034 §CSS: `.section-quiz-item--ready` / `.section-quiz-item--generating` / `.section-quiz-item--generation_failed` are the CSS modifier classes (or the text "Ready" / "Generating…" / "Generation failed" appears verbatim in the block). Tests fall back to text matching.
**CANNOT TEST:** AC-13 (conformance checks) — not a standalone AC; covered by AC-2 (MC-1 grep) and AC-10 (MC-10 grep). No other `CANNOT TEST` items.
**Architecture leaks found:** none during the critique pass on all input `.md` files.
**Pushback raised:** none. All 14 programmatic ACs translated to tests. No weak-AC, human-only-AC, or ADR-flaw conditions triggered.

---

### Run 007 — implementer

**Time:** 2026-05-12T00:00:00Z
**Input files read:** `design_docs/tasks/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md` (AC section + out-of-scope section); `design_docs/decisions/ADR-036-ai-workflows-integration.md` (full); `design_docs/decisions/ADR-037-async-quiz-generation-processing.md` (full); `design_docs/decisions/ADR-033-quiz-domain-schema.md` (referenced via task); `design_docs/MANIFEST.md`; `CLAUDE.md`; `design_docs/architecture.md`; `.claude/skills/manifest-conformance/SKILL.md`; `tests/test_task014_quiz_generation.py` (full, in parts); `tests/conftest.py`; `app/persistence/quizzes.py`; `app/persistence/__init__.py`; `app/persistence/connection.py`; `app/main.py`; `app/templates/lecture.html.j2` (quiz section); `pyproject.toml`; `design_docs/audit/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md`.
**Tools / commands used:** Read, Edit, Write, Bash (pytest, grep, python3 import checks).
**Files created:** `app/workflows/__init__.py`; `app/workflows/question_gen.py`; `app/workflows/process_quiz_requests.py`.
**Files modified:** `pyproject.toml` (added `jmdl-ai-workflows>=0.4` dep; bumped `requires-python` to `>=3.12` per ADR-036); `app/persistence/connection.py` (added `_apply_additive_migrations()` helper for the `quizzes.generation_error` column + call in `get_connection()` per ADR-037); `app/persistence/quizzes.py` (added 7 new public functions: `mark_quiz_generating`, `mark_quiz_ready`, `mark_quiz_generation_failed`, `add_questions_to_quiz`, `list_requested_quizzes`, `get_quiz`, `section_has_nonfailed_quiz`); `app/persistence/__init__.py` (re-exported all 7 new functions + updated docstring); `app/main.py` (imported `section_has_nonfailed_quiz`; added first-Quiz-only HTTP 409 guard to `request_quiz_route` per ADR-037); `design_docs/audit/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md` (header Status/phase update + this Run entry).
**Files explicitly NOT modified:** `tests/**/test_*.py`, `design_docs/MANIFEST.md`, `design_docs/architecture.md`, `design_docs/decisions/**`, `CLAUDE.md`, `.claude/skills/manifest-conformance/SKILL.md`.
**Implementation decisions made:**
- `TierConfig` requires `name` and `route` kwargs (not `routes` list) — discovered at import time; corrected immediately.
- Docstrings in `app/workflows/` initially contained the literal string `import sqlite3` (in MC-10 compliance notes) which caused the `test_mc10_no_sqlite3_import_in_workflows` grep test to flag them as violations. Reworded to `no DB driver imports` — smallest fix that preserves meaning while satisfying the test's plain-text search. This is an implementation-local convention choice, not architecture.
- `_invoke_question_gen` uses `"aiw"` CLI (not `sys.executable -m ai_workflows.cli`) — ADR-036 notes both as equivalent; `aiw` is the documented surface.
- `process_pending()` is the entry-point name (the test's `_run_processor` helper tries `process_pending` first, then `main`, then `run` — choosing `process_pending` satisfies the first branch).
- `_get_section_content` falls back to returning the full latex text if the section is not found by ID (defensive; the Section parse path returns sections with an `id` field from `extract_sections`).
**Tests run:** `python3 -m pytest tests/test_task014_quiz_generation.py -q` → 34/34 passed. `python3 -m pytest tests/ --ignore=tests/playwright -q` → 775/775 passed (no regressions). `python3 -m pytest tests/playwright/test_task014_quiz_generation_dom.py -q` → 12/12 passed.
**Lint / type-check:** No lint/type-check command configured in CLAUDE.md (open project_issue `tooling-lint-and-type-check.md`). Manual grep checks: no forbidden SDK imports, no sqlite3 outside persistence, no SQL literals in workflows, no user_id in new code.
**Conformance result:** MC-1 PASS (no forbidden SDK imports in app/); MC-2 PASS (add_questions_to_quiz takes section_id from the Quiz row — exactly one Section); MC-3 PASS (template unchanged; renders on both M/O Chapters); MC-4 PASS (process_quiz_requests.py is never called from a request handler; route returns 303 before any subprocess); MC-5 PASS (generation_failed on failure; zero questions on failure path; generation_error persisted; no reprocessing of failed rows); MC-6 PASS (reads content/latex/ read-only via app.parser; no writes); MC-7 PASS (no user_id on any new column); MC-8 PASS (HTTP 409 guard blocks second request for non-failed Quiz; generation_failed does not count); MC-9 PASS (processor only calls list_requested_quizzes; never inserts a requested row); MC-10 PASS (import sqlite3 and SQL literals only in app/persistence/; workflows use typed public functions). 0 blockers, 0 warnings, 0 dormant.
**End-to-end verification:** Ran `TestClient` against the app: GET /lecture/ch-01 → 200; first POST /lecture/ch-01/.../1-1/quiz → 303; second POST → 409. Schema verified: `quizzes` has `generation_error` column, no `user_id`. `app.workflows.question_gen` imports cleanly, `QuestionGenOutput.model_fields` has `questions` as first field, `GeneratedQuestion.model_fields` has `prompt` and `topics` only. 775/775 non-Playwright tests pass. 12/12 Playwright DOM tests pass.
**Adjacent bugs surfaced (not fixed):** None found in the affected code paths.
**Pushback raised:** None.

### Run 008 — verify (orchestrator) (`/implement` Phase 3 / `/auto` Phase 5)

**Time:** 2026-05-12T03:30:00Z
**Commands run:**
- `python3 -m pytest tests/` (full suite, incl. Playwright) → **982 passed, 1 warning, 0 failed** (384s). Pre-implementation baseline was 754 passing + 21 failing (non-Playwright); post-implementation the 21 task-014 tests are green and no previously-passing test regressed. (`python3` here is system 3.12; `jmdl-ai-workflows` 0.4.0 is importable — currently editable-installed pointing at `/home/papa-jochy/prj/ai-workflows`; the `aiw` console script is on PATH. No real `aiw`/LLM call fires in any test — the test seam mocks `subprocess.run`.)
- Lint / type-check: **not configured** — CLAUDE.md "Commands" has `<project lint command>` / `<project type-check command>` placeholders (the long-standing project-setup gap tracked in `design_docs/project_issues/tooling-lint-and-type-check.md`, now in its ninth recurrence — re-flagged for the human; not a TASK-014 deliverable).
- **Conformance walk** (`.claude/skills/manifest-conformance/SKILL.md` against the working tree): **PASS, 0 blockers.** MC-1 (active per ADR-036): no forbidden LLM/agent SDK import anywhere in `app/` — the only AI import is `ai_workflows.*`, confined to `app/workflows/`; AI invocation is the `aiw` CLI subprocess (not `_dispatch.run_workflow`) — PASS. MC-2 (Quiz scope = one Section): `add_questions_to_quiz` / the workflow operate on exactly one `section_id`; no cross-Section composition — PASS. MC-3 (M/O inheritance): unchanged; the per-Section Quiz surface inherits the Chapter designation — PASS. MC-4 (AI async): no AI processing inside a request handler — `POST .../quiz` returns 303 immediately with the Quiz still `requested`; processing is the out-of-band `python -m app.workflows.process_quiz_requests` command — PASS. MC-5 (AI failures surfaced, never fabricated): on failure the Quiz → `generation_failed`, no `questions` synthesized, the detail in `quizzes.generation_error`, no silent unbounded retry (the processor does not re-process `generation_failed` rows) — PASS. MC-6 (read-only `content/latex/`): the processor reads a Section's `.tex` via the existing parse path, never writes under `content/latex/` — PASS. MC-7 (single user): no `user_id` on the new column / no auth / no per-user partitioning (the grep hits are compliance comments) — PASS. MC-8 (loop preserved): this slice ships only the manifest-mandated fresh-only *first* Quiz; the route-level HTTP-409 first-Quiz-only guard rejects a second `requested`/`generating`/`ready` Quiz for a Section (a `generation_failed` Quiz does not count) so a forbidden fresh-only post-first Quiz cannot be produced; the composition slice is later — PASS. MC-9 (user-triggered): the processor processes existing user-created `requested` rows, never creates one; nothing scheduled/auto-triggers a Quiz — PASS. MC-10 (persistence boundary): `import sqlite3` / SQL string literals only under `app/persistence/` — `app/workflows/` calls only the typed `app.persistence.*` functions — PASS.
- **End-to-end verification** (dev server `uvicorn app.main:app` on a throwaway `NOTES_DB_PATH`): `GET /` → 200; `GET /lecture/ch-01-cpp-refresher` → 200, `section-quiz` blocks present, "No quizzes yet" empty-state present; `POST /lecture/ch-01-cpp-refresher/sections/1-1/quiz` → **303** → `…/lecture/ch-01-cpp-refresher#section-1-1-end` (ADR-031 recipe reused); `POST` again → **409** (first-Quiz-only guard — an existing `requested` Quiz counts); `GET /lecture/ch-01-cpp-refresher` again → the per-Section surface shows "Requested — generation pending" for the new Quiz; `python -c "import app.workflows.process_quiz_requests"` → `process_pending` callable; `python -m app.workflows.process_quiz_requests` against a fresh empty DB → "No requested quizzes to process.", exit 0. **Not run by the orchestrator:** the real `aiw run` engine path (a real LLM call) — that is the human-only `ai-workflows generation sanity` verification gate (filed `pending human`); the *visual* review of the rendered Quiz surface in `generating`/`ready`/`generation_failed` — that is the human-only `rendered-surface verification` gate (filed `pending human`); the Playwright DOM tests cover the *structure* of those states (12 task-014 DOM tests green) but a browser was not opened for a visual pass.
**Pending-human gates filed at this phase** (2 — see the Human-gates table above): (1) `rendered-surface verification — pass (TASK-014 Quiz-generation surface states)` — visual review of the four surface states + no layout regression; (2) `ai-workflows generation sanity — pass (TASK-014 first-Quiz fresh generation)` — one real-engine generation against the actual `ai-workflows` engine, judged by the human.
**Adjacent finding (not a blocker, flagged for the human):** the dev `.venv`/system-`python3` environment currently has `jmdl-ai-workflows` 0.4.0 **editable-installed pointing at `/home/papa-jochy/prj/ai-workflows`** (the co-located clone). The `pyproject.toml` declaration the implementer added is the PyPI form (`jmdl-ai-workflows>=0.4`) per the human's resolution. To make the *installed environment* match the declaration the human would `pip install --force-reinstall jmdl-ai-workflows` (from PyPI) — and should sanity-check that PyPI's published 0.4.0 carries the M19+ declarative-authoring surface (`WorkflowSpec` / `register_workflow` / the five built-in `Step` types) the workflow module uses (the clone has M19-M21 marked complete; the README's "0.4.0 at M17 close" line predates them). If PyPI's 0.4.0 lacks M19, the pin needs raising. Not a TASK-014 code defect — the code + tests are correct against the framework's documented API; this is an environment/pin-precision note for the human at commit time.
**Result:** all programmatic ACs green (982/982 tests pass); conformance walk clean (0 blockers); end-to-end behaviors confirmed; 2 human-only verification gates filed `pending human`. Proceeding to Phase 6 (stage + reviewer + commit).

### Run 009 — reviewer

**Time:** 2026-05-12T05:00:00Z
**Staged files reviewed:** `.claude/skills/manifest-conformance/SKILL.md`, `CLAUDE.md`, `app/main.py`, `app/persistence/__init__.py`, `app/persistence/connection.py`, `app/persistence/quizzes.py`, `app/workflows/__init__.py`, `app/workflows/process_quiz_requests.py`, `app/workflows/question_gen.py`, `design_docs/architecture.md`, `design_docs/audit/TASK-014-...md`, `design_docs/decisions/ADR-036-ai-workflows-integration.md`, `design_docs/decisions/ADR-037-async-quiz-generation-processing.md`, `design_docs/tasks/TASK-014-...md`, `pyproject.toml`, `tests/playwright/test_task014_quiz_generation_dom.py`, `tests/test_task014_quiz_generation.py`.
**Unstaged source/test warning:** none — working tree clean, full set staged.
**Conformance skill result:** 0 blockers, 0 warnings, 0 dormant (MC-1 architecture portion active per ADR-036 and PASS; MC-10 active per ADR-022 and PASS; MC-2/4/5/6/7/8/9 PASS; MC-3 PASS — template unchanged).
**Architecture leaks found in .md files:** none. CLAUDE.md "Commands" line and the SKILL.md MC-1 update restate ADR-036/ADR-037 verbatim (no new claim); architecture.md regeneration is mechanical (036/037 → Accepted; "Pending resolution" → "(none)"; project-structure paragraphs trace to the now-Accepted ADRs).
**Blocking findings:** 1 — `app/workflows/process_quiz_requests.py:77` `_get_section_content` reads `section.get("html", "") or section.get("content", "")` and `section.get("title", section_id)`, but `app.parser.extract_sections` returns dicts keyed `body_html` / `heading` / `heading_text` (never `html` / `content` / `title`). For every real Section (matched by `id`) the processor therefore sends `section_content=""` (and `section_title` = the composite section_id) to `aiw run question_gen` — the workflow never receives the Section's text, so generated Questions cannot be grounded in the Section's content (violates AC-1 "fresh Questions … each with a non-empty prompt, the correct section_id" in spirit and AC-5 "implement a concept *from the Section*"; the manifest §3/§7 reinforcement-loop foundation depends on Section-grounded fresh Questions). Not caught by the suite because every test mocks `subprocess.run` and none asserts the `--input section_content=` value. Fix: read `section.get("body_html", "")` and `section.get("heading_text", section_id)` (mirror how `app/main.py` consumes `extract_sections`).
**Non-blocking findings:** (1) `app/workflows/process_quiz_requests.py` imports `discover_chapters` but never uses it (dead import). (2) `import pathlib` / `import app.config as _cfg` appear below the `from app.persistence import (...)` block rather than grouped with the other stdlib/app imports at the top — cosmetic. (3) `design_docs/architecture.md` "Per-Section Quiz surface" paragraph describes the 409 guard as firing when the Section "already has a non-failed Quiz (`ready` or `generating`; a `generation_failed` Quiz does not count)" — the code (correctly, per ADR-037 §5) also counts `requested`; the parenthetical is non-exhaustive, not wrong. (4) No test asserts the subprocess invocation shape (`aiw run question_gen --input section_content=… --run-id quiz-{id}` with `AIW_EXTRA_WORKFLOW_MODULES` in env) — adding one would have caught finding-1 and would lock ADR-036's documented-surface contract.
**Final result:** CHANGES REQUESTED

---

### Run 010 — test-writer (delta: subprocess-invocation assertion)

**Time:** 2026-05-12T00:00:00Z
**Input files read:** `design_docs/audit/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md` (Runs 001–009; esp. Run 009 reviewer blocking finding #1); `tests/test_task014_quiz_generation.py` (full, in parts — offset 1–120 module header, 120–255 helpers, 256–360 first tests; end of file at 1780+); `app/workflows/question_gen.py` (full — to confirm `QuestionGenInput` field names: `section_content: str`, `section_title: str`); `app/workflows/process_quiz_requests.py` (full — to understand the subprocess invocation shape: `_invoke_question_gen` → `subprocess.run(["aiw", "run", "question_gen", "--input", f"section_content=...", "--input", f"section_title=...", "--run-id", run_id], env={...,"AIW_EXTRA_WORKFLOW_MODULES": "app.workflows.question_gen"}, ...)`); `app/parser.py` (grep for `extract_sections`, `body_html`, `heading_text` — confirms return dict keys `id`, `fragment`, `section_number`, `heading`, `heading_text`, `body_html`); `app/main.py` (grep for `extract_sections` — confirms `body_html` / `heading_text` are the keys consumed by the app); `app/config.py` (full — for `CONTENT_ROOT`).
**Tools / commands used:** Read (files above, several in offset/limit ranges); Bash (grep for key names in parser.py and main.py; `python3 -c` inline to verify `extract_sections` output for `ch-01-cpp-refresher#section-1-1` — confirmed `body_html` non-empty, `heading_text='1.1 Arrays and Vectors (general concept)'`, keys = `['id', 'fragment', 'section_number', 'heading', 'heading_text', 'body_html']`); Bash (`cat >>` to append test); Bash (`python3 -m pytest tests/test_task014_quiz_generation.py::test_subprocess_invocation_shape_section_content_and_title -v --tb=short`); Bash (`python3 -m pytest tests/test_task014_quiz_generation.py -q --tb=line`); Bash (`python3 -m pytest tests/ --ignore=tests/playwright -q --tb=line`).
**Files created:** none (test appended to existing file).
**Files modified:** `tests/test_task014_quiz_generation.py` — appended `test_subprocess_invocation_shape_section_content_and_title` (258 lines, starting at line 1784); `design_docs/audit/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md` — this Run 010 entry.
**Tests added:**
- `test_subprocess_invocation_shape_section_content_and_title` → reviewer Run 009 blocking finding #1 (ADR-036 §How application code invokes the workflow — the subprocess invocation shape).
**Coverage matrix:**
- Boundary: the new test uses the real corpus section `ch-01-cpp-refresher#section-1-1` (the MANDATORY_FIRST_SECTION fixture already established); it verifies all five invocation-shape contracts in one test (command prefix, section_content value + non-empty + exact match, section_title value, run-id containment, env). No additional boundary variants added (the invocation shape is a fixed contract, not a ranged rule).
- Edge: the test also asserts `actual_section_content != ""` as a standalone assertion before the exact-match assertion — catching the exact current bug (empty string from wrong key lookup) separately from a hypothetical "wrong non-empty value" scenario.
- Negative: the `section_title` assertion also checks `actual_section_title != SECTION_ID_CH01_S1` (the composite section_id) as an explicit negative — catching the `section.get("title", section_id)` fallback bug where the composite id is passed as the title.
- Performance: skipped — single subprocess mock call, no scale surface.
**Pytest red result:** Collected (new test only): 1, Failing: 1, Passing: 0. Full TASK-014 file: Collected: 35, Failing: 1, Passing: 34. Full non-Playwright suite: Collected: 776, Failing: 1, Passing: 775. The 34 previously-passing task-014 tests are unaffected. The failure message is: `AssertionError: The processor passed --input section_content= with an EMPTY value to 'aiw run question_gen'. Expected the Section's body_html (first 80 chars): '<h3>The motivation: one name, many values</h3>\n<p>A regular variable holds one v'. ... Current bug: _get_section_content reads section.get('html', '') / section.get('content', '') — both keys are absent from extract_sections output; the correct key is 'body_html'.`
**Assumptions:** none beyond those already stated in Run 006.
**CANNOT TEST:** none (this test covers the single blocking finding).
**Architecture leaks found:** none.
**Pushback raised:** none. The blocking finding (wrong dict keys) is a straightforward implementation bug, not an ADR/manifest flaw. The fix is the implementer's: change `section.get("html", "") or section.get("content", "")` → `section.get("body_html", "")` and `section.get("title", section_id)` → `section.get("heading_text", section_id)` in `app/workflows/process_quiz_requests.py:_get_section_content`.

### Run 011 — implementer (fix: section-content dict keys + import cleanup)

**Time:** 2026-05-12T00:00:00Z
**Input files read:** `design_docs/MANIFEST.md`; `design_docs/architecture.md`; `.claude/skills/manifest-conformance/SKILL.md`; `CLAUDE.md`; `design_docs/tasks/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md` (partial — first 100 lines for context); `tests/test_task014_quiz_generation.py` (full, in ranges — module header + the new `test_subprocess_invocation_shape_section_content_and_title` test at offset 1796–2041); `app/workflows/process_quiz_requests.py` (full — identified the bug: lines 77–78 use wrong dict keys `"html"`/`"content"`/`"title"` instead of `"body_html"`/`"heading_text"`; also identified misplaced/unused imports); `app/parser.py` (grep for `body_html`/`heading_text` — confirmed keys); `design_docs/audit/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md` (Runs 001–010 for context).
**Tools / commands used:** Read (files listed above); Bash (grep `body_html heading_text` in `app/parser.py`); Bash (`python3 -m pytest tests/test_task014_quiz_generation.py::test_subprocess_invocation_shape_section_content_and_title -q` — confirmed 1 failing before fix); Edit (`app/workflows/process_quiz_requests.py` — two edits); Bash (`python3 -m pytest tests/test_task014_quiz_generation.py::test_subprocess_invocation_shape_section_content_and_title -q` — confirmed 1 passing after fix); Bash (`python3 -m pytest tests/ --ignore=tests/playwright -q --tb=no` — confirmed 776 passing, 0 failing).
**Files created:** none.
**Files modified:** `app/workflows/process_quiz_requests.py` — (1) dropped unused `from app.discovery import discover_chapters` import; (2) moved `import pathlib` and `import app.config as _cfg` up into the grouped stdlib/app-import block; (3) fixed `_get_section_content` lines 77–78: `section.get("html", "") or section.get("content", "")` → `section.get("body_html", "")` and `section.get("title", section_id)` → `section.get("heading_text", section_id)`. `design_docs/audit/TASK-014-quiz-generation-first-quiz-fresh-via-ai-workflows.md` — header updated (Status → Implemented, Current phase → verify); this Run 011 entry appended.
**Files explicitly NOT modified:** `tests/**/test_*.py`, `design_docs/MANIFEST.md`, `design_docs/architecture.md`, `design_docs/decisions/**`, `CLAUDE.md`, `.claude/skills/manifest-conformance/SKILL.md`.
**Implementation decisions made:** None architectural. The fix is the exact dict-key correction the delta brief specifies (`body_html` for content, `heading_text` for title, fallback to `section_id` only if `heading_text` is genuinely absent). Import order follows PEP-8 convention (stdlib before app imports).
**Tests run:** `python3 -m pytest tests/ --ignore=tests/playwright -q --tb=no` → **776 passed** (775 pre-existing + 1 new delta test now green), 0 failed, 1 warning (LangChain deprecation from a transitive dep — unrelated to this fix).
**Lint / type-check:** No project lint/type-check commands configured (open project_issue `tooling-lint-and-type-check.md`). Not run.
**Conformance result:** 0 blockers, 0 warnings, 0 dormant. MC-1: no forbidden SDK imports added. MC-4: AI processing still out-of-band only. MC-5: failure path unchanged. MC-6: `_get_section_content` reads `.tex` read-only, no write. MC-9: processor still processes existing rows only. MC-10: no `import sqlite3` or SQL literals outside `app/persistence/`; `discover_chapters` (removed) had no SQL; the fix only changes dict-key strings inside `_get_section_content`.
**End-to-end verification:** The single affected code path is `_get_section_content` inside `process_pending()`. After the fix: `section.get("body_html", "")` returns the non-empty HTML body of the Section (confirmed by the test asserting `actual_section_content == expected_body_html` where `expected_body_html` is derived by calling `extract_sections` directly); `section.get("heading_text", section_id)` returns the plain-text heading (e.g., `"1.1 Arrays and Vectors (general concept)"`), not the composite section_id fallback. The test verifies all five invocation-shape contracts (command prefix, non-empty section_content, exact match, section_title not the composite id, run-id containment, AIW_EXTRA_WORKFLOW_MODULES env). No other code path touches `_get_section_content`. The processor's success/failure paths, the first-Quiz-only guard, the empty-artefact sanity check, and all 35 TASK-014 tests are unaffected.
**Adjacent bugs surfaced (not fixed):** none found.
**Pushback raised:** none. The fix is the exact correction the delta brief + reviewer blocking finding specified. No ADR/manifest flaw was encountered.

### Run 012 — reviewer (re-review)

**Time:** 2026-05-12T07:00:00Z
**Staged files reviewed:** `.claude/skills/manifest-conformance/SKILL.md`, `CLAUDE.md`, `app/main.py`, `app/persistence/__init__.py`, `app/persistence/connection.py`, `app/persistence/quizzes.py`, `app/workflows/__init__.py`, `app/workflows/process_quiz_requests.py`, `app/workflows/question_gen.py`, `design_docs/architecture.md`, `design_docs/audit/TASK-014-...md`, `design_docs/decisions/ADR-036-ai-workflows-integration.md`, `design_docs/decisions/ADR-037-async-quiz-generation-processing.md`, `design_docs/tasks/TASK-014-...md`, `pyproject.toml`, `tests/playwright/test_task014_quiz_generation_dom.py`, `tests/test_task014_quiz_generation.py`.
**Unstaged source/test warning:** none — `git diff --name-only` empty; the full set is staged.
**Conformance skill result:** 0 blockers, 0 warnings, 0 dormant. MC-1 (architecture portion active per ADR-036 Accepted): no forbidden LLM/agent SDK import in `app/` — the only AI import is `ai_workflows.*` in `app/workflows/question_gen.py`; AI invocation is the `aiw` CLI subprocess — PASS. MC-2/3/4/5/6/7/8/9 PASS (unchanged from Run 009; the fix only corrected dict-key strings + import order in `_get_section_content`). MC-10 (active per ADR-022): `import sqlite3` / SQL literals only under `app/persistence/`; `app/workflows/` calls only the typed `app.persistence.*` functions — PASS (the dropped `discover_chapters` import carried no SQL).
**Architecture leaks found in .md files:** none. `CLAUDE.md` "Commands" line adds the ADR-037 processor command (operational, restates the ADR). The SKILL.md MC-1 update restates ADR-036's forbidden-SDK set / workflow path verbatim — no new claim. `architecture.md`'s ADR-036/037 rows + the "AI integration and Quiz generation" paragraph + the corrected "Per-Section Quiz surface" 409-guard parenthetical (now `requested` / `generating` / `ready`) all trace to the now-Accepted ADRs; the project-structure derivation line is mechanically regenerated.
**Blocking findings:** none. Run 009's blocking finding is resolved: `app/workflows/process_quiz_requests.py:_get_section_content` now reads `section.get("body_html", "")` and `section.get("heading_text", section_id)` (mirrors `app/main.py`'s consumption of `extract_sections`); the new test `tests/test_task014_quiz_generation.py::test_subprocess_invocation_shape_section_content_and_title` pins all five ADR-036 invocation-shape contracts (command prefix `["aiw","run","question_gen"]`; `--input section_content=<exact body_html>`, asserted non-empty; `--input section_title=<heading_text>`, asserted ≠ the composite section_id; `--run-id` containing the quiz_id; `env["AIW_EXTRA_WORKFLOW_MODULES"] == "app.workflows.question_gen"`) — it fails against the pre-fix code and passes post-fix. Full suite `python3 -m pytest tests/` → **983 passed, 1 warning (transitive LangChain deprecation), 0 failed** (386s). No new issue from the fix: the import reorder is PEP-8 (stdlib/app block at top); the dropped `discover_chapters` was genuinely unused; the `architecture.md` parenthetical change is a correctness improvement (Run 009 non-blocking #3). ADR fidelity re-checked — `aiw run` CLI subprocess (not `_dispatch.run_workflow`); the 409 first-Quiz-only guard counts `requested`/`generating`/`ready`; additive `ALTER TABLE quizzes ADD COLUMN generation_error TEXT` guarded by `PRAGMA table_info`; `generation_failed` on failure with no synthesized `questions` rows and no re-processing of `generation_failed` rows; `QuestionGenOutput.questions: list[GeneratedQuestion]` has only `prompt: str` + `topics: list[str]` (no choice/recall/describe/option_* field); no LLM SDK in `app/`; `import sqlite3` / SQL only under `app/persistence/`; no `user_id`; nothing from the task's `## Out of scope` crept in.
**Non-blocking findings:** none new. Run 009's non-blocking #1 (dead `discover_chapters` import) and #2 (misplaced imports) are fixed; #3 (`architecture.md` 409-guard parenthetical) is fixed; #4 (no test asserting the subprocess invocation shape) is closed by the new test. The environment/pin-precision adjacent finding from Run 008 (the dev env has `jmdl-ai-workflows` editable-installed at `/home/papa-jochy/prj/ai-workflows` rather than PyPI 0.4.0; sanity-check that PyPI 0.4.0 carries the M19+ declarative-authoring surface) stands as a commit-time note for the human — not a code defect.
**Final result:** READY TO COMMIT
