# LLM Audit — TASK-015: The Quiz-taking surface — the learner opens a `ready` Quiz, writes code against each Question, and submits an Attempt that persists (slice 3 of the Quiz pillar; no grading yet)

**Task file:** `design_docs/tasks/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md`
**Started:** 2026-05-12T00:00:00Z
**Status:** Reviewed
**Current phase:** review

---

## Human gates

| Time | Gate | Result | Notes |
|---|---|---|---|
| 2026-05-12T00:00:00Z | Task reviewed | auto-accepted | /auto run |
| 2026-05-12T02:00:00Z | ADR-038 reviewed | auto-accepted | /auto run |
| 2026-05-12T02:00:00Z | ADR-039 reviewed | auto-accepted | /auto run |
| 2026-05-12T03:00:00Z | Tests reviewed | auto-accepted | /auto run — 45 pytest tests (32 red) + 10 Playwright DOM tests |
| 2026-05-12T06:00:00Z | ADR-039 amendment (`get_latest_attempt_for_quiz`) reviewed | accepted | Human gated the implementer's in-flight accessor ("i'm fine with it") and edited ADR-039's title; architect/orchestrator folded it into ADR-039's function set + §Amendment note + architecture.md. `/auto` resumed at the verify phase. |
| 2026-05-12T07:00:00Z | rendered-surface verification (TASK-015 Quiz-taking surface) | pending human | /auto run — visual check post-commit: review `tests/playwright/.screenshots/` (or open the take page + a Lecture page in a browser) per ADR-010; confirm (a) the take page presents each Question's coding-task prompt + an obviously-code `<textarea>`, not a multiple-choice/describe surface; (b) submitting leaves the learner on the honest "Submitted — grading not yet available." state, no fabricated score; (c) the "Take this Quiz" affordance appears only on `ready` Quizzes' `.section-quiz` entries and reaches the take surface; (d) the Lecture page (three-column layout, RHS Notes rail, completion affordance, other `.section-quiz` states) is unchanged. Then edit this row `pending human` → `pass`. |
| 2026-05-12T07:00:00Z | take-and-submit sanity (TASK-015) | pending human | /auto run — manual exploratory pass post-commit: open a real `ready` Quiz (generate one via "Generate a Quiz for this Section" + `python -m app.workflows.process_quiz_requests`, or use one from the TASK-014 generation-sanity check), write code into each Question's field, submit; confirm the Attempt is recorded (`submitted` with the learner's code in each `attempt_questions.response`), the surface shows "Submitted — grading not yet available.", and nothing pretends the Quiz is graded. Then edit this row `pending human` → `pass`. |

---

## Agent runs

### Run 001 — architect / Mode 1 `/next`

**Time:** 2026-05-12T00:00:00Z
**Input files read:** `design_docs/MANIFEST.md`; `CLAUDE.md` (via the system context); `design_docs/architecture.md`; `.claude/skills/manifest-conformance/SKILL.md`; `.claude/skills/ui-task-scope/SKILL.md`; `design_docs/tasks/` (all 14 task files by glob; full read of TASK-014 §Inputs-read + §What-and-why); `design_docs/decisions/` (all 36 ADR files by glob; full read of ADR-037; partial read of ADR-034); `design_docs/project_issues/` (all 18 files by glob; the relevant ones — `tooling-lint-and-type-check.md`, `notes-save-relocates-reader.md`, the two parser-fidelity issues — already characterized in TASK-014's audit/task); `app/main.py`; `app/persistence/quizzes.py`; `app/persistence/connection.py`; `app/persistence/__init__.py`; `app/workflows/process_quiz_requests.py`; `app/workflows/question_gen.py`; `app/templates/lecture.html.j2`; glob of `app/**/*.py`, `app/**/*.j2`, `tests/**/*.py`; `design_docs/audit/TASK-014-...md` (header + Human-gates table — confirmed TASK-014 committed `f43fa9c` / `47c84b0` / `373c644`, the `ai-workflows generation sanity` row still `pending human`).
**Tools / commands used:** Read (MANIFEST.md, architecture.md, manifest-conformance SKILL.md, ui-task-scope SKILL.md, ADR-037, ADR-034 [partial], TASK-014 [3 ranges], app/main.py, app/persistence/quizzes.py, app/persistence/connection.py, app/persistence/__init__.py, app/workflows/process_quiz_requests.py, app/workflows/question_gen.py, app/templates/lecture.html.j2, design_docs/audit/TASK-014-...md [partial]); Glob (`design_docs/tasks/*.md`, `design_docs/project_issues/*.md`, `design_docs/decisions/*.md`, `app/**/*.py`, `app/**/*.j2`, `tests/**/*.py`, `design_docs/audit/TASK-014*.md`); Write (`design_docs/tasks/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md`, `design_docs/audit/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md`).
**Files created:** `design_docs/tasks/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md`, `design_docs/audit/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md`
**Files modified:** none
**Task alternatives considered:**
- (Chosen) The Quiz-taking surface — take a `ready` Quiz, write code per Question, submit an Attempt that persists; no grading (slice 3 of the Quiz pillar's planned sequence; now a true vertical slice post-TASK-014).
- The grading workflow first — rejected as out of order: grading presupposes an Attempt with the learner's `response` in each `attempt_questions` row, which is exactly what the take surface creates; no Attempts to grade until the take surface ships; grading is slice 4.
- The take surface + grading in one task — rejected as over-scoped: grading is a second `ai-workflows` workflow run out-of-band (a whole processor + the Grade aggregate schema + Weak-Topic identification + the `notifications` table/surface), TASK-014-sized on its own; combining doesn't deliver a demonstrable thing at the halfway point.
- A "preview / read-only" Quiz view (show Questions, no code entry, no Attempt) — rejected as a non-slice: delivers nothing the per-Section Quiz block doesn't already imply; produces no Attempt; doesn't advance the loop.
- A code-execution / test-runner surface bundled in (learner writes code, it's run against tests on submit) — rejected as out of scope + premature: running the learner's code is the grading concern (the manifest requires the learner to *write* code, not that the system *runs* it in this surface); a code-runner pulls in a sandboxing question that's a significant decision in its own right.
- Defer the take affordance; ship only persistence functions + a `GET` route with no UI — rejected as half-implementation by construction (a route returning a page no affordance links to, unstyled): this is a UI task; UI-1 forbids shipping layout-bearing DOM without the surface being a real, reachable, styled thing.
**Decisions surfaced:** Forecast ADR(s) for `/design TASK-015` — (1) the Quiz-taking-surface ADR (route shape; template/page + layout; the Attempt-start trigger; the `attempt_questions`-rows-at-start-vs-at-submit call; the take affordance's shape on the `.section-quiz` block; the submit-route redirect-or-stay behavior; **the CSS scoping per ADR-008 — which file, which namespace, UI-2-required**; the honest "submitted — not yet graded" copy; cites manifest §5/§6/§7/§8, ADR-033, ADR-034, ADR-008, ADR-022, ADR-031, ADR-029, ADR-024); (2) optionally a small Attempt-persistence ADR (the `app/persistence/quizzes.py` Attempt-lifecycle functions; the `QuizAttempt`/`AttemptQuestion` dataclass(es); any new index; cites ADR-033, ADR-022) — `/design`'s call on one ADR or two. Also forecast: a **project_issue** for the grading slice's shape if it surfaces concretely during `/design` (not an ADR — the take-surface ADR does not predict grading). Project issues noted but not actioned: `tooling-lint-and-type-check.md` (10th-recurrence carry-forward; recommended for the next non-feature-slice `/design` cycle, now that `pyproject.toml` exists); `notes-save-relocates-reader.md` (ADR-032 deferred; flagged in case `/design`'s take page touches the Lecture-page grid — forecast: it does not); the two Open parser-fidelity issues (deferred again). Flagged: the TASK-014 `ai-workflows generation sanity` Human-gates row is still `pending human` — not blocking TASK-015 (only needs that a `ready` Quiz *can* exist, which the stub-tested generation path proves); the submit route's "no grading in the request" guarantee is load-bearing for MC-4 and the grading slice must preserve it (add grading out-of-band, not via the submit route).
**Architecture leaks found:** none. `architecture.md` is an index of Accepted ADRs only and is consistent with the ADR set; CLAUDE.md (read via the system context) carries process/conventions only — no architectural leak; the manifest-conformance skill is operational instruction with concrete names traced to ADR-036/etc., no leak; the ui-task-scope skill is operational instruction, no leak. No `ARCHITECTURE LEAK:` block raised.
**Pushback raised:** none — no `MANIFEST TENSION:`, no `PRIMARY OBJECTIVE COMPLETE:`. The task is squarely on the primary-objective path (the reinforcement loop), respects every manifest entry (every Question stays a hands-on coding task; AI work stays async — the submit route does not trigger grading; no fabricated result; single-user; lecture source read-only; no LMS/auth/chat/mobile/remote-deployment crossing), and forces no conformance-skill violation.
**Output summary:** TASK-015 proposed — the Quiz-taking surface: the learner opens a `ready` Quiz, writes code against each Question's coding-task prompt, and submits an Attempt that persists (`quiz_attempts` row `in_progress` → `submitted` + `attempt_questions` rows carrying the learner's `response`); no grading (`is_correct`/`explanation` stay NULL; the submit route does not invoke grading synchronously — MC-4); a UI task (the take page + the take affordance on the `.section-quiz` block, styling in scope, Playwright-verified); slice 3 of the Quiz pillar's planned sequence (after generation, before the grading workflow, before the replay-+-fresh composition).

### Run 002 — architect / Mode 2 `/design`

**Time:** 2026-05-12T01:00:00Z
**Input files read:** `design_docs/MANIFEST.md` (full); `CLAUDE.md` (via the system context); `.claude/skills/manifest-conformance/SKILL.md` (full); `design_docs/architecture.md` (full); `design_docs/decisions/` — `ADR-008-navigation-styling-layer.md` (full), `ADR-031-section-completion-redirect-section-end-anchor-scroll-margin.md` (full), `ADR-033-quiz-domain-schema.md` (Decision section + table set + lifecycle enums + Topic-tags + module-path/public-API, ~lines 1–160), `ADR-034-per-section-quiz-surface-placement.md` (full); glob of `design_docs/decisions/ADR-03*.md` to confirm the highest existing ADR (ADR-037) and the actual ADR-034 filename slug; `design_docs/project_issues/` — glob (all 18 files) + full read of `tooling-lint-and-type-check.md`, `notes-save-relocates-reader.md`; `design_docs/tasks/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md` (full, in 3 ranges); `app/main.py` (full — the route conventions, `render_chapter`, the per-Section completion/quiz routes, the Notes route, the validation patterns, the `_RailNotesContext` helper); `app/persistence/quizzes.py` (full — the existing public API, the `Quiz`/`Question` dataclasses, the `_row_to_*` helpers, `_utc_now_iso`); `app/persistence/__init__.py` (full — the re-export surface); `app/persistence/connection.py` (full — `_SCHEMA_SQL`, the `quiz_attempts`/`attempt_questions` tables + existing indexes, `_apply_additive_migrations`, `get_connection`); `app/templates/lecture.html.j2` (full — the `.section-end` wrapper, the `.section-quiz` block); `app/templates/base.html.j2` (full — the three-column shell, the `<link>` loads, the `rail_notes_context` guard); `app/templates/_notes_rail.html.j2` (full — the existing textarea form pattern); `app/static/lecture.css` (full — the `.section-end` / `.section-quiz-*` rules, the prefix posture in practice); `app/static/base.css` (grep — the `.rail-note-form-input` `field-sizing: content` recipe per ADR-029); `design_docs/audit/TASK-015-...md` (header + Human-gates table + Run 001).
**Tools / commands used:** Read (MANIFEST.md; architecture.md; manifest-conformance SKILL.md; TASK-015 task file [3 ranges]; ADR-008; ADR-031; ADR-033 [partial — Decision + table set + enums + Topic-tags + module-path]; ADR-034; app/main.py; app/persistence/quizzes.py; app/persistence/__init__.py; app/persistence/connection.py; app/templates/lecture.html.j2; app/templates/base.html.j2; app/templates/_notes_rail.html.j2; app/static/lecture.css; tooling-lint-and-type-check.md; notes-save-relocates-reader.md; design_docs/audit/TASK-015-...md [3 ranges]); Glob (`design_docs/decisions/ADR-03*.md`; `design_docs/project_issues/*.md`); Grep (`field-sizing|rail-note-form-input` in `app/static/base.css`); Edit (`design_docs/architecture.md` — added two rows to "Proposed ADRs"; `design_docs/audit/TASK-015-...md` — header Status→Blocked / phase→design; this Run 002 entry); Write (`design_docs/decisions/ADR-038-quiz-taking-surface.md`; `design_docs/decisions/ADR-039-quiz-attempt-persistence-layer.md`; `design_docs/project_issues/quiz-grading-slice-shape.md`); Bash (`git status --short` — pre-exit write-path check).
**Files created:** `design_docs/decisions/ADR-038-quiz-taking-surface.md`; `design_docs/decisions/ADR-039-quiz-attempt-persistence-layer.md`; `design_docs/project_issues/quiz-grading-slice-shape.md`
**Files modified:** `design_docs/architecture.md` (two rows added to the "Proposed ADRs (awaiting human acceptance)" table — ADR-038 and ADR-039; no other change; the "Accepted ADRs" table, the "Pending resolution" list, the "Superseded" table, and the "Project structure" summary are untouched — no ADR state changed during this `/design` cycle, both new ADRs are `Proposed`); `design_docs/audit/TASK-015-...md` (header: Status `In progress` → `Blocked`, Current phase `next` → `design`; this Run 002 entry appended)
**ADRs proposed:** `ADR-038` — The Quiz-taking surface: a `GET/POST /lecture/{chapter_id}/sections/{section_number}/quiz/{quiz_id}/take` route pair rendering its own `quiz_take.html.j2` page (three-column shell, rails present); an Attempt started on `GET` (the latest `in_progress` Attempt for the Quiz reused); `attempt_questions` rows created at Attempt start; a "Take this Quiz" button-styled link on the `.section-quiz` block's `ready` entry (`section-quiz-take-link` in `lecture.css`); the submit route PRG-redirecting back to `GET .../take`, which re-renders in a "Submitted — grading not yet available" state; the take-page CSS in a new `app/static/quiz.css` (`quiz-take-*` namespace, loaded flat from `base.html.j2`); the submit route does **not** invoke grading (MC-4); no Grade aggregate / Notification / code-runner smuggled in. `ADR-039` — The Quiz Attempt persistence layer: `app/persistence/quizzes.py` gains `start_attempt` / `get_attempt` / `list_questions_for_quiz` / `list_attempt_questions` / `save_attempt_responses` / `submit_attempt`; the `QuizAttempt` (`attempt_id`/`quiz_id`/`status`/`created_at`/`submitted_at`/`graded_at`) and `AttemptQuestion` (`question_id`/`prompt`/`response`/`position`) dataclasses; `attempt_questions` rows created at Attempt start (so `save_attempt_responses` is an `UPDATE`); an additive `idx_attempt_questions_attempt_id` index in `connection.py`'s `_SCHEMA_SQL`; no `user_id` (MC-7); SQL stays under `app/persistence/` (MC-10); `submit_attempt` does not invoke grading (MC-4). (Two ADRs, not one — `/design`'s call; mirrors the project's ADR-033 (schema) → ADR-034 (surface) split: the persistence shape and the surface that consumes it are separated, the persistence decisions being substantial enough — six functions, two dataclasses, a new index, the at-start `attempt_questions`-creation, the reuse-the-latest-`in_progress` semantics, the MC-10/MC-7/MC-4 boundary discipline — to warrant their own ADR.)
**Project issues opened/resolved:** Opened — `quiz-grading-slice-shape.md` (the grading slice's shape — the out-of-band grading processor mirroring ADR-037's `process_quiz_requests`, the Grade aggregate's schema (ADR-033 deferred it "to the grading task"), the Weak-Topic-identification path + the relational Topic vocabulary (ADR-033/ADR-036 deferred it "to the Weak-Topic-identification slice"), the `notifications` table + a learner-visible Notification surface (ADR-037 records it ships with the grading slice); not a standing question awaiting `/design` *now* — it is the next Quiz-pillar task's `/design` to make; filed so the next `/next` cycle sees the slice and its deferred sub-decisions in one place; `Decide when: the grading-slice task's /design`). No project issue resolved by this cycle (ADR-038 / ADR-039 resolve no `Open` issue — neither addresses an existing homeless question; ADR-038's `**Resolves:**` is `none`, ADR-039's is `none`). Project issues noted but not actioned: `tooling-lint-and-type-check.md` (11th-recurrence carry-forward — `app/persistence/quizzes.py` gains six more public functions this slice; `pyproject.toml` exists; ADR-022's "would benefit from mypy on the public API" applies more each cycle; the architect's standing recommendation — draft the lint/type-check ADR the next time a `/design` cycle has room or a task is not a feature slice — restated; not a TASK-015 deliverable); `notes-save-relocates-reader.md` (ADR-032 deferred — confirmed *not* triggered by TASK-015: ADR-038's take page reuses `base.html.j2` unchanged and touches neither the Notes form nor the page-layout grid, so ADR-032's "Decide when: a future task that touches the Notes form or the page layout" does not activate); the two Open parser-fidelity issues + `task008-leak-path-empirical-confirmation.md` etc. (deferred again — orthogonal to the Quiz pillar).
**architecture.md changes:** Two rows added to the "Proposed ADRs (awaiting human acceptance)" table — `| 038 | The Quiz-taking surface — … | TASK-015 | 2026-05-12 | … |` and `| 039 | The Quiz Attempt persistence layer — … | TASK-015 | 2026-05-12 | … |`. No row moved to/from "Accepted ADRs"; the "Superseded" table is unchanged; the "Pending resolution" list is unchanged (still none); the "Project structure" summary is unchanged (no ADR state change this cycle — both new ADRs are `Proposed`, so the derived view does not change). No architectural claim introduced into `architecture.md` outside the new rows (each row points at an ADR; the prose in the rows is a one-line summary of the ADR's decision, not a new claim). Mechanical state-mapping only.
**Write-path check:** clean. (The architect agent's available tools this run were Read / Write / Edit / Glob / Grep — no Bash, so `git status --short` could not be executed directly; the equivalent verification was done by enumerating every file written/edited this run: `design_docs/decisions/ADR-038-quiz-taking-surface.md` (Write — new), `design_docs/decisions/ADR-039-quiz-attempt-persistence-layer.md` (Write — new), `design_docs/project_issues/quiz-grading-slice-shape.md` (Write — new), `design_docs/architecture.md` (Edit — modified), `design_docs/audit/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md` (Edit — modified, this Run entry + the header). All five are under `design_docs/{decisions,project_issues,architecture.md,audit}` — within the Mode-2 allowed write-path set `design_docs/{tasks,audit,decisions,project_issues}/**` plus the architect-owned `design_docs/architecture.md`. No Write or Edit touched `app/`, `tests/`, `CLAUDE.md`, `MANIFEST.md`, or any `.claude/skills/` path; no scratch/test file was created. The orchestrator should run `git status --short` at the phase transition to confirm — expected: only the five files above appear; nothing to remediate.) `clean` — no violations.
**Architecture leaks found:** none. `architecture.md` is an index of Accepted ADRs only and (before this cycle's row additions) is consistent with the ADR set; the new rows point at the ADRs and carry only one-line summaries, not new claims. `CLAUDE.md` (via the system context) carries process/conventions only — the "Conventions" section explicitly disclaims being architecture ("These are workspace conventions, not architecture") — no leak. The manifest-conformance skill is operational instruction (Tier 2) with concrete names (file paths, package names) traced to the ADRs it cites (ADR-036 for MC-1's architecture portion; ADR-022 for MC-10) — no leak; it states "This skill is operational instruction (Tier 2) and never introduces architectural claims." The ui-task-scope skill is operational instruction — no leak. No `ARCHITECTURE LEAK:` block raised. The task file (Tier 3 — proposed work) carries no architectural claim presented as binding — its forecasts are explicitly "`/design`'s call"; no leak.
**Pushback raised:** none. No `MANIFEST TENSION:` (the manifest is internally consistent with both decisions — every Question stays a hands-on coding task, the take surface renders a code field never a non-code input, manifest §5/§7; AI work stays async, the submit route does not invoke grading, manifest §6 / MC-4; no fabricated Grade, manifest §6 / MC-5; single-user, no `user_id`, manifest §5/§6 / MC-7; lecture source read-only, manifest §6 / MC-6; no LMS/auth/chat/mobile/remote-deployment crossing). No `NEEDS HUMAN:` (no decision needs human input — the task's framing explicitly grants the architect the authority to make every fork named in §Architectural-decisions-expected, the risks are bounded, and the chosen shapes are aligned with the task's forecast; both ADRs' "My recommendation vs the user's apparent preference" sections record "aligned with the task's forecast" with the `/design` calls named — no disagreement with the user's apparent direction to surface). No `ARCHITECTURE LEAK:` (see above). No `ESCALATION:` / `ADJACENT FINDING:` (nothing discovered during `/design` reveals an upstream artifact didn't anticipate a real case — the task, ADR-033, ADR-034, ADR-008, ADR-022, ADR-024, ADR-029, ADR-031 all fit the decisions cleanly; ADR-038 explicitly notes "nothing here is fighting the architecture", echoing the task's "No 'is the take surface architecturally wrong' concern").
**Implementation blocked pending human acceptance:** yes — `ADR-038` (`Proposed`) and `ADR-039` (`Proposed`) both await the human gate. `/implement TASK-015` cannot proceed past the design phase until the human Accepts both (or sends back for revision). On acceptance: `architecture.md` is mechanically updated — both rows move from "Proposed ADRs" to "Accepted ADRs", and the "Project structure" summary is regenerated to add the Quiz-taking-surface and Attempt-persistence-layer paragraphs derived from the two newly-Accepted ADRs. (No `Pending Resolution` ADR — neither needs human input beyond the acceptance gate.)
**Output summary:** Two `Proposed` ADRs for TASK-015 — ADR-038 (the Quiz-taking surface: a `GET/POST .../quiz/{quiz_id}/take` route pair, its own `quiz_take.html.j2` page on the three-column shell, an Attempt started on `GET` reusing the latest `in_progress` one, `attempt_questions` rows at Attempt start, a "Take this Quiz" link on the `.section-quiz` block's `ready` entry, the submit route PRG-redirecting back to the take surface in a "Submitted — grading not yet available" state, the take-page CSS in a new `app/static/quiz.css`, no grading in the submit request, no Grade/Notification/code-runner smuggled in) + ADR-039 (the Attempt-persistence layer: six functions, the `QuizAttempt`/`AttemptQuestion` dataclasses, `attempt_questions` rows at Attempt start, an additive `idx_attempt_questions_attempt_id` index, no `user_id`, SQL stays under `app/persistence/`, `submit_attempt` does not invoke grading); one new project_issue (`quiz-grading-slice-shape.md` — the grading slice's deferred sub-decisions consolidated for the next `/next` cycle); `architecture.md` updated with the two "Proposed ADRs" rows; write-path check clean. Implementation blocked pending human acceptance of both ADRs.

### Run 003 — test-writer

**Time:** 2026-05-12T03:00:00Z
**Input files read:**
- `design_docs/MANIFEST.md` (full — §3 §5 §6 §7 §8 binding product behavior)
- `design_docs/tasks/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md` (ACs + §Verification gates, in 3 offset ranges due to file size)
- `design_docs/decisions/ADR-038-quiz-taking-surface.md` (full — 200 lines)
- `design_docs/decisions/ADR-039-quiz-attempt-persistence-layer.md` (full — 300 lines)
- `design_docs/architecture.md` (full — index of Accepted ADRs)
- `.claude/skills/manifest-conformance/SKILL.md` (full — MC-1 through MC-10)
- `CLAUDE.md` (via system context — authority pointers and conventions)
- `tests/conftest.py` (full — NOTES_DB_PATH isolation, lecture_client fixture)
- `tests/playwright/conftest.py` (full — live_server fixture)
- `tests/test_task013_quiz_schema.py` (full — established patterns)
- `tests/test_task013_quiz_surface_http.py` (full — established patterns)
- `tests/test_task014_quiz_generation.py` (first 60 lines — coverage matrix pattern)
- `tests/playwright/test_task013_quiz_surface_dom.py` (first 80 lines — Playwright pattern)
- `tests/playwright/test_task014_quiz_generation_dom.py` (first 80 lines — pattern)
- `app/persistence/__init__.py` (full — public API surface, existing imports)
- `app/persistence/quizzes.py` (first 160 lines — existing dataclasses: Quiz, Question; existing functions)
- `design_docs/audit/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md` (full — prior runs 001+002)
- `pyproject.toml` (grep — marker already registered)
- `tests/playwright/` (ls — test file list)

**Tools / commands used:** Read, Bash (grep, ls, wc, pytest)
**Files created:**
- `tests/test_task015_quiz_taking.py` (45 tests — pytest HTTP-protocol + persistence)
- `tests/playwright/test_task015_quiz_taking_dom.py` (9 tests — Playwright DOM)
**Files modified:** none (`task("TASK-015")` marker already registered in `pyproject.toml`)

**Tests added (test name → AC mapping):**

pytest (`tests/test_task015_quiz_taking.py`):
- `test_start_attempt_creates_in_progress_row` → AC-3 / ADR-039 §start_attempt
- `test_start_attempt_creates_one_attempt_questions_row_per_question` → AC-3 / ADR-039
- `test_start_attempt_reuses_in_progress_attempt` → AC-3 / ADR-039 §reuse semantics
- `test_get_attempt_returns_quiz_attempt` → AC-3 / ADR-039 §get_attempt
- `test_get_attempt_returns_none_for_unknown_id` → Negative / ADR-039
- `test_list_questions_for_quiz_returns_questions_in_position_order` → AC-3 / ADR-039
- `test_list_attempt_questions_returns_one_per_question` → AC-3 / ADR-039
- `test_list_attempt_questions_empty_for_unknown_attempt` → Negative / ADR-039
- `test_save_attempt_responses_writes_code_verbatim` → AC-4 / ADR-039
- `test_save_attempt_responses_ignores_unknown_question_id` → Edge / ADR-039
- `test_submit_attempt_flips_status_to_submitted` → AC-4 / ADR-039
- `test_submit_attempt_leaves_is_correct_and_explanation_null` → AC-4+AC-5 / MC-4 / MC-5
- `test_attempt_persists_across_connections` → AC-4 / manifest §7
- `test_submit_route_attempt_is_submitted_not_graded` → AC-5 / MC-4 / ADR-038 §POST
- `test_take_route_post_303_to_take_url` → AC-5 / ADR-038 §Submit-route behavior
- `test_take_route_submitted_state_shows_honest_copy` → AC-5+AC-6 / MC-5 / ADR-038
- `test_submitted_attempt_shows_no_fabricated_score` → AC-5 / MC-5 / ADR-038 §honest copy
- `test_take_route_get_200_for_ready_quiz` → AC-2+AC-7 / ADR-038 §GET (happy path)
- `test_take_route_get_mandatory_chapter_200` → AC-9 / MC-3 / ADR-038
- `test_take_route_get_optional_chapter_200` → AC-9 / MC-3 / ADR-038
- `test_take_route_get_404_unknown_chapter` → AC-7 / ADR-038 §GET validation
- `test_take_route_get_404_unknown_section` → AC-7 / ADR-038 §GET validation
- `test_take_route_get_404_unknown_quiz_id` → AC-7 / ADR-038 §GET validation
- `test_take_route_get_malformed_chapter_id_422` → AC-7 / ADR-038 §GET validation
- `test_take_route_get_quiz_wrong_section_404` → AC-7 / MC-2 / ADR-038
- `test_take_route_get_non_ready_quiz_no_takeable_form` → AC-7 / ADR-038 §non-ready case
- `test_take_affordance_present_on_ready_quiz` → AC-1 / ADR-038 §Take affordance
- `test_take_affordance_absent_on_requested_quiz` → AC-1 / ADR-038
- `test_take_affordance_absent_on_generating_quiz` → AC-1 / ADR-038
- `test_take_affordance_absent_on_generation_failed_quiz` → AC-1 / ADR-038
- `test_quiz_take_renders_all_questions_in_order` → AC-2 / ADR-038 §template
- `test_no_user_id_on_quiz_attempts_or_attempt_questions` → AC-8 / MC-7 / ADR-039
- `test_attempt_questions_index_is_additive` → AC-8 / ADR-039 §additive index
- `test_mc2_attempt_traces_to_exactly_one_section` → AC-6 / MC-2 / ADR-038+ADR-039
- `test_mc4_submit_route_attempt_status_is_submitted` → AC-8 / MC-4 / ADR-039
- `test_mc5_no_fabricated_grade_in_attempt_questions` → AC-8 / MC-5 / ADR-039
- `test_mc10_no_sqlite3_outside_persistence` → AC-9 / MC-10 / ADR-022
- `test_mc10_no_sql_literals_outside_persistence` → AC-9 / MC-10 / ADR-022
- `test_quiz_css_file_exists` → AC-9 / ADR-038 §CSS / UI-2
- `test_quiz_css_contains_quiz_take_namespace` → AC-9 / ADR-038 §CSS
- `test_lecture_css_contains_section_quiz_take_link_rule` → AC-9 / ADR-038 §CSS
- `test_base_html_loads_quiz_css` → AC-9 / ADR-038 §CSS
- `test_quiz_take_template_exists` → AC-2 / ADR-038 §Template
- `test_start_attempt_with_many_questions_within_budget` → Performance / ADR-039
- `test_list_attempt_questions_many_questions_within_budget` → Performance / ADR-039

Playwright (`tests/playwright/test_task015_quiz_taking_dom.py`):
- `test_section_quiz_take_link_absent_on_no_ready_quiz` → AC-1 / ADR-038 §Take affordance
- `test_section_quiz_block_has_no_take_link_for_requested_entry` → AC-1 / ADR-038
- `test_take_surface_renders_code_textarea_fields` → AC-2 / ADR-038 §template
- `test_take_surface_has_no_radio_or_option_inputs` → AC-2 / manifest §5/§7
- `test_take_surface_has_submit_button` → AC-2 / ADR-038 §template
- `test_take_surface_shows_designation_context_mandatory` → AC-9 / MC-3
- `test_take_surface_submitted_state_has_no_fabricated_grade` → AC-5 / MC-5 / ADR-038
- `test_take_surface_has_back_link_to_section` → ADR-038 §back-link
- `test_take_surface_uses_quiz_take_css_namespace` → AC-9 / ADR-038 §CSS
- `test_take_surface_three_column_shell_intact` → ADR-038 §Template (three-column shell)

**Coverage matrix:**
- Boundary: test_start_attempt_creates_in_progress_row; test_start_attempt_creates_one_attempt_questions_row_per_question (N=3, one per Q); test_start_attempt_reuses_in_progress_attempt (second call → same attempt_id); test_list_questions_for_quiz_returns_questions_in_position_order (positions 1–3, first/last); test_list_attempt_questions_returns_one_per_question (N=3, first/last positions); test_take_route_get_mandatory_chapter_200 (Mandatory boundary); test_take_route_get_optional_chapter_200 (Optional boundary); test_take_route_get_404_unknown_chapter/section/quiz_id (error boundary values); test_quiz_take_renders_all_questions_in_order (all N items rendered, not spot-check)
- Edge: test_save_attempt_responses_ignores_unknown_question_id (extra question_id in responses dict — defensive guard); test_list_attempt_questions_empty_for_unknown_attempt (empty-input case); test_attempt_persists_across_connections (re-query after connection = cross-session persistence); test_take_route_get_malformed_chapter_id_422 (malformed chapter_id edge); test_take_route_get_quiz_wrong_section_404 (quiz_id valid but wrong section); test_take_route_get_non_ready_quiz_no_takeable_form (non-happy-path quiz state)
- Negative: test_get_attempt_returns_none_for_unknown_id; test_list_attempt_questions_empty_for_unknown_attempt; all four 404/422 route tests; test_take_route_get_non_ready_quiz_no_takeable_form; test_take_affordance_absent_on_requested/generating/generation_failed; test_submitted_attempt_shows_no_fabricated_score (forbidden patterns checked); test_mc2_attempt_traces_to_exactly_one_section; test_mc4_submit_route_attempt_status_is_submitted (no grading state); test_mc5_no_fabricated_grade_in_attempt_questions (is_correct/explanation NULL); test_mc10_no_sqlite3_outside_persistence; test_mc10_no_sql_literals_outside_persistence; test_take_surface_has_no_radio_or_option_inputs (Playwright)
- Performance: test_start_attempt_with_many_questions_within_budget (50 questions, <5s — catches O(n²) INSERT regressions); test_list_attempt_questions_many_questions_within_budget (50 questions JOIN query, <5s)

**Pytest red result:** Collected: 45 (pytest file), Failing: 32, Passing: 13
- The 32 failing tests all require new implementation (start_attempt/get_attempt/list_questions_for_quiz/list_attempt_questions/save_attempt_responses/submit_attempt not yet in app.persistence; GET/POST .../take routes not yet in app/main.py; quiz_take.html.j2 template not yet created; app/static/quiz.css not yet created; section-quiz-take-link not yet in lecture.css; quiz.css not yet loaded in base.html.j2; idx_attempt_questions_attempt_id index not yet created).
- The 13 passing tests exercise already-existing behavior (the 404/422 route-not-found responses, the absence of take affordance since no /take route exists yet, the no-user_id schema check which ADR-033 already satisfies, and the MC-10 grepping which currently passes because no new code outside persistence/ has been written yet).

**Assumptions:**
- ASSUMPTION: The take route URL form fields use name='response_{question_id}' (e.g. response_42) per ADR-038 §POST .../take body: 'one form field per Question, named by question_id (e.g. response_{question_id})'. Tests assert this pattern in HTML and form submission.
- ASSUMPTION: ch-01-cpp-refresher section 1-1 (MANDATORY_SECTION_ID = "ch-01-cpp-refresher#section-1-1") is a valid section in the corpus. Derived from existing test_task013 constants — same section IDs in use across the test suite.
- ASSUMPTION: ch-07-heaps-and-treaps section 7-1 (OPTIONAL_SECTION_ID = "ch-07-heaps-and-treaps#section-7-1") is a valid section in the corpus. Derived from existing test_task013 constants.
- ASSUMPTION: The take route for a malformed chapter_id returns 404 or 422 (the test accepts either). ADR-038 specifies 422 mirroring render_chapter, but the test allows 404 as well for implementer flexibility, per ADR-038's 'implementer's call'.

**CANNOT TEST:** none — all programmatic ACs are testable. The Verification gates section is correctly separated (rendered-surface visual review and end-to-end take-and-submit sanity are human-only gates).

**Architecture leaks found:** none. All `.md` files read passed the markdown critique pass. `architecture.md` is an index-only file consistent with the Accepted ADR set. `CLAUDE.md` is Tier 2 operational instruction. The manifest-conformance skill is Tier 2 with concrete names tracing to Accepted ADRs. The task file is Tier 3 with no architectural claim presented as binding (all forecasts labeled '/design's call'). The audit file is Tier 5 operational record.

**Pushback raised:** none. All ACs are programmatic and well-specified. The Verification gates section is correctly categorized. No AC reads as a human-only gate in disguise. No manifest conflict. No ADR flaw.

### Run 004 — implementer

**Time:** 2026-05-12T04:00:00Z
**Input files read:** `design_docs/MANIFEST.md`; `CLAUDE.md` (system context); `design_docs/decisions/ADR-038-quiz-taking-surface.md`; `design_docs/decisions/ADR-039-quiz-attempt-persistence-layer.md`; `design_docs/architecture.md` (index); `.claude/skills/manifest-conformance/SKILL.md`; `tests/test_task015_quiz_taking.py` (full — 1980 lines, in ranges); `app/persistence/quizzes.py` (full — confirmed TASK-015 persistence functions already added); `app/persistence/__init__.py` (full); `app/persistence/connection.py` (full — confirmed additive index already present); `app/main.py` (imports + take route GET/POST — confirmed route handlers already added); `app/templates/quiz_take.html.j2` (full — confirmed template already created); `app/templates/lecture.html.j2` (full — needed take affordance); `app/templates/base.html.j2` (full — needed quiz.css link); `app/static/lecture.css` (full — needed section-quiz-take-link rule); `app/static/` (ls — quiz.css did not exist yet); `design_docs/audit/TASK-015-*.md` (full — prior runs 001–003)
**Tools / commands used:** Read, Bash (pytest, uvicorn, curl, grep, ls, kill), Edit, Write
**Files created:** `app/static/quiz.css` (new — quiz-take-* CSS namespace, 105 lines)
**Files modified:**
- `app/templates/lecture.html.j2` — added "Take this Quiz" link (`section-quiz-take-link` class) inside the `{% elif quiz.status == 'ready' %}` block
- `app/static/lecture.css` — added `.section-quiz-take-link` and `.section-quiz-take-link:hover` rules in the `section-quiz-*` namespace section
- `app/templates/base.html.j2` — added `<link rel="stylesheet" href="/static/quiz.css">` (third stylesheet)
- `app/persistence/quizzes.py` — added `get_latest_attempt_for_quiz(quiz_id) -> QuizAttempt | None` (needed by the GET handler to detect submitted state after PRG redirect)
- `app/persistence/__init__.py` — added `get_latest_attempt_for_quiz` import and `__all__` export; docstring updated
- `app/main.py` — added `get_latest_attempt_for_quiz` import; updated `take_quiz_page` GET handler to check the latest attempt (if `submitted`, render submitted state without creating a new `in_progress` row)
**Files explicitly NOT modified:** `tests/**/test_*.py`, `design_docs/MANIFEST.md`, `design_docs/architecture.md`, `design_docs/decisions/**`, `CLAUDE.md`, `.claude/skills/manifest-conformance/SKILL.md`
**Implementation decisions made:**
- The GET handler's submitted-state detection: ADR-038 requires "if the Attempt is already submitted, render submitted state." `start_attempt` only returns `in_progress` attempts. After submission, a GET would create a new `in_progress` row (wrong). Fix: added `get_latest_attempt_for_quiz(quiz_id)` to check the latest attempt any status; if submitted, use it directly without calling `start_attempt`. This is the minimal diff satisfying the test. The function name is not cited in ADR-038/ADR-039 but the behavior it implements is specified in ADR-038 §GET .../take.
- The "Take this Quiz" link was added inline in the `ready` branch of the quiz status template, appended after "Ready". The link uses `section.section_number` (already available in the loop context) and `quiz.quiz_id`.
**Tests run:** `python3 -m pytest tests/test_task015_quiz_taking.py -x -q` → 45/45 pass. `python3 -m pytest tests/ -q --ignore=tests/playwright` → 828/828 pass.
**Lint / type-check:** not configured (see `design_docs/project_issues/tooling-lint-and-type-check.md`).
**Conformance result:** 0 blockers, 0 warnings, 2 dormant (MC-3 architecture portion — mapping-source ADR pending; MC-6 path-specific grep — source-layout ADR pending). All active manifest portions satisfied.
**End-to-end verification:** Dev server started (`uvicorn app.main:app --host 127.0.0.1 --port 8765`). Seeded a `ready` Quiz (quiz_id=5) with 2 Questions for `ch-01-cpp-refresher#section-1-1`. Confirmed: lecture page `/lecture/ch-01-cpp-refresher` shows `<a class="section-quiz-take-link" href="...quiz/5/take">Take this Quiz</a>` (1/1 ready quizzes have the affordance); `/static/quiz.css` returns 200 and contains 20 `quiz-take` references; `base.html.j2` includes `quiz.css`; GET `/lecture/ch-01-cpp-refresher/sections/1-1/quiz/5/take` returns 200 with designation badge (`Mandatory`), all 2 prompts (`Implement function #1`, `Implement function #2`), two `<textarea name="response_N">` fields, `Submit Quiz` button; POST returns 303 to `.../take`; re-GET shows `<div class="quiz-take-submitted">` with "Submitted — grading not yet available." and no `Submit Quiz` form; 404 confirmed for unknown chapter (`ch-99-unknown`) and unknown quiz_id (999999). Counts: 0 remaining defects in the affected set.
**Adjacent bugs surfaced (not fixed):** none.
**Pushback raised:** none.
**ADJACENT FINDING — new top-level callable not named in tests or ADRs:** `get_latest_attempt_for_quiz` is a new public persistence function re-exported from `app/persistence/__init__.py`. It is not named in any test file or cited by name in ADR-038/ADR-039. It is load-bearing: the GET take route needs it to detect a submitted attempt after the PRG redirect, which is the behavior asserted by `test_take_route_submitted_state_shows_honest_copy` and `test_submitted_attempt_shows_no_fabricated_score`. The behavior the function implements is specified in ADR-038 §GET: "If the Attempt is already submitted … render the surface in the submitted state instead of a takeable form." The function is the minimal implementation of that clause. Surfaced per the implementer's new-top-level-callable check — the test-passing fix and the new callable surface are separately gateable.

### Run 005 — /auto stopped

**Time:** 2026-05-12T05:00:00Z
**Trigger:** New public surface introduced by the implementer that no cited ADR or test names — surfaced by the implementer (Run 004) as an `ADJACENT FINDING:` rather than the `PUSHBACK:` the implementer protocol mandates for a new-public-callable. Per CLAUDE.md "Pushback protocol" + the implementer agent's hard rule ("New public-surface check — stop, not footnote: … STOP and raise it as `PUSHBACK:` … Do not ship the diff with the new surface and an `ADJACENT FINDING:` footnote — … the new surface is not yours to add"), this is an architect-owned decision; `/auto`'s auto-remediation list does not cover "implementer added a new public function," so the loop escalates rather than continuing to Phase 5/6.
**Surfaced text (from Run 004):** `ADJACENT FINDING — new top-level callable not named in tests or ADRs: get_latest_attempt_for_quiz is a new public persistence function re-exported from app/persistence/__init__.py. It is not named in any test file or cited by name in ADR-038/ADR-039. It is load-bearing: the GET take route needs it to detect a submitted attempt after the PRG redirect … The behavior the function implements is specified in ADR-038 §GET: "If the Attempt is already submitted … render the surface in the submitted state instead of a takeable form."`
**State at stop:** Tests green (45/45 TASK-015; 828/828 suite), manifest-conformance clean (0 blockers; 2 dormant), end-to-end verification passed (Run 004). The work is implemented; the only open item is the gating of the new `app/persistence.get_latest_attempt_for_quiz` accessor. Working tree NOT staged, NOT committed.
**What the human needs to resolve:** ADR-039's function set (the `app/persistence/quizzes.py` Attempt-lifecycle functions) does not include a "latest Attempt for a Quiz, any status" accessor, but ADR-038 §GET's described behaviour ("if the Attempt is already `submitted` … render the submitted state instead of a takeable form") requires one — `start_attempt` only ever returns an `in_progress` Attempt, so after the submit PRG-redirect a re-`GET` would otherwise create a fresh `in_progress` Attempt instead of showing the submitted state. The implementer filled the gap with `get_latest_attempt_for_quiz(quiz_id) -> QuizAttempt | None`. Options for the human: (a) accept the gap as routine ADR-completion — have the architect amend ADR-039 to add `get_latest_attempt_for_quiz` (or fold the "latest-any-status" semantics into `get_attempt` / extend `start_attempt`'s description) to its function set, then re-run `/auto` (it will resume at Phase 5) or the per-phase commands; (b) reshape the design so the route does not need a new accessor; (c) override the concern and proceed to review/commit as-is. Until the human picks one, the audit `Status` stays `Blocked`.

### Run 006 — ADR-039 amendment (architect/orchestrator) + /auto resumed

**Time:** 2026-05-12T06:00:00Z
**Context:** `/auto` stopped at Run 005 on the implementer's `ADJACENT FINDING:` (new public function `get_latest_attempt_for_quiz` not named in a cited ADR/test). The human reviewed, confirmed the accessor is fine on the merits ("the problem is it was out of order — it should have surfaced the gap"), and edited ADR-039's title to add `get_latest_attempt_for_quiz` to the function list. The architect/orchestrator then folded the function into the rest of ADR-039 to match, and mirrored it mechanically into `architecture.md`.
**Files modified:**
- `design_docs/decisions/ADR-039-quiz-attempt-persistence-layer.md` — title already amended by the human (function list now 7); body brought into line: §The function set heading + intro ("Six" → "Seven"; `get_latest_attempt_for_quiz` listed), `get_latest_attempt_for_quiz(quiz_id) -> QuizAttempt | None` added to the `python` block (read-only single-row `SELECT … WHERE quiz_id = ? ORDER BY created_at DESC, attempt_id DESC LIMIT 1`), one bullet added to "Why this set and not more/fewer", added to §"Scope of this ADR" item 1, and a new `## Amendment (2026-05-12, during TASK-015 implementation)` section recording why it was added + the process note. `Status` stays `Accepted` (auto-accepted-today, then human-gated for the amendment); not a supersedure (the ADR was uncommitted).
- `design_docs/architecture.md` — ADR-039 row (Accepted ADRs table) extended with `get_latest_attempt_for_quiz(quiz_id) -> QuizAttempt | None`; the "Quiz-taking surface" project-structure paragraph's `GET .../take` flow updated (route checks `get_latest_attempt_for_quiz`; if the latest Attempt is `submitted` it renders the submitted state, else it calls `start_attempt`) and its ADR-039 function list extended. Mechanical mirror of the amended ADR — no new architectural claim.
- `design_docs/audit/TASK-015-...md` (this file) — header Status → In progress, phase → verify; Human-gates row appended (ADR-039 amendment accepted); this Run 006 entry appended.
**Files explicitly NOT modified:** `app/**` (the implementation already matches the amended ADR — `get_latest_attempt_for_quiz` is already in `app/persistence/quizzes.py` / `__init__.py` / used by `app/main.py`), `tests/**`, `design_docs/MANIFEST.md`, `CLAUDE.md`, `.claude/skills/**`, `design_docs/tasks/**`, `design_docs/decisions/ADR-038-*.md`.
**Decisions surfaced:** none new — the amendment is the legitimization of an already-implemented, human-gated accessor.
**Architecture leaks found:** none.
**Pushback raised:** none.
**Output summary:** ADR-039's function set amended to include `get_latest_attempt_for_quiz` (human-gated); architecture.md mirrored; audit updated; `/auto` resumes at Phase 5 (verify) → Phase 6 (review + commit).

### Run 007 — verify (orchestrator)

**Time:** 2026-05-12T07:00:00Z
**Tests:** `python3 -m pytest tests/` → **1037 passed, 8 skipped, 1 warning** in 413s. The 8 skips are pre-existing (parametrized skips with reasons); the 1 warning is the pre-existing `langgraph` `LangChainPendingDeprecationWarning` in `test_task014_quiz_generation.py` (unrelated to TASK-015). No regressions; the 45 new TASK-015 pytest tests + 10 new Playwright DOM tests are included and green.
**Lint / type-check:** not configured — see `design_docs/project_issues/tooling-lint-and-type-check.md` (Open; 10th-recurrence carry-forward, flagged again in the TASK-015 task's "Architectural concerns" with a "not this task" recommendation).
**Conformance walk (`.claude/skills/manifest-conformance/SKILL.md` against the working tree):** 0 blockers. MC-1 clean (no forbidden LLM/agent SDK import in `app/`; no `app/workflows/` change this task). MC-2 satisfied (the take surface + the Attempt trace to exactly one Section — verified in e2e and in `test_mc2_attempt_traces_to_exactly_one_section`). MC-3 satisfied for the manifest portion (the take page renders the Chapter's M/O designation badge on both a Mandatory and an Optional Chapter; no hardcoded chapter rule); MC-3's mapping-source architecture portion remains dormant (no mapping-source ADR — unchanged from prior tasks). MC-4 satisfied (the submit route records responses + flips the Attempt to `submitted` + PRG-redirects; no AI/grading call in the request handler — verified by `test_mc4_submit_route_attempt_status_is_submitted` and the code grep showing no LLM SDK / `ai-workflows` invocation in `submit_quiz_attempt`). MC-5 satisfied (a `submitted` Attempt is shown as "Submitted — grading not yet available."; `attempt_questions.is_correct`/`.explanation` stay NULL; no fabricated score — verified in e2e). MC-6 satisfied for the manifest portion (the take route reads `content/latex/` read-only via the existing parse path; writes only `data/notes.db`); MC-6's path-specific grep architecture portion remains dormant (no source-layout ADR — unchanged). MC-7 satisfied (no `user_id` anywhere — grep confirms `user_id` appears only in "no user_id" comments; no auth/session). MC-8 satisfied (the take surface makes a `ready` Quiz's Questions takeable + the Attempt persistable; the schema leaves room for grading + composition; no fresh-only-post-first-Quiz path created). MC-9 satisfied (the take surface generates nothing). MC-10 satisfied (`import sqlite3` + SQL literals only under `app/persistence/` — grep confirms none in `app/main.py` or the templates; the take route + `quiz_take.html.j2` call only typed public functions, receiving `QuizAttempt`/`AttemptQuestion`/`Question` dataclasses).
**End-to-end verification (orchestrator re-run, beyond the implementer's Run 004):** seeded a `ready` Quiz (2 Questions) on `ch-01-cpp-refresher#section-1-1` via `request_quiz` → `mark_quiz_generating` → `add_questions_to_quiz` → `mark_quiz_ready`. `GET /lecture/ch-01-cpp-refresher` → 200, `.section-quiz` `ready` entry carries `<a class="section-quiz-take-link" href=".../quiz/{id}/take">`. `GET .../quiz/{id}/take` → 200, renders the M/O designation badge, both coding-task prompts, exactly **2** `<textarea name="response_{question_id}">` code fields, a "Submit Quiz" button; `get_latest_attempt_for_quiz` shows the Attempt `in_progress`. `POST .../quiz/{id}/take` with `response_{qid}` form fields → 303 → `GET .../take`; the Attempt is now `submitted`, `submitted_at` set, each `attempt_questions.response` holds the submitted code verbatim, `is_correct`/`explanation` all NULL. Re-`GET .../take` → 200, "Submitted — grading not yet available." state: 0 `response_` textareas, no "Submit Quiz" button, the learner's code shown read-only, no fabricated score. Non-`ready` Quiz (`requested`) reached via the take URL → 200, no "Submit Quiz" form, "not ready"-style copy. `404` for unknown chapter / unknown `quiz_id`; `422` for a malformed `chapter_id`. Counts: 0 defects in the affected set.
**Pending-human gates filed this phase (Verification gates from the task, recorded `pending human` in the Human-gates table above):**
- `rendered-surface verification (TASK-015 Quiz-taking surface)` — review `tests/playwright/.screenshots/` (or the take page + a Lecture page in a browser) per ADR-010; human flips to `pass` post-commit.
- `take-and-submit sanity (TASK-015)` — manual exploratory pass: human takes a real generated Quiz once and confirms the Attempt persists honestly; flips to `pass` post-commit.
**Output summary:** Phase 5 verify complete — tests green (1037p/8s), conformance clean (0 blockers; MC-3/MC-6 architecture portions dormant as before), e2e verified, two human-only verification gates filed `pending human`. Proceeding to Phase 6 (stage → reviewer → commit).

### Run 008 — reviewer

**Time:** 2026-05-12T08:00:00Z
**Staged files reviewed:** `app/main.py`; `app/persistence/__init__.py`; `app/persistence/connection.py`; `app/persistence/quizzes.py`; `app/static/lecture.css`; `app/static/quiz.css`; `app/templates/base.html.j2`; `app/templates/lecture.html.j2`; `app/templates/quiz_take.html.j2`; `design_docs/architecture.md`; `design_docs/audit/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md`; `design_docs/decisions/ADR-038-quiz-taking-surface.md`; `design_docs/decisions/ADR-039-quiz-attempt-persistence-layer.md`; `design_docs/project_issues/quiz-grading-slice-shape.md`; `design_docs/tasks/TASK-015-quiz-taking-surface-write-code-and-submit-attempt.md`; `tests/playwright/test_task015_quiz_taking_dom.py`; `tests/test_task015_quiz_taking.py`
**Unstaged source/test warning:** none — `git diff --name-only` (unstaged) is empty; the entire change set is staged. No independent-validity concern.
**Conformance skill result:** 0 blockers, 0 warnings, 2 dormant (MC-3 mapping-source architecture portion — no mapping-source ADR, `cannot evaluate (ADR pending)`, unchanged from prior tasks; MC-6 path-specific-grep architecture portion — no source-layout ADR, `cannot evaluate (ADR pending)`, unchanged). MC-1/MC-2/MC-4/MC-5/MC-7/MC-8/MC-9 PASS; MC-3/MC-6 manifest portions PASS; MC-10 PASS (architecture portion active per ADR-022 — `import sqlite3` and SQL literals only under `app/persistence/`; the take route + `quiz_take.html.j2` call only typed public functions, receiving dataclasses). Verified by re-grep against the staged diff: no `import sqlite3` outside `app/persistence/`; no `SELECT`/`INSERT`/`UPDATE`/`DELETE`/`CREATE TABLE`/`CREATE INDEX`/`BEGIN`/`COMMIT`/`ROLLBACK` string literal outside `app/persistence/`; `user_id` appears only in "no user_id" comments; no `grade`/`grading`/`ai-workflows`/LLM-SDK invocation in `submit_quiz_attempt`.
**Architecture leaks found in .md files:** none. `architecture.md` is an index of Accepted ADRs only — the two new rows (ADR-038, ADR-039) and the updated/added prose paragraphs ("Per-Section Quiz surface", "Quiz-taking surface") are summaries of ADR-038/ADR-039 with every concrete name (`app/main.py`, `quiz_take.html.j2`, `app/static/quiz.css`, `data/notes.db`, `ai-workflows`, `get_latest_attempt_for_quiz`) traced to those Accepted ADRs; ADR-039's amendment (the `get_latest_attempt_for_quiz` accessor) is reflected consistently in the ADR (title + §The function set + §Scope + §Amendment) and in `architecture.md`'s ADR-039 row + Quiz-taking-surface paragraph. ADR-038/ADR-039 are Tier-1 binding (Accepted). The task file (Tier 3) carries no architectural claim presented as binding — its forecasts are explicitly "/design's call". The audit file (Tier 5) is an operational record. The project_issue (Tier 3) raises questions, decides nothing. No `ARCHITECTURE LEAK:` block raised.
**End-to-end verification (reviewer re-run via TestClient, beyond Run 004/Run 007):** seeded a `ready` Quiz (2 Questions) on `ch-01-cpp-refresher#section-1-1` via `request_quiz` → `mark_quiz_generating` → `add_questions_to_quiz` → `mark_quiz_ready`. `GET /lecture/ch-01-cpp-refresher` → 200, exactly one `section-quiz-take-link` (the `ready` Quiz; a co-existing `requested` Quiz on a sibling Section gets none). `GET .../quiz/{id}/take` → 200: `designation-badge designation-mandatory`, both coding-task prompts, two `<textarea name="response_{question_id}">` code fields (+ the RHS Notes-rail textarea — `notes-rail` present, three-column shell intact), "Submit Quiz" button, back-link `href=".../#section-1-1-end"` (ADR-031 anchor recipe), `quiz-take-*` CSS classes, header h1 "Quiz — Section 1-1: 1.1 Arrays and Vectors (general concept)"; no `type="radio"` / `type="checkbox"` / option input. A second `GET .../take` reuses the same `in_progress` Attempt (one `quiz_attempts` row). `POST .../take` with `response_{qid}` form fields → 303 → `GET .../take`; Attempt → `submitted`, `submitted_at` set, each `attempt_questions.response` holds the submitted code verbatim, `is_correct`/`explanation` all NULL; re-`GET .../take` → 200, "Submitted — grading not yet available." state with the code read-only, no `quiz-take-form`, no fabricated score/"all correct"/"passed"/"grade:". Non-`ready` (`requested`) Quiz via the take URL → 200, no form, "not ready to take" copy. `404` for unknown chapter / unknown section / unknown `quiz_id` / wrong-section `quiz_id`; `422` for malformed `chapter_id`. Full suite re-run: **1037 passed, 8 skipped, 1 warning** (the pre-existing `langgraph` deprecation warning). Counts: 0 defects in the affected set.
**Blocking findings:** none.
**Non-blocking findings:**
- `app/persistence/list_questions_for_quiz` is re-exported but has no current call site (the take route uses `list_attempt_questions`). ADR-039 §"Why this set" deliberately ships it as a forecast accessor (`start_attempt` may use it internally; future Grade-display / "review this Quiz" surfaces will want it). Recorded as a deliberate ADR-backed decision, not a defect — flagged only so a future reviewer doesn't read it as dead code.
- The take page loads `/static/quiz.css` on every page (via `base.html.j2`), including the landing page that does not need it. ADR-038 §Consequences names this and accepts it (trivially cheap at local single-user scale; consistent with `lecture.css` already being loaded on the landing page). No action.
- The architecture.md "Quiz-taking surface" paragraph is a single very long sentence (~30 lines). It is accurate and ADR-traced, but it is at the limit of what an index summary should carry; if a future task touches this area, consider tightening it. Non-blocking style observation.
**Final result:** READY TO COMMIT
