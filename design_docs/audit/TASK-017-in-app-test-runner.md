# LLM Audit — TASK-017: In-app test runner — the learner runs a Question's test suite against their submitted code, in-app, in a sandboxed subprocess, and sees pass/fail (slice 4 of the Quiz pillar)

**Task file:** `design_docs/tasks/TASK-017-in-app-test-runner.md`
**Started:** 2026-05-12T00:00:00Z
**Status:** Committed
**Current phase:** done

---

## Human gates

| Time | Gate | Result | Notes |
|---|---|---|---|
| 2026-05-12T21:48:00Z | Task reviewed | auto-accepted | /auto run |
| 2026-05-12T22:05:00Z | ADR-042 reviewed | auto-accepted | /auto run |
| 2026-05-12T22:05:00Z | ADR-043 reviewed | auto-accepted | /auto run |
| 2026-05-12T22:05:00Z | ADR-044 reviewed | auto-accepted | /auto run |
| 2026-05-12T22:22:00Z | Tests reviewed | auto-accepted | /auto run — 65 new tests (44 failing red-phase: sandbox/persistence/route/template not built; 21 passing boundary greps; 1 skipped) + 8 Playwright; 851 existing tests still green |
| 2026-05-12T23:05:00Z → 2026-05-13T14:20:00Z | rendered-surface verification — TASK-017 quiz-take test-runner | pass | Human-verified live on the running dev server — the read-only `<pre class="quiz-take-test-suite">` block, the `.quiz-take-run-tests` button, and the `.quiz-take-results` panel all render per ADR-043; the take `<form>` + three-column shell + designation badge unchanged. |
| 2026-05-12T23:05:00Z → 2026-05-13T14:20:00Z | in-app test-runner end-to-end sanity — TASK-017 | pass | Human-verified live end-to-end. The `compile_error` honesty path was exercised on a real generated Question (a `struct Book` collision between the learner's `response` and the test suite's embedded reference impl — the runner surfaced the real `g++` redefinition diagnostic on the take page, not a fabricated pass/fail; MC-5's spirit). The pass path was also exercised and renders the green `.quiz-take-results-pass` block. The bounded caveat from ADR-042 §The splice stands: a *learner-authored* verdict on a real generated Question is still bounded by the `question_gen`-prompt follow-on (`design_docs/project_issues/question-gen-prompt-emit-assertion-only-test-suites.md`) — does not block this gate; the runner is verified correct for assertion-only test suites and honest on the current generator output. |
| 2026-05-12T23:36:00Z | Commit review | auto-accepted | /auto run — reviewer verdict APPROVE / READY TO COMMIT (0 MC blockers, 0 warnings; all 11 programmatic ACs trace to passing tests; ADR-042/043/044 fidelity confirmed; architecture.md regeneration faithful); committed as `d5b183b` |

---

## Agent runs

### Run 001 — architect / Mode 1 `/next`

**Time:** 2026-05-12T00:00:00Z
**Input files read:** `design_docs/MANIFEST.md` (full); `CLAUDE.md` (via project-instructions context); `design_docs/architecture.md` (full); `.claude/skills/manifest-conformance/SKILL.md` (full); `.claude/skills/ui-task-scope/SKILL.md` (referenced — its UI-1..UI-6 rules cited in the task; not re-read this run, applied from prior cycles); `design_docs/tasks/` (all 16 task files — TASK-016 read in full, TASK-015/TASK-014 portions, others by index/title); `design_docs/decisions/` (ADR-040 read in full; ADR-041 via architecture.md's "Quiz domain schema" summary + the index; ADR-033/ADR-036/ADR-037/ADR-038/ADR-039 via the architecture.md project-structure summary + targeted reads; ADR-008/ADR-022/ADR-035 via the index/summary); `design_docs/project_issues/` (all 20 — `in-app-test-runner-slice-shape.md` and `quiz-grading-slice-shape.md` read in full; `tooling-lint-and-type-check.md` referenced; rest by Glob index); `design_docs/audit/TASK-016-questions-carry-a-test-suite.md` (full — the real-engine generation gate's `pass` note and its "Observation for the in-app-test-runner slice" about the test suites embedding reference implementations inline); `~/.claude/.../memory/MEMORY.md` + `memory/project_roadmap_ide_and_known_issues.md` (the roadmap memory — the 2026-05-12 manifest amendment note, the IDE/code-runner "future big feature" framing, the now-stale stated order, the deprioritized Quiz-prompt-rendering bug); `app/persistence/quizzes.py` (full — the `Question`/`QuizAttempt`/`AttemptQuestion` dataclasses, `list_questions_for_quiz`/`list_attempt_questions`/`save_attempt_responses`/`submit_attempt`, the existing `test_suite` carry-through); source layout via Glob (`app/**/*.py`, `app/templates/*`, `app/static/*` — confirmed `app/workflows/` is the AI path, `app/static/quiz.css` exists per ADR-039, no `app/sandbox.py`/`app/runner.py` yet).
**Tools / commands used:** `Read` (MANIFEST.md, architecture.md, SKILL.md, ADR-040, project_roadmap memory, quiz-grading-slice-shape.md, in-app-test-runner-slice-shape.md, TASK-016 task file, TASK-016 audit, quizzes.py); `Glob` (`design_docs/project_issues/*.md`, `design_docs/tasks/*.md`, `app/**/*.py`, `app/templates/*`, `app/static/*`, `design_docs/audit/TASK-016*`); `Write` (this audit file, the task file).
**Files created:** `design_docs/tasks/TASK-017-in-app-test-runner.md`, `design_docs/audit/TASK-017-in-app-test-runner.md`
**Files modified:** none
**Task alternatives considered:** (1) **The in-app test runner** (sandboxed subprocess + "Run tests" route + take-surface affordance + `attempt_questions` test-result columns + the test suite shown read-only) — **chosen**: the conformant next slice per ADR-040's reordering (Questions-carry-tests → in-app test-runner → grading → composition → TTS); the §6-mandated "Code is written, run, and tested within the application" second half (TASK-016 shipped the first half); the precondition for a §8-conformant Grade; resolves the open `in-app-test-runner-slice-shape.md` issue; a focused-but-substantial vertical slice. (2) **The grading slice** (out-of-band grading processor + `grade_attempt` workflow + Grade aggregate + Weak Topics + `notifications`) — rejected as **still out of order**: §8 Grade now reads "correctness determined by whether the learner's code passed the Question's tests" but nothing runs the tests yet and `attempt_questions` has no test-result columns; a grading slice before the runner would either contradict §8 (AI judges correctness by reading code) or stub against columns that don't exist; the chain is in-app-test-runner (this task) → grading (next). (3) **A full in-browser IDE / "VS Code online" surface in one task** — rejected as multi-session and over-scoped: "VS Code online" was a roadmap *reference point, not a literal spec*; §6 is satisfied by the `<textarea>` + "Run tests" + results panel; bundling a rich editor with the sandbox ADR (the hard part) is 2–3 sessions with no demonstrable halfway point; split — TASK-017 ships the sandbox + "Run tests" on the existing take page; a richer editing surface is a later separate consideration. (4) **The sandbox primitive alone (a "spike")** — rejected as a horizontal layer / non-slice: a sandbox with no caller and no UI delivers nothing the human can see; §6's "the learner answers a Question by … running tests against it — all inside the application's own interface" isn't true without an in-app affordance. (5) **The `attempt_questions` test-result columns + a stub runner** — rejected: a stub that fabricates a result violates MC-5's spirit and §6; the columns with nothing real to fill them is the schema-only non-slice. (6) **The lint/type-check tooling ADR + task** (`tooling-lint-and-type-check.md`, Open, 11th+ recurrence) — rejected: feature-precondition work remains (the loop is not closed); standing recommendation is "not while there's a feature slice"; re-flagged in the task's "Architectural concerns", not actioned. (7) **The deprioritized Quiz-prompt-rendering bug** (`{{ aq.prompt }}` autoescaped — literal backticks) — rejected: explicitly deprioritized by the user 2026-05-12; *adjacent* to this task (same `quiz_take.html.j2` surface, ADR-040's prompts now name `` `void append(...)` ``-style signatures) but the user's deprioritization stands; flagged so `/design` doesn't quietly fold it in.
**Decisions surfaced:** the task forecasts **two to three ADRs** for `/design TASK-017` — (A) **The in-app code-execution sandbox** (the isolation mechanism — a bare subprocess with `resource` limits + a timeout + a restricted temp `cwd` (forecast) vs a `bubblewrap`/`nsjail`/`firejail` jail vs a container vs a WASM/`pyodide` in-browser sandbox; the test-suite/learner-code splice — ADR-040's generated test suites currently embed a reference implementation inline, so the runner must decide replace-the-placeholder-body vs compile-against-a-harness, working with ADR-040's *current* output; the supported languages — C++ per ADR-040's `question_gen` prompt steer, Python if a `test_suite` targets it; the structured result shape — passed / output / status (ran|timed_out|compile_error|setup_error); the `pyproject.toml` question — a sandbox dependency or none); (B) **The "Run tests" route + the take-surface test-runner affordance** (the route shape — synchronous form-POST + PRG re-render vs an async `fetch()` JSON endpoint + a small JS handler — *and the explicit reasoning that MC-4 does not bind it (running tests is not AI work) and §6's "all inside the application's own interface" reads as wanting a responsive run-and-see loop*; if JS is chosen, *that JS* decided on its merits per ADR-035 — the project's first JS asset; the `quiz_take.html.j2` changes — a read-only test-suite block, a "Run tests" affordance, a results panel; the `quiz-take-*` CSS additions in `quiz.css`; whether the `submitted`-Attempt state changes); (C) **The `attempt_questions` test-result persistence layer** (may be a section of (A) or (B) — `/design`'s call) (the column shape — `test_passed` / `test_output` / `test_run_at` (forecast) vs an `attempt_question_runs` table for run-history; whether `is_correct` is set by the runner or by the grading slice (forecast: by the grading slice); the `save_attempt_test_result`/equivalent signature; the new `questions.test_suite` accessor (`get_question` / `get_test_suite_for_question`) — ADR-041 §No new accessor said "the runner slice adds one"; the `AttemptQuestion` (or new) dataclass change; `list_attempt_questions` carrying the test-result fields; the column DDL in `connection.py` via the `_apply_additive_migrations` `PRAGMA table_info` check, mirroring ADR-037's `generation_error` and ADR-041's `questions.test_suite`; additive per ADR-022; no `user_id` — MC-7). This `/design` **resolves** the open `in-app-test-runner-slice-shape.md` project_issue (it's the task that forces it). `/design` may file a new `project_issue` if it surfaces a question this slice doesn't need (e.g. "should `question_gen`'s prompt change to emit assertion-only test files?" — a future TASK if so) — not an ADR. `quiz-grading-slice-shape.md` is **not** edited this task (the grading slice's own `/design` updates it to reflect "correctness is a test-execution result the in-app runner produces"; no decision here forces it).
**Architecture leaks found:** none. (`architecture.md`'s "Quiz-taking surface" and "Quiz domain schema" paragraphs trace to Accepted ADRs (ADR-038/ADR-039 and ADR-033 respectively) and will regenerate mechanically when this task's ADR(s) are Accepted. The roadmap *memory* file's stated order — grading → composition → TTS → IDE — is stale relative to the 2026-05-12 manifest amendment + ADR-040's reordering, but a memory file is not a project `.md` the architect owns or that carries authority — re-flagged in the task's "Architectural concerns" so the human can update the roadmap note; not an ARCHITECTURE LEAK — it does not introduce an architectural claim into a project `.md`.) CLAUDE.md and the conformance skill are clean for this task's purposes — no architectural content leaked in; no guardrail this task would force a violation of (MC-1 stays vacuously clean — the new sandbox/runner module is under `app/` but not `app/workflows/`, and adds no AI; MC-4 is reasoned-around-explicitly, not violated — running tests is not AI work; MC-6 is the load-bearing one for the sandbox and the task ACs pin it).
**Pushback raised:** five architectural concerns in the task — (1) **the sandbox ADR is the genuinely hard decision** (running arbitrary learner code, bounded by the single-user/single-machine/locally-run threat model; the forecast — a bare subprocess with `resource` limits + a timeout + a restricted temp `cwd` — is probably right but deserves a real `/design`, not a rubber-stamp; a `bubblewrap`-style jail a reasonable upgrade) **plus the splice complication ADR-040 left behind** (the generated test suites currently embed a reference implementation inline — the runner must work with that, can't depend on a `question_gen` change that hasn't happened; if `/design` concludes `question_gen`'s prompt should change, that's a future TASK — file a project_issue); (2) **MC-4 does NOT bind the test-run path** — `/design` must reason this explicitly, not reflexively copy ADR-037's async pattern (running a test suite is not AI work; §6 reads as wanting a responsive write→run→see loop; the forecast is synchronous-with-a-timeout; the ADR should say *why* MC-4 doesn't apply); (3) **this task likely adds the project's first client-side JavaScript** — and that's fine, decided on its merits in the take-surface ADR per ADR-035 (a full-page reload that discards in-progress edits in other textareas on a multi-Question page is arguably not "clean and sufficient", which is a legitimate basis for choosing JS); the parked ADR-032 is *not* activated by this — different surface, different decision; (4) **the amended manifest's Quiz-pillar reorder** — the roadmap memory's stated order is still stale; this task is the in-app-test-runner step ADR-040's "My recommendation" already established; re-flagged so `/design`'s ADR records it, the human can update the roadmap note, and the grading slice's future `/design` updates `quiz-grading-slice-shape.md`; no `> MANIFEST TENSION:` — the manifest is internally consistent, the codebase is incomplete relative to the amendment; (5) **`tooling-lint-and-type-check.md`** (Open, 11th+ recurrence) — re-flagged, not actioned (not while there's a feature slice; `pyproject.toml` exists since TASK-014; a sandbox module is exactly the kind of code where a type-checker earns its keep). No `> MANIFEST TENSION:`, no `> NEEDS HUMAN:`, no `> PRIMARY OBJECTIVE COMPLETE:` — the primary objective (the reinforcement loop) is not complete (no grading, no composition); this task advances it (the §6 in-app run-and-test second half + the raw correctness signal the grading slice needs).
**Output summary:** Proposed TASK-017 — "In-app test runner" — the learner runs a Question's test suite against their submitted code, in-app, in a sandboxed subprocess, and sees pass/fail; the §6-mandated "Code is written, run, and tested within the application" second half (TASK-016 shipped the first half — the tests exist) and the precondition for a §8-conformant Grade; resolves the open `in-app-test-runner-slice-shape.md` issue; chosen over the grading slice (still out of order per the amended manifest) and a monolithic in-browser-IDE slice (multi-session — split). Two-to-three ADRs forecast: the sandbox/code-execution ADR (the hard one — the isolation mechanism + the test-suite/learner-code splice + the supported languages + the result shape), the "Run tests" route + take-surface ADR (the route shape + the explicit MC-4-doesn't-apply reasoning + the take-page changes + possibly the project's first JS asset per ADR-035), and the `attempt_questions` test-result persistence ADR (additive columns; the new `questions.test_suite` accessor; `is_correct`'s source — by the grading slice, forecast). UI task — `## Verification gates (human-only)` carries the rendered-surface review + a real-engine end-to-end run-tests check.

### Run 002 — design (architect) / Mode 2 `/design TASK-017`

**Time:** 2026-05-12T22:00:00Z (approx — `/auto` cycle)
**Input files read:** `design_docs/MANIFEST.md` (full); `CLAUDE.md` (via project-instructions context); `.claude/skills/manifest-conformance/SKILL.md` (full); `design_docs/architecture.md` (full); `design_docs/decisions/` (index via Glob — ADR-001..ADR-041 present; ADR-040 read in full via TASK-016-context targeted Greps for the test-suite representation / reference-impl-embedding / `prompt` "name the implementation target" / scope-deferral; ADR-008/ADR-022/ADR-033/ADR-035/ADR-036/ADR-037/ADR-038/ADR-039/ADR-041 via the architecture.md project-structure summary + the index; ADR-031 via the summary); `design_docs/project_issues/` (full Glob — top-level + `Resolved/`; `in-app-test-runner-slice-shape.md` read in full — the one this task resolves; `quiz-grading-slice-shape.md` and `tooling-lint-and-type-check.md` referenced); `design_docs/tasks/TASK-017-in-app-test-runner.md` (full — read in offset chunks due to file size); `design_docs/audit/TASK-017-in-app-test-runner.md` (full — Run 001) + `design_docs/audit/TASK-016-questions-carry-a-test-suite.md` (the `ai-workflows generation sanity (with test suites)` gate row — the "Observation for the in-app-test-runner slice": each generated `test_suite` currently embeds a reference implementation inline); source: `app/persistence/quizzes.py` (full — the `Question`/`QuizAttempt`/`AttemptQuestion` dataclasses, `list_questions_for_quiz`/`list_attempt_questions`/`save_attempt_responses`/`submit_attempt`/`start_attempt`/`get_quiz`, the existing `test_suite` carry-through), `app/persistence/connection.py` (full — `_SCHEMA_SQL`'s `CREATE TABLE attempt_questions` block, `_apply_additive_migrations`'s `PRAGMA table_info` checks for `generation_error` / `test_suite`), `app/persistence/__init__.py` (full — the public-API re-exports + `__all__`), `app/templates/quiz_take.html.j2` (full — the `not_ready` / `submitted` / `in_progress` branches, the per-Question `prompt` + `<textarea name="response_{id}">`, the take `<form action=".../take">` + "Submit Quiz" button), `app/static/quiz.css` (full — the `quiz-take-*` namespace), `app/templates/base.html.j2` (full — the three-column shell, `base.css`/`lecture.css`/`quiz.css` loaded flat, no `<script>` beyond MathJax), `pyproject.toml` (full — stdlib-only deps + `jmdl-ai-workflows>=0.4`; `requires-python >= 3.12`), `app/main.py` (lines ~560–837 — the `POST .../quiz` route, the `GET .../take` and `POST .../take` take routes, the path-param validation pattern, the PRG-redirect pattern).
**Tools / commands used:** `Read` (MANIFEST.md, SKILL.md, architecture.md, TASK-017 task file in offset chunks, TASK-017 audit, in-app-test-runner-slice-shape.md, quizzes.py, connection.py, persistence `__init__.py`, quiz_take.html.j2, quiz.css, base.html.j2, pyproject.toml, app/main.py §560–837); `Grep` (TASK-016 audit for reference-impl observation; ADR-040 for the test-suite representation / `prompt` / scope; `app/main.py` for the take-route names/locations); `Glob` (`design_docs/decisions/ADR-*.md`, `design_docs/project_issues/**/*.md`); `Write` (the three ADRs + the new project_issue); `Edit` (`in-app-test-runner-slice-shape.md` → Resolved; `architecture.md` → 3 Proposed-ADR rows; this audit file).
**Files created:** `design_docs/decisions/ADR-042-in-app-code-execution-sandbox.md`, `design_docs/decisions/ADR-043-run-tests-route-and-take-surface-affordance.md`, `design_docs/decisions/ADR-044-attempt-question-test-result-persistence.md`, `design_docs/project_issues/question-gen-prompt-emit-assertion-only-test-suites.md`
**Files modified:** `design_docs/architecture.md` (3 rows added to "Proposed ADRs (awaiting human acceptance)" — ADR-042/ADR-043/ADR-044; nothing moved to Accepted; no project-structure-summary change — the no-op rule: no ADR state change yet, the summary regenerates only after the human gate); `design_docs/project_issues/in-app-test-runner-slice-shape.md` (`Status: Open` → `Status: Resolved by ADR-042 + ADR-043 + ADR-044`; a one-paragraph Resolution note at the top + a rewritten `## Resolution` section); `design_docs/audit/TASK-017-in-app-test-runner.md` (header `Status: In progress` → `Blocked`, `Current phase: next` → `design`; this Run 002 entry)
**ADRs proposed:** `ADR-042` — the in-app code-execution sandbox: a bare subprocess under POSIX `resource` rlimits + a `subprocess.run(timeout=...)` wall-clock cap in a throwaway temp `cwd` (no container/jail/WASM — the lean fit for the §5 single-user/single-machine/locally-run threat model; `bubblewrap` the documented future upgrade behind the same `run_test_suite` interface); C++ via `g++` primary, Python via `python3` secondary (sniffed), unrecognized → `setup_error`; the splice = concatenate `learner_response + test_suite` into one TU (correct for assertion-only test suites; ADR-040's *current* reference-impl-embedding output → an honest `compile_error` — body-replacement splicing rejected as too fragile; a new project_issue tracks fixing the *generator*); `RunResult(status, passed, output)` shape; no `pyproject.toml` dependency; MC-1 vacuously clean (`app/sandbox.py` not under `app/workflows/`, no AI), MC-4 reasoned around explicitly (running tests is not AI work), MC-6 load-bearing (the child runs `cwd=` a temp dir; the corpus path is never passed to it). `ADR-043` — the "Run tests" route + take-surface affordance: a **synchronous** `POST .../take/run-tests` form-POST that submits the whole take form, saves all responses, runs one Question's test suite, persists the result, PRG-redirects to `GET .../take#question-{id}` with a `scroll-margin-top` anchor (ADR-031's no-relocate recipe) — **no JavaScript** (no-JS is the better fit on its merits per ADR-035 — the async-`fetch()`+JS variant's only win, in-place panel update, doesn't justify the project's first JS dependency; the parked ADR-032 is not activated — different surface); `quiz_take.html.j2` gains per `in_progress` Question a read-only `<pre class="quiz-take-test-suite">` (the test suite **is shown read-only** — it's the spec), a `<button formaction=".../run-tests" name="question_id">Run tests</button>`, and a `.quiz-take-results` panel; the `submitted` state **additionally shows the last test result read-only** (the honest §8 record — no fabricated Grade/score/correctness); new `quiz-take-*` rules in `app/static/quiz.css` (no `base.css` change, no new CSS file); the per-Section `ready`-entry caption on the Lecture page **unchanged**; MC-4 reasoned around, MC-5's spirit (a sandbox failure shown honestly), MC-9 (no Quiz generated), MC-10 (typed persistence functions only). `ADR-044` — the `attempt_questions` test-result persistence layer: **four nullable additive columns** (`test_passed INTEGER`, `test_status TEXT`, `test_output TEXT`, `test_run_at TEXT`) via the `_apply_additive_migrations` `PRAGMA table_info` check + the `CREATE TABLE attempt_questions` block (additive per ADR-022 — no migration-trigger; mirrors ADR-037's `generation_error` / ADR-041's `test_suite`) — denormalized "latest run", **not** an `attempt_question_runs` table; **`is_correct` set by the grading slice** (reading the persisted test result and adding the explanation/aggregate alongside), not by the runner (keeps the Grade coherent — no half-graded state); new `save_attempt_test_result(attempt_id, question_id, *, passed, status, output, run_at=None)` + `get_question(question_id) -> Question | None` (the runner-slice accessor ADR-041 §No new accessor forecast — the reusable whole-Question form); the `AttemptQuestion` dataclass gains `test_suite` + the four `test_*` fields; `list_attempt_questions` carries them through; re-exported from `app/persistence/__init__.py`; SQL stays under `app/persistence/` (MC-10), no `user_id` (MC-7). (Three ADRs, not two — the persistence change is tightly coupled but clean enough to stand alone and the sandbox/route/persistence split mirrors how TASK-014 → ADR-036+ADR-037 and TASK-016 → ADR-040+ADR-041.)
**Project issues opened/resolved:** opened `question-gen-prompt-emit-assertion-only-test-suites.md` (the `question_gen` prompt should change to emit assertion-only test suites — reference the implementation target by name, don't define it — so the runner's concatenate splice works on real generated Questions; ADR-042 §The splice files it; TASK-017's real-engine end-to-end gate depends on the follow-on landing or being filled against a hand-written test suite — surfaced as upflow, the human picks the ordering; a future TASK, not folded into TASK-017 per TASK-017 §Out of scope). Resolved `in-app-test-runner-slice-shape.md` → `Resolved by ADR-042 + ADR-043 + ADR-044` with a one-paragraph resolution note recording the choices (the bare-subprocess sandbox, synchronous-no-JS run, the four denormalized test-result columns, `is_correct` by the grading slice, the test suite shown read-only, the per-Section caption unchanged) + the new project_issue + the dependency on it. Did **not** edit `quiz-grading-slice-shape.md` (no decision in the runner slice forces it — the grading slice's own `/design` updates it; noted there for that future cycle). Re-flagged (not actioned) `tooling-lint-and-type-check.md` (Open, 11th+ recurrence) in each ADR's "My recommendation vs the user's apparent preference" — not this task.
**architecture.md changes:** 3 rows added to "Proposed ADRs (awaiting human acceptance)" — ADR-042/ADR-043/ADR-044, each with `Awaiting human acceptance` notes and the consumed-ADR list. Nothing moved to "Accepted ADRs". No project-structure-summary change (no-op rule — the summary regenerates from the Accepted-ADR set only when that set changes, i.e. after the human gate). No "Pending resolution" / "Superseded" change. No substantive architecture written into architecture.md — only the rows.
**Write-path check:** clean. Only `design_docs/decisions/ADR-042-*.md`, `design_docs/decisions/ADR-043-*.md`, `design_docs/decisions/ADR-044-*.md`, `design_docs/project_issues/question-gen-prompt-emit-assertion-only-test-suites.md` (created), `design_docs/architecture.md`, `design_docs/project_issues/in-app-test-runner-slice-shape.md`, `design_docs/audit/TASK-017-in-app-test-runner.md` (modified). No file outside `design_docs/{tasks,audit,decisions,project_issues}/**` created or modified — no `app/` edit, no `tests/` edit, no `CLAUDE.md` / `MANIFEST.md` / skill edit, no scratch file. Nothing to remediate.
**Architecture leaks found:** none. (architecture.md's "Quiz-taking surface" / "Quiz domain schema" / "AI integration and Quiz generation" paragraphs all trace to Accepted ADRs (ADR-038/ADR-039, ADR-033, ADR-036/ADR-037) and will regenerate mechanically when this task's ADRs are Accepted. CLAUDE.md and the conformance skill are clean for this task — no architectural content leaked in; no guardrail this task would force a violation of. The roadmap *memory* file's stated Quiz-pillar order (grading → composition → TTS → IDE) is stale relative to the 2026-05-12 manifest amendment + ADR-040's reordering — re-flagged in each ADR's "My recommendation" for the human to update the memory note; not an ARCHITECTURE LEAK — a memory file is not a project `.md` the architect owns and it introduces no architectural claim into a project `.md`.)
**Pushback raised:** one upflow surfaced — **the runner's concatenate splice (ADR-042) is incompatible with ADR-040's *current* generated output** (reference-impl-embedding test suites → an honest `compile_error`, never a fabricated result, so MC-5's spirit is intact, but not the responsive pass/fail loop §6 wants on a real generated Question). The architect built the runner right (concatenate; correct for assertion-only test suites — what the tests use, what a fixed prompt produces) rather than bolt a fragile body-replacement splice into it or fold a `question_gen` change into TASK-017 (out of scope). Recorded in ADR-042 §The splice + §My recommendation, and as a new project_issue (`question-gen-prompt-emit-assertion-only-test-suites.md`); **TASK-017's real-engine end-to-end gate depends on the follow-on landing or being filled against a hand-written test suite — the human picks the ordering.** No `> MANIFEST TENSION:` (the manifest is internally consistent; the gap is between ADR-040's current generator output and the runner's expectation — between two ADRs / a project_issue, not with the manifest), no `> NEEDS HUMAN:` (no ADR is `Pending Resolution` — all three are decidable now; the upflow is a scheduling question for the human, not a blocking architectural unknown), no `ARCHITECTURE LEAK:`. The architect's positions vs the user's apparent direction (per the ADRs' "My recommendation" sections): aligned with the task's forecast and ADR-040's reordering throughout — the bare-subprocess sandbox (forecast), synchronous-no-JS run (forecast leaned synchronous; the architect picked no-JS on the merits, not as a posture), the four denormalized test-result columns (forecast's three + `test_status` for honesty), `is_correct` by the grading slice (forecast), the test suite shown read-only (forecast), no `pyproject.toml` dependency (forecast); the roadmap memory's "VS Code online" framing is a reference point not a literal spec — this slice ships a "Run tests" affordance on the existing take page, not an IDE (ADR-040 §My recommendation, TASK-017 §Alternatives — aligned).
**Implementation blocked pending human acceptance:** yes — ADR-042, ADR-043, ADR-044 are all `Status: Proposed`; `/implement TASK-017` cannot proceed past the design gate until the human (or the `/auto` gate) flips them to `Accepted`. (No `> NEEDS HUMAN:` — there is no `Pending Resolution` ADR; the only thing for the human beyond the routine accept-gate is the scheduling call on the new `question-gen` project_issue vs TASK-017's real-engine gate, recorded in ADR-042.)
**Output summary:** `/design TASK-017` produced three Proposed ADRs (ADR-042 the in-app code-execution sandbox — bare subprocess + rlimits + timeout + temp `cwd`, C++ primary / Python secondary, concatenate splice, `RunResult` shape, no new dep; ADR-043 the synchronous no-JS `POST .../take/run-tests` route + the take-surface read-only test-suite block / "Run tests" button / results panel + the `submitted`-state last-result display; ADR-044 the four additive `attempt_questions` test-result columns + `save_attempt_test_result` + `get_question` + the `AttemptQuestion` extension, `is_correct` by the grading slice), resolved `in-app-test-runner-slice-shape.md`, filed `question-gen-prompt-emit-assertion-only-test-suites.md` (the one upflow — the generator should emit assertion-only test suites; TASK-017's real-engine gate depends on it), added 3 rows to architecture.md's Proposed table. Implementation blocked pending human acceptance of all three ADRs.

### Run 003 — test (test-writer)

**Time:** 2026-05-12T23:45:00Z
**Input files read:**
- `design_docs/tasks/TASK-017-in-app-test-runner.md` (full — ACs 1–11 + Verification gates)
- `design_docs/decisions/ADR-042-in-app-code-execution-sandbox.md` (full)
- `design_docs/decisions/ADR-043-run-tests-route-and-take-surface-affordance.md` (full)
- `design_docs/decisions/ADR-044-attempt-question-test-result-persistence.md` (full)
- `design_docs/MANIFEST.md` (full — §5 non-goals, §6 behaviors, §7 invariants, §8 glossary)
- `.claude/skills/manifest-conformance/SKILL.md` (full — MC-1, MC-5, MC-6, MC-7, MC-10)
- `CLAUDE.md` (via project-instructions context)
- `design_docs/architecture.md` (index only — for ADR state)
- `app/persistence/__init__.py` (public API surface — re-exports, `__all__`)
- `tests/conftest.py` (full — env-var isolation pattern, deferred-import pattern)
- `tests/test_task015_quiz_taking.py` (partial — `_bootstrap`, `_seed_ready_quiz_with_questions`, raw sqlite3 insert helpers)
- `tests/playwright/conftest.py` (full — `live_server` fixture shape)
- `pyproject.toml` (full — existing `task` marker registration, deps)
**Tools / commands used:** `Read`, `Bash` (pytest runs, python3 syntax checks, shutil.which check)
**Files created:**
- `tests/test_task017_sandbox.py`
- `tests/test_task017_persistence.py`
- `tests/test_task017_run_tests_route.py`
- `tests/playwright/test_task017_quiz_take_runner_dom.py`
**Files modified:** none (`task` marker already registered in `pyproject.toml`; `tests/conftest.py` unchanged)

**Tests added (AC mapping):**

*test_task017_sandbox.py — AC-1 (sandbox primitive)*
- `test_sandbox_passing_cpp_response_returns_ran_true` → AC-1: `status='ran'`, `passed=True` for correct C++ response
- `test_sandbox_failing_cpp_response_returns_ran_false` → AC-1: `status='ran'`, `passed=False` for wrong C++ response
- `test_sandbox_passed_false_has_output` → AC-1 (boundary): output non-empty when passed=False
- `test_sandbox_status_ran_passed_true_no_corpus_write` → AC-1 + MC-6: corpus snapshot unchanged after passing run
- `test_sandbox_status_ran_passed_false_no_corpus_write` → AC-1 + MC-6: corpus snapshot unchanged after failing run
- `test_sandbox_infinite_loop_returns_within_bounded_wall_clock` → AC-1 (edge + performance): timed_out within 30 s wall-clock
- `test_sandbox_compile_error_has_diagnostic_in_output` → AC-1 (negative): compile_error, passed=None, output has diagnostic
- `test_sandbox_unrecognized_language_setup_error` → AC-1 (negative + edge): gibberish language → setup_error, passed=None
- `test_sandbox_corpus_write_attempt_blocked` → MC-6 (negative): test suite that writes to corpus path → corpus unchanged
- `test_sandbox_reference_impl_embedding_gives_compile_error` → AC-1 (edge — ADR-040 compatibility): reference-impl-embedding test suite → compile_error (not timed_out or setup_error)
- `test_sandbox_python_passing_response` → AC-1 (boundary — secondary language): Python passing → ran, True
- `test_sandbox_python_failing_response` → AC-1 (boundary — secondary language): Python failing → ran, False
- `test_sandbox_empty_response_with_compile_error` → AC-1 (edge — empty input): empty string → compile_error
- `test_sandbox_invalid_cpp_syntax_in_test_suite` → AC-1 (edge — bad test suite): syntax error in test suite → compile_error
- `test_sandbox_passed_none_for_compile_error` → AC-1 + MC-5 (negative invariant): passed is always None when status != 'ran'
- `test_sandbox_passed_none_for_setup_error` → AC-1 + MC-5 (negative invariant): passed is always None for setup_error
- `test_sandbox_module_no_sqlite3_import` → MC-10 (boundary — no SQL in sandbox): `import sqlite3` absent from app/sandbox.py
- `test_sandbox_module_no_forbidden_sdk_import` → MC-1 (boundary — no AI SDK): forbidden SDK imports absent from app/sandbox.py
- `test_sandbox_module_not_under_app_workflows` → MC-1 (boundary — placement): app/sandbox.py NOT under app/workflows/
- `test_sandbox_passing_run_completes_within_5s` → AC-1 (performance): simple passing run ≤ 5 s wall-clock

*test_task017_persistence.py — AC-4 + AC-5*
- `test_save_attempt_test_result_writes_all_four_columns` → AC-4: all four columns written by save_attempt_test_result
- `test_save_attempt_test_result_passed_false` → AC-4 (boundary): passed=False persisted correctly
- `test_save_attempt_test_result_passed_none_for_timed_out` → AC-4 + MC-5: passed=None stored as NULL for non-'ran' status
- `test_save_attempt_test_result_does_not_touch_is_correct_or_explanation` → AC-4 (negative): is_correct/explanation unchanged
- `test_save_attempt_test_result_does_not_change_quiz_attempts_status` → AC-4 (negative): quiz_attempts.status unchanged
- `test_get_question_returns_question_with_test_suite` → AC-5: get_question returns Question with test_suite populated
- `test_get_question_returns_none_for_unknown_id` → AC-5 (negative): get_question returns None for missing id
- `test_list_attempt_questions_carries_test_suite_field` → AC-4 (boundary): list_attempt_questions includes .test_suite
- `test_list_attempt_questions_carries_all_four_test_result_fields` → AC-4: all four test_* fields on AttemptQuestion
- `test_attempt_question_fields_none_before_any_run` → AC-4 (boundary — before first run): all test_* fields are None
- `test_save_attempt_test_result_no_op_for_unknown_pair` → AC-4 (edge): unknown attempt_id/question_id — no exception
- `test_save_attempt_test_result_overwrites_previous_result` → AC-4 (edge — second run): second call overwrites first
- `test_save_attempt_test_result_persists_across_fresh_connection` → AC-4 (edge — durability): result survives new DB connection
- `test_four_columns_exist_on_fresh_db` → AC-4: DDL has all four columns on fresh schema
- `test_four_columns_added_on_existing_db_missing_them` → AC-4 (boundary — additive migration): pre-TASK-017 schema gains columns after init_schema
- `test_save_attempt_test_result_importable_from_persistence_init` → AC-4 + MC-10: function re-exported from app/persistence/__init__.py
- `test_get_question_importable_from_persistence_init` → AC-5 + MC-10: function re-exported from app/persistence/__init__.py
- `test_no_user_id_on_attempt_questions` → MC-7 (negative): PRAGMA table_info confirms no user_id column on attempt_questions
- `test_mc7_no_user_id_in_new_persistence_functions` → MC-7 (boundary): regex scan of persistence source for code-level user_id token
- `test_save_attempt_test_result_for_many_questions_within_budget` → AC-4 (performance): 20 questions, all persisted in ≤ 5 s

*test_task017_run_tests_route.py — AC-2 + AC-3 + AC-6 + AC-7 + AC-8 + AC-9 + AC-10 + AC-11*
- `test_run_tests_route_happy_path_303_redirect` → AC-2 + AC-3: POST run-tests → 303 to GET .../take#question-{id}
- `test_run_tests_route_happy_path_result_rendered_on_take_page` → AC-3 (boundary — after redirect): test result visible in HTML
- `test_run_tests_route_persists_test_result` → AC-2 + AC-4: result written to DB after run
- `test_take_page_shows_test_suite_block_per_question` → AC-6: pre.quiz-take-test-suite in HTML for in_progress Quiz
- `test_take_page_shows_run_tests_button_per_question` → AC-7: button.quiz-take-run-tests with formaction in HTML
- `test_take_page_shows_results_panel_per_question` → AC-8: div.quiz-take-results in HTML for in_progress Quiz
- `test_run_tests_route_unknown_chapter_404` → AC-2 (negative): unknown chapter → 404/422
- `test_run_tests_route_unknown_section_404` → AC-2 (negative): unknown section → 404
- `test_run_tests_route_wrong_quiz_for_section_404` → AC-2 (negative): quiz not belonging to section → 404
- `test_run_tests_route_non_ready_quiz_rejects` → AC-2 (negative): non-ready quiz → error response
- `test_run_tests_route_get_method_rejected` → AC-2 (negative): GET on POST-only route → 404/405
- `test_run_tests_route_no_new_sqlite3_in_main` → MC-10 (boundary): no new `import sqlite3` in app/main.py
- `test_mc1_no_forbidden_sdk_in_app_dir` → MC-1 (negative): no forbidden AI SDK imports in any app/ module
- `test_no_new_js_file_in_static` → AC-10 (negative): no .js file in app/static/
- `test_no_script_tag_in_take_page` → AC-10 (negative): no <script> tag in quiz_take.html.j2
- `test_quiz_css_is_the_only_new_css_file` → AC-11 (negative): only {base.css, quiz.css, lecture.css} in app/static/
- `test_workflows_dir_unchanged_by_task017` → AC-9 (boundary): app/workflows/ module list unchanged

*tests/playwright/test_task017_quiz_take_runner_dom.py — AC-6 + AC-7 + AC-8 (rendered DOM)*
- `test_take_page_renders_test_suite_block` → AC-6 (end-to-end): live browser — pre.quiz-take-test-suite present
- `test_take_page_renders_run_tests_button` → AC-7 (end-to-end): live browser — button.quiz-take-run-tests present
- `test_take_page_renders_results_panel` → AC-8 (end-to-end): live browser — .quiz-take-results present
- `test_take_page_existing_form_unchanged` → AC-6 (boundary): existing response textareas still present
- `test_take_page_no_script_tag` → AC-10 (end-to-end): no <script> tag in live DOM
- `test_take_page_three_column_shell_unchanged` → AC-9 (negative): three-column shell structure preserved
- `test_run_tests_button_click_shows_results_panel` → AC-3 (end-to-end): click "Run tests" → results panel visible
- `test_submitted_state_no_run_tests_button` → AC-7 (boundary — submitted state): run-tests button absent after submit

**Coverage matrix:**
- Boundary: `test_sandbox_passing_cpp_response_returns_ran_true`, `test_sandbox_python_passing_response`, `test_sandbox_python_failing_response`, `test_sandbox_passed_false_has_output`, `test_sandbox_module_no_sqlite3_import`, `test_sandbox_module_not_under_app_workflows`, `test_save_attempt_test_result_passed_false`, `test_attempt_question_fields_none_before_any_run`, `test_four_columns_exist_on_fresh_db`, `test_four_columns_added_on_existing_db_missing_them`, `test_no_user_id_on_attempt_questions`, `test_mc7_no_user_id_in_new_persistence_functions`, `test_run_tests_route_no_new_sqlite3_in_main`, `test_submitted_state_no_run_tests_button`, `test_list_attempt_questions_carries_test_suite_field`, `test_workflows_dir_unchanged_by_task017`
- Edge: `test_sandbox_infinite_loop_returns_within_bounded_wall_clock` (timed_out path), `test_sandbox_corpus_write_attempt_blocked` (MC-6 active enforcement), `test_sandbox_reference_impl_embedding_gives_compile_error` (ADR-040 compatibility surface), `test_sandbox_empty_response_with_compile_error` (empty input), `test_sandbox_invalid_cpp_syntax_in_test_suite` (malformed test suite), `test_save_attempt_test_result_no_op_for_unknown_pair` (phantom IDs), `test_save_attempt_test_result_overwrites_previous_result` (second run overwrites), `test_save_attempt_test_result_persists_across_fresh_connection` (durability across new connection), `test_run_tests_route_wrong_quiz_for_section_404` (cross-section quiz)
- Negative: `test_sandbox_compile_error_has_diagnostic_in_output`, `test_sandbox_unrecognized_language_setup_error`, `test_sandbox_passed_none_for_compile_error`, `test_sandbox_passed_none_for_setup_error`, `test_sandbox_module_no_forbidden_sdk_import`, `test_save_attempt_test_result_passed_none_for_timed_out`, `test_save_attempt_test_result_does_not_touch_is_correct_or_explanation`, `test_save_attempt_test_result_does_not_change_quiz_attempts_status`, `test_get_question_returns_none_for_unknown_id`, `test_run_tests_route_unknown_chapter_404`, `test_run_tests_route_unknown_section_404`, `test_run_tests_route_non_ready_quiz_rejects`, `test_run_tests_route_get_method_rejected`, `test_mc1_no_forbidden_sdk_in_app_dir`, `test_no_new_js_file_in_static`, `test_no_script_tag_in_take_page`, `test_take_page_no_script_tag`, `test_quiz_css_is_the_only_new_css_file`
- Performance: `test_sandbox_infinite_loop_returns_within_bounded_wall_clock` (30 s wall-clock budget — catches unbounded waits), `test_sandbox_passing_run_completes_within_5s` (5 s budget — catches setup overhead regressions), `test_save_attempt_test_result_for_many_questions_within_budget` (20 questions ≤ 5 s — catches O(n) DB regression)

**Pytest red result:**
- Collected from `tests/test_task017_sandbox.py` + `tests/test_task017_persistence.py` + `tests/test_task017_run_tests_route.py`: **44 failing, 21 passing, 1 skipped** in ~9.8 s
- Playwright tests (`tests/playwright/test_task017_quiz_take_runner_dom.py`): not included in the count above (require live server and browser — covered by the live_server fixture; they will fail at collection when playwright is not installed or the feature doesn't exist)
- All 44 failures are correct TDD failures:
  - `test_task017_sandbox.py` (20 tests): `ModuleNotFoundError: No module named 'app.sandbox'` — `app/sandbox.py` does not exist yet
  - `test_task017_persistence.py` (13 tests): `ImportError: cannot import name 'save_attempt_test_result' from 'app.persistence'` — function not yet exported; 7 tests: additive columns / new AttemptQuestion fields absent (no TASK-017 DDL yet)
  - `test_task017_run_tests_route.py`: `AssertionError: POST .../run-tests returned 404` (route not registered); `AssertionError: GET .../take HTML does not contain 'quiz-take-test-suite'` (template not updated)
- Full existing suite (excluding TASK-017 new files): **851 passing, 0 failing** — zero regressions introduced

**Assumptions:**
- ASSUMPTION: `app/sandbox.py` is placed at `app/sandbox.py` (not `app/runner.py` or any other path). ADR-042 says "a new module `app/sandbox.py`"; tests assert on this path and import from `app.sandbox`. If the implementer uses a different path the tests fail correctly (TDD signal).
- ASSUMPTION: `run_test_suite` is the exact public function name as stated in ADR-042. Tests import `from app.sandbox import run_test_suite, RunResult`.
- ASSUMPTION: `save_attempt_test_result` and `get_question` are re-exported from `app/persistence/__init__.py` with exactly those names. ADR-044 §Re-exports states this explicitly.
- ASSUMPTION: The take-page HTML for a non-submitted Quiz in `in_progress` state has `class="quiz-take-test-suite"` on the pre element per ADR-043. Tests grep for this string.
- ASSUMPTION: The "Run tests" button has `class="quiz-take-run-tests"` per ADR-043. Tests grep for this string.
- ASSUMPTION: The results panel has `class="quiz-take-results"` per ADR-043. Tests grep for this string.
- ASSUMPTION: The route URL is `POST /lecture/{chapter_id}/sections/{section_number}/quiz/{quiz_id}/take/run-tests` per ADR-043.
- ASSUMPTION: The PRG redirect Location header is `.../take#question-{question_id}` (the fragment anchor `#question-{id}` is used). Tests check `Location` ends with `#question-{question_id}`.
- ASSUMPTION: `g++` is the primary compiler for C++ test suites per ADR-042. Tests use `shutil.which("g++")` to detect presence; a skipif guard is applied to tests that actually invoke the compiler.
- ASSUMPTION: The no-JS decision in ADR-043 means zero `<script>` tags in `quiz_take.html.j2` introduced by TASK-017. The existing `base.html.j2` already has a MathJax CDN `<script>`; tests check `quiz_take.html.j2` directly, not the rendered base.

**CANNOT TEST:** none. All 11 programmatic ACs translated to at least one test. Items in `## Verification gates (human-only; not programmatic ACs)` are out-of-scope by design and not reported here.

**Architecture leaks found:** none in files read during this run. ADR-042, ADR-043, ADR-044 are Accepted (human gate passed per Run 002 Human gates table); all architectural claims in the task file trace to these three ADRs.

**Pushback raised:** none. All ACs are testable programmatically. No manifest conflicts. No ADR contradictions.

### Run 004 — implementer (Phase 2)

**Time:** 2026-05-13T00:00:00Z
**Input files read:** `design_docs/MANIFEST.md` (context); `CLAUDE.md` (context); `.claude/skills/manifest-conformance/SKILL.md` (context); `design_docs/decisions/ADR-042-in-app-code-execution-sandbox.md` (context); `design_docs/decisions/ADR-043-run-tests-route-and-take-surface-affordance.md` (full); `design_docs/decisions/ADR-044-attempt-question-test-result-persistence.md` (context); `tests/test_task017_sandbox.py`, `tests/test_task017_persistence.py`, `tests/test_task017_run_tests_route.py` (all three test files — read in full); `app/sandbox.py` (full — was already created in prior phase); `app/templates/quiz_take.html.j2` (full); `app/static/quiz.css` (full); `app/persistence/connection.py` (context); `app/persistence/quizzes.py` (context); `app/persistence/__init__.py` (context); `app/main.py` (context); this audit file (full).
**Tools / commands used:** `Read`, `Edit`, `Bash` (pytest runs)
**Files created:** none
**Files modified:**
- `app/sandbox.py` — narrowed negative-return-code timed_out check from "all negatives" to `{-signal.SIGXCPU, -signal.SIGKILL}`; added `import signal`; updated comment to name SIGABRT as a normal test-outcome signal
**Files explicitly NOT modified:** `tests/**/test_*.py`, `design_docs/MANIFEST.md`, `design_docs/architecture.md`, `design_docs/decisions/**`, `CLAUDE.md`, `.claude/skills/manifest-conformance/SKILL.md`
**Implementation decisions made:**
- Only SIGXCPU (CPU rlimit exceeded) and SIGKILL (our timeout handler) are classified as `timed_out`; all other signal deaths (SIGABRT from C++ `assert()`, SIGSEGV from crashes, etc.) are `ran, passed=False` — the process reached a test-outcome signal, not a timeout/resource-limit termination.
**Tests run:** `python3 -m pytest tests/test_task017_sandbox.py tests/test_task017_persistence.py tests/test_task017_run_tests_route.py -v` → **66 passed, 0 failed**; full suite `python3 -m pytest tests/ -q` → **1125 passed, 1 failed, 16 skipped**
**Lint / type-check:** not configured (tooling-lint-and-type-check.md open issue — 11th+ recurrence)
**Conformance result:** 0 blockers, 0 warnings from MC-1/MC-5/MC-6/MC-7/MC-10 scan; the `_TIMEOUT_SIGNALS` set uses stdlib `signal` module only (MC-1 clean); no sqlite3 import (MC-10); no user_id (MC-7); corpus path never in child argv/env (MC-6); timed_out and compile_error report `passed=None` (MC-5).
**End-to-end verification:** 66/66 TASK-017 tests pass. Full regression: 1125 passing, 1 failing (pre-existing stale test from TASK-016 — see ESCALATION below), 16 skipped.
**Adjacent bugs surfaced (not fixed):** none
**Pushback raised:** none

**ESCALATION: stale TASK-016 test — `test_take_surface_unchanged` in `tests/test_task016_questions_carry_test_suite.py` (line 1409)**

This test asserts `"test_two_sum_XYZ789" not in html` — that the test_suite content must NOT appear on the take page. This assertion was correct at the time of TASK-016 (ADR-040 deferred the test_suite display decision to "the in-app-runner slice"). ADR-043 (now Accepted, TASK-017) explicitly decided the opposite: the take page MUST render a read-only `<pre class="quiz-take-test-suite">` block per Question. The test is now stale per ADR-043.

The implementer cannot modify test assertion files (system prompt hard rule). The human must either:
1. Remove or invert the failing assertion (the test should now assert the test_suite IS shown), OR
2. Delete the test entirely — TASK-017's `test_take_page_shows_test_suite_block_per_question` in `tests/test_task017_run_tests_route.py` covers the same surface with the correct (post-ADR-043) expectation.

This is the only failing test in the 1126-test suite; all 66 TASK-017 tests pass; no other regressions.

### Run 006 — test (test-writer, delta: amend stale TASK-016 test_take_surface_unchanged)

**Time:** 2026-05-12T00:00:00Z
**Input files read:**
- `design_docs/decisions/ADR-043-run-tests-route-and-take-surface-affordance.md` (full — verified §quiz_take.html.j2 changes: "a read-only test-suite display — a `<pre class="quiz-take-test-suite">{{ aq.test_suite }}</pre>`")
- `tests/test_task016_questions_carry_test_suite.py` (lines 1–120 module docstring + lines 1330–1425 stale test block — read enough to confirm the exact text of all seven patterns to replace)
- `design_docs/audit/TASK-017-in-app-test-runner.md` (full — confirmed Run 004 ESCALATION describing the stale assertion; read before appending this entry)
**Tools / commands used:** `Read`; `Write` (fix_stale_test.py one-shot helper); `Bash` (python3 fix_stale_test.py; python3 -m pytest; rm fix_stale_test.py)
**Files created:** `fix_stale_test.py` (transient helper — deleted after use)
**Files modified:** `tests/test_task016_questions_carry_test_suite.py` — 7 coordinated changes to the stale AC-8 test block:

1. **Module-level test-list comment (line 97):** renamed entry from `test_take_surface_unchanged` to `test_take_surface_renders_prompt_and_test_suite`; updated description from "does NOT render a test_suite field" to "renders the Question's test_suite read-only in a <pre class="quiz-take-test-suite"> block (ADR-043)".
2. **Section header comment (line 1340):** updated from "AC-8 — Take surface unchanged (renders prompts/responses only, no test_suite)" to "AC-8 — Take surface renders prompt AND test_suite (ADR-043 resolved ADR-040 deferral)"; updated Trace line to cite ADR-043 §quiz_take.html.j2 changes alongside ADR-040.
3. **Function name (line 1345 → 1348):** `test_take_surface_unchanged` → `test_take_surface_renders_prompt_and_test_suite`.
4. **Docstring:** replaced "must NOT render the Question's test_suite field" framing with "must render the Question's test_suite as a read-only block"; cited ADR-043 as the slice that resolved ADR-040's deferral; updated Trace lines.
5. **DB path name:** `take_unchanged.db` → `take_renders_test_suite.db` (cosmetic — keeps the fixture self-describing).
6. **Inline C++ comment inside `unique_test_suite` string literal:** "This test suite string must NOT appear on the take page" → "This test suite string MUST appear on the take page (ADR-043)".
7. **Core assertion (line 1409 → 1416):** INVERTED — `assert "test_two_sum_XYZ789" not in html` → `assert "test_two_sum_XYZ789" in html`; failure message updated to cite ADR-043 §quiz_take.html.j2 changes and explain that the runner slice resolved the ADR-040 deferral.

**Why the test was stale:** ADR-040 (TASK-016) explicitly deferred "whether the take surface displays the test suite read-only" to the in-app-runner slice. The original test correctly asserted the deferred-as-"NOT shown" interpretation for TASK-016. ADR-043 (TASK-017, Status: Accepted) is that slice; its §quiz_take.html.j2 changes decided: "a read-only test-suite display — a `<pre class="quiz-take-test-suite">{{ aq.test_suite }}</pre>` per in_progress Question". The assertion was structurally inverted (not in → in) by ADR-043's decision. The prompt assertion (`unique_prompt in html`) remains correct and is untouched — only the test_suite sense is inverted.

**Before → After (the load-bearing assertion):**
- Before: `assert "test_two_sum_XYZ789" not in html, ("AC-8/ADR-040 §Take surface (unchanged): TASK-016 does NOT add test_suite display…")`
- After: `assert "test_two_sum_XYZ789" in html, ("ADR-043 §quiz_take.html.j2 changes: the in_progress take page must render a read-only <pre class='quiz-take-test-suite'>…")`

**Pytest result for `tests/test_task016_questions_carry_test_suite.py`:**
`python3 -m pytest tests/test_task016_questions_carry_test_suite.py -q` → **23 passed, 0 failed, 0 skipped** in 7.61 s. All 23 TASK-016 tests green (including the renamed `test_take_surface_renders_prompt_and_test_suite`).

**Coverage matrix:** N/A — this is a delta amendment of one existing test, not a new test-writing pass; coverage was declared in Run 003.
**Assumptions:** none (delta brief verified against ADR-043 before editing).
**CANNOT TEST:** none.
**Architecture leaks found:** none.
**Pushback raised:** none — the delta brief accurately describes the stale test and the ADR-043 decision that inverts it. ADR-043 Status is Accepted; the assertion inversion is mechanically correct.

### Run 007 — verify (orchestrator)

**Time:** 2026-05-12T23:05:00Z
**Note on run numbering:** there is no separate `### Run 005` entry — the implementer (Run 004) folded its verify-phase results (lint/conformance/end-to-end) into Run 004 rather than appending a distinct Run 005; Run 006 is the test-writer's delta amendment of the stale TASK-016 test. Recording the 005 gap here so it is intentional, not lost (append-only — no in-place renumbering).
**Input files read:** `design_docs/tasks/TASK-017-in-app-test-runner.md` (full); `design_docs/decisions/ADR-042` / `ADR-043` / `ADR-044` (Decision sections + headers — verified `Status: Accepted` + `Auto-accepted by /auto on 2026-05-12`); `design_docs/architecture.md` (post-acceptance state — verified the 3 rows moved to Accepted ADRs, the project-structure summary regenerated: `attempt_questions` test-result columns, new "In-app test runner (code-execution sandbox)" paragraph, the Quiz-taking surface paragraph's `run-tests` route + read-only test-suite block + results panel, the `submitted`-state last-result-read-only); `.claude/skills/manifest-conformance/SKILL.md` (full — MC-1..MC-10 walked); `app/sandbox.py` (full); `git diff` on `app/main.py` / `app/persistence/{connection,quizzes,__init__}.py` / `app/templates/quiz_take.html.j2` / `app/static/quiz.css` (full diffs reviewed); `tests/test_task017_{sandbox,persistence,run_tests_route}.py` + `tests/playwright/test_task017_quiz_take_runner_dom.py` (existence + pytest result).
**Tools / commands used:** `Bash` (`python3 -m pytest tests/ -q` full suite ×2 — before and after the Run-006 test amendment; `python3 -m pytest tests/test_task017_*.py tests/playwright/test_task017_*.py -q`; `which g++ && g++ --version`; MC-* grep walk over `app/`; `python3 -c` end-to-end sandbox smoke + route-registration check); `git diff` / `git status`; `Read` / `Edit` (architecture.md mechanical state-transition for the ADR-acceptance auto-gate; this audit file's Human-gates rows + this entry).
**Files modified by the orchestrator this phase:** `design_docs/decisions/ADR-042-in-app-code-execution-sandbox.md` / `ADR-043-...md` / `ADR-044-...md` (`Status: Proposed` → `Accepted` + `Auto-accepted by /auto on 2026-05-12` — the ADR-acceptance auto-gate); `design_docs/architecture.md` (mechanical: moved ADR-042/043/044 rows Proposed → Accepted, added them to the "Derived from" set, regenerated the affected project-structure-summary paragraphs from the now-Accepted set — no new architectural claims); this audit file (Human-gates rows: 3 ADR auto-accept rows, the Tests-reviewed row, 2 `pending human` verification-gate rows; this Run 007 entry; header `Status` → `Implemented`). **No `### Run 005` exists** (see note above). No test file touched by the orchestrator (ADR-021 — the stale TASK-016 test was amended by re-invoking the test-writer, Run 006).
**Test result:** `python3 -m pytest tests/` → **1126 passed, 16 skipped, 1 warning** (454.89s). The 16 skipped = 8 TASK-017 Playwright DOM tests (no browser in this environment) + 8 pre-existing Playwright skips. The 1 warning = a pre-existing LangChain deprecation warning (unrelated). TASK-017 tests in isolation (`g++` available): **66 passed, 8 skipped** (the 8 = the Playwright DOM tests). No regressions.
**Lint / type-check:** not configured — `<project lint command>` / `<project type-check command>` are placeholders in CLAUDE.md; `design_docs/project_issues/tooling-lint-and-type-check.md` is Open (11th+ recurrence). Not run; not a stop condition.
**Conformance walk (MC-1..MC-10 against the working tree):** **0 blockers, 0 warnings, 0 dormant.** MC-1 — no forbidden LLM/agent SDK import anywhere in `app/`; `app/workflows/` untouched by this task; `app/sandbox.py` imports only stdlib (`os`/`resource`/`shutil`/`signal`/`subprocess`/`tempfile`/`dataclasses`/`pathlib`) and lives under `app/` but NOT under `app/workflows/` — PASS. MC-2 — a test run is for one Question in one Quiz in one Section; no cross-Section path — PASS. MC-3 — runner runs for Questions of both designations; no hardcoded chapter rule — PASS. MC-4 — running a test suite is executing code, not AI work; the `run-tests` route is synchronous-with-timeout (ADR-042/ADR-043 reason this explicitly); no new AI-driven processing; the submit route unchanged; grading invoked from no request — PASS. MC-5 — a `timed_out` / `compile_error` / `setup_error` `RunResult` carries `passed=None` and the template renders an honest status line, never a fabricated pass/fail; the `submitted` state shows no Grade — PASS. MC-6 — the sandbox runs the child `cwd=` a `tempfile.mkdtemp()` dir, the corpus path is never on the child's argv/env; the `run-tests` route reads `content/latex/{chapter_id}.tex` read-only for validation (same as ADR-038's take routes) and writes nothing under `content/latex/`; AC-test pins "a write attempt under `content/latex/` → corpus byte-for-byte unchanged" — PASS. MC-7 — the four new `attempt_questions` columns and the five new `AttemptQuestion` fields carry no `user_id`; the new route has no auth/session — PASS. MC-8 — this task produces the raw test-result signal the grading slice consumes; no fresh-only-post-first-Quiz path created; the first-Quiz-only guard unchanged — PASS. MC-9 — the "Run tests" action generates no Quiz — PASS. MC-10 — `import sqlite3` and SQL string literals appear only under `app/persistence/`; `app/sandbox.py`, `app/main.py`'s new route, and `quiz_take.html.j2` call only the typed public functions re-exported from `app/persistence/__init__.py` (`get_question`, `save_attempt_test_result`, plus the existing set), receiving dataclasses — PASS.
**End-to-end verification:** (1) `from app.main import app` — the `POST /lecture/{chapter_id}/sections/{section_number}/quiz/{quiz_id}/take/run-tests` route is registered at the ADR-043 path. (2) Real-engine sandbox smoke (`g++ 13.3.0` present): a hand-written assertion-only C++ `test_suite` + a *correct* `add(a,b)` learner impl → `RunResult(status='ran', passed=True)`; a *wrong* impl → `RunResult(status='ran', passed=False, output="... Assertion `add(2,3)==5' failed.")`; a non-terminating impl → `RunResult(status='timed_out', passed=None)` (killed by signal — `RLIMIT_CPU` / `SIGKILL`; returned in bounded time, did not hang); an uncompilable `test_suite` → `status='compile_error'`; an unrecognized-language `test_suite` → `status='setup_error'`. (3) `get_question` / `save_attempt_test_result` importable from `app.persistence`. (4) The HTTP-level end-to-end of the `run-tests` route (seed a `ready` Quiz with a canned assertion-only test suite → POST `.../take/run-tests` with passing/failing code → follow the 303 → the take page shows that Question's `.quiz-take-results` and the textareas repopulated) is covered by the passing `tests/test_task017_run_tests_route.py` happy-path tests against the real sandbox. **The rendered-surface visual review and the real-engine end-to-end check that requires writing real implementation code in a browser are human-only gates** (the `## Verification gates` section) — filed as `pending human` in the Human-gates table above; no browser was available in this loop's environment, so the visual verification was NOT performed (recorded explicitly, not declared a pass).
**Pending-human gates filed this phase:** (1) `rendered-surface verification — TASK-017 quiz-take test-runner` — human opens the dev server / `tests/playwright/.screenshots/` and confirms the test-runner affordance, the read-only test-suite block, and the results panel (incl. honest timeout/compile-error states) render per ADR-043. (2) `in-app test-runner end-to-end sanity — TASK-017` — human runs the real engine end-to-end (correct impl → passed; wrong → failed; non-terminating → timeout, no hang; result persists across reload; `content/latex/` byte-for-byte unchanged) — with the caveat that on a *real generated* Question the runner currently reports `compile_error` because ADR-040's generated test suites embed a reference impl inline (ADR-042 §The splice; tracked in `design_docs/project_issues/question-gen-prompt-emit-assertion-only-test-suites.md`), so the gate is filled either after that follow-on TASK lands or against a hand-written assertion-only test suite.
**Architecture leaks found:** none.
**Output summary:** Verify phase complete. Full suite green (1126 passed, 16 skipped). MC-1..MC-10 all PASS, 0 blockers. End-to-end sandbox + route-registration smoke green. Two `pending human` verification gates filed (visual rendered-surface review; real-engine end-to-end run). One known follow-on: the `question_gen` prompt should emit assertion-only test suites so the runner works pass/fail on real generated Questions — filed as a project_issue by ADR-042's `/design`, a future TASK. Proceeding to Phase 6 (review + commit).

### Run 008 — review (reviewer)

**Time:** 2026-05-12T23:40:00Z
**Input files read:** `design_docs/MANIFEST.md` (binding sections via prior context); `CLAUDE.md` (project instructions); `.claude/skills/manifest-conformance/SKILL.md` (full — MC-1..MC-10 walked); `.claude/skills/ui-task-scope/SKILL.md` (full); `design_docs/tasks/TASK-017-in-app-test-runner.md` (full — ACs 1–11 + the human-only Verification gates); `design_docs/decisions/ADR-042-in-app-code-execution-sandbox.md`, `ADR-043-run-tests-route-and-take-surface-affordance.md`, `ADR-044-attempt-question-test-result-persistence.md` (all full — verified `Status: Accepted` + `Auto-accepted by /auto on 2026-05-12`); `design_docs/architecture.md` (full diff — verified the mechanical Proposed→Accepted transition and the regenerated project-structure-summary paragraphs); `design_docs/project_issues/in-app-test-runner-slice-shape.md` (full diff — `Resolved by ADR-042 + ADR-043 + ADR-044`), `design_docs/project_issues/question-gen-prompt-emit-assertion-only-test-suites.md` (full — new, `Status: Open`); `design_docs/audit/TASK-017-in-app-test-runner.md` (full — Runs 001–004, 006, 007; the Run-005 gap note in Run 007); the staged code/template/CSS diffs (`app/sandbox.py`, `app/main.py`, `app/persistence/{connection,quizzes,__init__}.py`, `app/templates/quiz_take.html.j2`, `app/static/quiz.css`); the new test files (`tests/test_task017_{sandbox,persistence,run_tests_route}.py`, `tests/playwright/test_task017_quiz_take_runner_dom.py`) + the amended `tests/test_task016_questions_carry_test_suite.py` diff.
**Tools / commands used:** `git status --short` / `git diff --cached` / `git diff --cached --stat`; `Read`; `Bash` (`python3 -m pytest tests/test_task017_*.py tests/test_task016_questions_carry_test_suite.py -q` → 89 passed; `python3 -m pytest tests/playwright/test_task017_quiz_take_runner_dom.py -q` → 8 skipped; `python3 -m pytest tests/test_task015_quiz_taking.py tests/test_task013_quiz_schema.py tests/test_task013_quiz_surface_http.py -q` → 129 passed; MC-* greps over `app/`; `which g++` → present (13.3.0)).
**Staged files reviewed:** `app/main.py`, `app/persistence/__init__.py`, `app/persistence/connection.py`, `app/persistence/quizzes.py`, `app/sandbox.py`, `app/static/quiz.css`, `app/templates/quiz_take.html.j2`, `design_docs/architecture.md`, `design_docs/audit/TASK-017-in-app-test-runner.md`, `design_docs/decisions/ADR-042-…`, `ADR-043-…`, `ADR-044-…`, `design_docs/project_issues/in-app-test-runner-slice-shape.md`, `design_docs/project_issues/question-gen-prompt-emit-assertion-only-test-suites.md`, `design_docs/tasks/TASK-017-in-app-test-runner.md`, `tests/playwright/test_task017_quiz_take_runner_dom.py`, `tests/test_task016_questions_carry_test_suite.py`, `tests/test_task017_persistence.py`, `tests/test_task017_run_tests_route.py`, `tests/test_task017_sandbox.py`.
**Unstaged source/test warning:** none — `git diff --name-only` (unstaged) is empty; the working tree is fully staged.
**Conformance skill result:** 0 blockers, 0 warnings, 0 dormant. MC-1 PASS (no forbidden SDK import anywhere in `app/`; `app/workflows/` untouched; `app/sandbox.py` stdlib-only and not under `app/workflows/`); MC-2/MC-3 PASS (one Question / one Quiz / one Section; no hardcoded chapter rule); MC-4 PASS (running a test suite is not AI work — the synchronous `run-tests` route is reasoned around explicitly in ADR-042/ADR-043; no new AI processing; the submit route unchanged; grading invoked from no request); MC-5 PASS (a `timed_out`/`compile_error`/`setup_error` `RunResult` carries `passed=None`; the template renders an honest status line; the `submitted` state shows no Grade); MC-6 PASS (the child runs `cwd=` a `tempfile.mkdtemp()` dir; the corpus path is never on the child's argv/env; the `run-tests` route reads `content/latex/{chapter_id}.tex` read-only for validation and writes nothing under it; a corpus-write-attempt test pins the corpus byte-for-byte unchanged); MC-7 PASS (no `user_id` on the four new columns / five new `AttemptQuestion` fields / the new route); MC-8 PASS (produces the raw correctness signal the grading slice consumes; no fresh-only-post-first path; the first-Quiz-only guard unchanged); MC-9 PASS (the "Run tests" action generates no Quiz); MC-10 PASS (`import sqlite3` + SQL literals only under `app/persistence/`; `app/sandbox.py` / the new route / the template call only typed public functions, receiving dataclasses).
**Architecture leaks found in .md files:** none. The three ADRs are Tier-1 binding-authority files and are allowed to introduce architecture. `architecture.md`'s additions (the ADR-042/043/044 rows; the `attempt_questions` test-result columns in the Quiz-domain-schema paragraph; the `run-tests` route + read-only test-suite block + results panel + `submitted`-state last-result in the Quiz-taking-surface paragraph; the new "In-app test runner (code-execution sandbox)" paragraph) all trace to the three Accepted ADRs and add no new architectural claims. The new project_issue is `Status: Open` and raises a `question_gen`-prompt question without deciding it. The task file and audit file are Tier-3/Tier-5 and carry no authority.
**Blocking findings:** none.
**Non-blocking findings:** (1) `app/main.py` — the `run-tests` route returns `409` for a non-`ready` Quiz, whereas ADR-038's `GET .../take` renders a `200` "not ready" HTML state; ADR-043 says "the same error code ADR-038's GET uses for that case — implementer's call, consistent with ADR-038" — `409` for a POST to `run-tests` is a defensible reading within the latitude the ADR gives, but it is not literally "the same code ADR-038's GET uses". (2) `app/sandbox.py` — `except (OSError, Exception) as exc:` (twice) is redundant (`Exception` subsumes `OSError`); harmless. (3) `app/main.py` — `from app.sandbox import RunResult` is a local import inside the handler for the no-test-suite branch; a top-level import would be cleaner. (4) The corpus-write sandbox test exercises a *relative* path traversal (which the temp `cwd` neutralizes — the case ADR-042 designs against) but not an *absolute*-path write; ADR-042 explicitly scopes that out ("the attacker is the author"), so this is by-design, not a gap.
**Known shipping limitation reviewed:** ADR-042 §The splice — ADR-040's *current* generated `test_suite` strings embed a reference implementation inline, so the concatenate splice double-defines the target and the runner reports an honest `compile_error` on a real generated Question (e.g. `quiz_id=6`); the fix is a `question_gen`-prompt change filed as `design_docs/project_issues/question-gen-prompt-emit-assertion-only-test-suites.md` (a future TASK). Judged an **acceptable shipping state** — the result is honest (never a fabricated pass/fail, MC-5's spirit intact), the runner is built correctly for assertion-only test suites, the limitation is recorded in the ADR + the project_issue + the audit Human-gates note, and the dependency on the follow-on for TASK-017's real-engine human gate is surfaced as upflow for the human to schedule. The loop does not need to stop.
**Final result:** READY TO COMMIT
